## 2026-06-24 start
- Changed: created Ralph run folder for the final moderating platform plus realtime ontology planning work.
- Verified: current branch is main and synced to origin; only pre-existing untracked CSV files are present.
- Learned: `/workshop-graph/` is currently a static JSON graph app, while live moderator tools use separate Google Sheet polling and Supabase `climate_vote` patterns.
- Next: P1 and R1 planning artifacts.

## 2026-06-24 P1-R1
- Changed: added two implementation handoff specs: moderating platform plan and realtime transcription-to-ontology plan.
- Verified: parallel read-only audits confirmed the current state: moderator platform core should build on `CanvasBoard + JoinForm + useRealtimeAgendas + useAuth`; `/workshop-graph/` should first consume reviewed live JSON exports before a DB-backed adapter; raw transcript/audio must stay private and review-gated.
- Learned: the first blockers are not UI polish but DB contract/RLS/realtime publication, canvas write hardening, and a review queue/export loop.
- Next: V1 closeout, commit, push.

## 2026-06-24 V1
- Changed: marked all Ralph stories complete after document verification.
- Verified: `git diff --check` passed; grep checks found the required platform core, safety gates, live JSON first slice, no-AI-consensus rule, and private transcript/RLS constraints in the specs.
- Learned: implementation should proceed next as small slices, starting with DB contract freeze and canvas write hardening rather than live microphone capture.
- Next: commit and push.

## 2026-06-28 T0628
- Changed: added a separate 0628 participant open-question ontology page instead of changing the existing `/workshop-graph/` record surface.
- Verified: Playwright opened `/workshop-graph-0628-test/index.html`; state showed showcase mode, source `participant-open-questions`, count 75, edge labels on; stats showed 38 visible nodes, 81 visible edges, 38 node labels, and 81 edge labels. Hover card showed speaker `1조`, question `기후시민회의 운영 관련 질문`, and the full sentence text.
- Learned: the existing Miro/post-it resources are already in `src/components/BoardAgendaCard.astro`, `src/components/BoardZone.astro`, `src/pages/[lang]/moderator/board.astro`, and `public/board-demo.html`; 0628 should reuse their interaction tone without mutating archival graph pages.
- Next: commit and push the isolated 0628 test slice.

## 2026-06-28 GWS
- Changed: created the Google Sheet `0628 참여단 주관식 질문 응답 - 운영 감축` and documented the live Sheet URL, CSV URL candidate, and Form-to-Sheet checklist in `docs/participant-open-question-ontology-test.md`.
- Verified: Google Sheets range `Form Responses 1!A1:F2` contains the expected headers and one sample row for the two open-question fields.
- Learned: the current Google Drive connector can create Sheets but not Google Forms; headless browser access to `forms.new` stops at Google sign-in, so Form creation and response-Sheet linking must happen in a logged-in browser.
- Next: user creates the Google Form and links it to the prepared Sheet, then run the CSV URL command to refresh the 0628 graph JSON.

## 2026-06-28 GWS-CLI
- Changed: connected `googleworkspace/cli` (`gws 0.11.1`) with Forms body, Forms responses readonly, Drive, Drive file, and Sheets scopes; created Google Form `0628 참여단 주관식 질문 - 운영 감축` with the three required questions and a gws-owned response Sheet.
- Verified: `gws forms forms get` returned formId `1yktkA_XAMGcVt4mlnC-0Yc3d3N0N0YQ__Dk1TfdTaCc`, published response URL, and all three required question items; `gws sheets spreadsheets values get` read the gws Sheet `Form Responses 1!A1:D2` headers and sample row.
- Learned: `gws forms forms create` works after clearing stale token cache and reauthing with explicit scopes; Google Forms API still does not expose response destination linking to an existing spreadsheet, so the last Form UI `Responses` to Sheet selection remains manual.
- Next: link the created Form to the prepared Sheet in the Forms UI, then refresh ontology JSON from the Sheet CSV URL.

## 2026-06-28 shared-memory-analysis
- Changed: added `docs/codex-claude-shared-memory-2026-06-28.md` as the shared coordination note for Codex and Claude, and expanded `docs/participant-open-question-ontology-test.md` with ontology/text-network analysis methods beyond frequency and betweenness.
- Verified: the shared note records the active working root, current 0628 URLs, design baseline, non-overlap rule for `질문`/`소감` hubs, QR PNG requirement, and the last verified deployed commit.
- Learned: the day-of analysis should show facilitator-facing Korean labels (`많이 나온 것`, `이어주는 질문`, `유사 묶음`, `논의 프레임`) while keeping English metric field names internally.
- Next: implement the next analysis slice in JSON generation/UI only after checking this shared memory and avoiding duplication with parallel Claude work.

## 2026-06-28 advanced-analysis-json
- Changed: extended `scripts/build-participant-question-ontology.mjs` so generated 0628 JSON includes PageRank, frequency terms, similarity clusters, per-node analysis fields, and review-only link candidates.
- Verified: regenerating `public/workshop-graph-0628-test/data/participant-open-questions.json` from the current 40-row test snapshot produced 80 responses, 90 nodes, 362 edges, 120 similarity edges, 22 similarity clusters, 24 link candidates, 24 frequency terms, and 8 theory lens nodes.
- Learned: n-gram tokens are useful for similarity but too noisy for facilitator-facing frequency labels, so analysis display terms now use a stricter filter.
- Next: integrate the UI-side overlap and edge-label check from the parallel worker, then expose analysis categories in the 0628 view.
