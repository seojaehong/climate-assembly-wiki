# Dependencies
node_modules/
.pnpm-store/
.yarn/
package-lock.json.bak

# Data file backups (do not commit)
src/data/*.bak
src/data/*.bak.*
src/data/*.json.bak

# Astro
dist/
.astro/
.output/

# Build / cache
.cache/
.tmp/
.turbo/
.vercel/
.wrangler/

# Pagefind
public/pagefind/
pagefind/

# Scratch render files (PNG 추출용 임시 HTML — 노출/배포 제외, 로컬엔 보존)
public/tmp-postit.html
public/tmp-rounds/
public/tmp-*

# Environment
.env
.env.*
!.env.example

# Secrets / keys
*.pem
*.key
*.crt
secrets.json
.anthropic_key

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Local browser and generated verification artifacts
output/

# Editor / IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db
desktop.ini
$RECYCLE.BIN/
ehthumbs.db

# Python (translation scripts may use python helpers)
__pycache__/
*.pyc
.venv/
venv/

# Translation cache
.translation-cache/
scripts/.cache/

# Internal subagent memory (not public)
.claude/

# Local workflow state (machine-specific paths and session counters)
.bkit/

build_output.txt
# Root package-lock.json is tracked for reproducible Node 20 installs.
dist_backup_tmp
dist_backup2
# 2026-06-22: 모든 dist_*·dist.bak.* 패턴 일괄 차단 (재발 방지)
dist_*/
dist.bak.*/
dist.bak.*

# Vote snapshots (PII·용량 → git 추적 금지, OneDrive 동기화로만 보존)
# export-snapshots-onedrive.mjs 기본 출력 폴더
.vote-snapshots/
# ONEDRIVE_EXPORT_DIR 사용자 지정 폴더도 명시 시 여기에 추가
