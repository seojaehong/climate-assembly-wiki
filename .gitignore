# Dependencies
node_modules/
.pnpm-store/
.yarn/
package-lock.json.bak

# Data file backups (do not commit)
src/data/*.bak
src/data/*.bak.*
src/data/*.json.bak

# Astro
dist/
.astro/
.output/

# Build / cache
.cache/
.tmp/
.turbo/
.vercel/
.wrangler/

# Pagefind
public/pagefind/
pagefind/

# Scratch render files (PNG 추출용 임시 HTML — 노출/배포 제외, 로컬엔 보존)
public/tmp-postit.html
public/tmp-rounds/
public/tmp-*

# Environment
.env
.env.*
!.env.example

# Secrets / keys
*.pem
*.key
*.crt
secrets.json
.anthropic_key

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Editor / IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db
desktop.ini
$RECYCLE.BIN/
ehthumbs.db

# Python (translation scripts may use python helpers)
__pycache__/
*.pyc
.venv/
venv/

# Translation cache
.translation-cache/
scripts/.cache/

# Internal subagent memory (not public)
.claude/
build_output.txt
# 2026-06-21: lockfile 추적 복원 — 빌드 재현성 (npm ci 가능)
# (이전 우려였던 sharp/@emnapi cross-platform 충돌은 npm install + 로컬 ci 검증으로 해소 확인)
# Cloudflare 빌드 깨지면 본 line 재활성화 + package-lock.json 다시 untrack
# package-lock.json
dist_backup_tmp
dist_backup2
