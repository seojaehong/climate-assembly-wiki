# 완료 판정 재검토 — 2026-09-12

상태: 미완료. 기존 report.json의 production_verified는 전체 요구사항 충족 판정으로 사용하지 않는다.

- 최신 TASK-169는 기록모더 정적 상태 요약, 카드 상단 5단계 표시 및 다음 행동 상시 노출, HQ 필터 선택·해제를 요구한다. 현재 AgendaProgressBoard.tsx의 요약은 team에서도 button이며 HQ 재선택 해제가 없다. 행동 버튼은 카드 본문 뒤에 있다.
- 같은 파일에 외부 세션의 송출 페이지 분할·전체 폭·고급 관리 UI 변경이 추가되는 것을 연속 diff로 확인했다. HqGate.tsx에도 외부 변경이 있다. 작업 소유권 확인 전 해당 파일을 편집·커밋하지 않는다.
- 이전 검증에서 기명 HQ 3명 로그인은 확인했지만 조 코드 091201 로그인은 실패했다. 과거 코드 회전이 원인일 가능성이 있으며 현재 유효 코드로 재검증해야 한다.
- 이전 실제 송출 검증은 1분과 화면만 열었다. 버튼 3개 존재를 실제 3분과 송출 검증으로 세면 안 된다.
- 운영 권고안 저장 및 새로고침 후 재조회 검증은 미완료다.
- 8/29 건수는 현재 자료 존재 증거이며 변경 전후 동일성 증거가 아니다.
- Docker 엔진 응답 확인. 기본 bash는 WSL 경로 문제로 실행 실패했다. Git Bash 첫 실행은 MSYS가 컨테이너 /tmp 경로를 Windows 경로로 치환하여 실패했다. MSYS_NO_PATHCONV=1로 재실행했다.
- 드라이런 로그: .tmp-verify/0912-postgres-resumed.log. 전체 E2E 완료 판정은 아직 하지 않는다.
- Notion TASK-169를 완료에서 진행중으로 정정했다.

## 이번 재실행 결과

- s23 및 s24 계약 검증 통과. 조 동시접속 제한, 라운드 중복 생성, HQ 인증 제한, 기명 계정 복구, 투표 마감 경합 검사 통과.
- 전체 스크립트는 CLI seed 단계에서 종료 코드 1로 끝났다. 따라서 드라이런 전체 통과 아님.
- 병렬 UI 변경은 실행 도중 a531256으로 커밋되었다. 해당 5개 파일의 새 버전을 이후 화면 검증 기준에 포함해야 한다.

기준 커밋: 0fa86377c7f0216b0dc3f29d3c4eeaf39bf02632. 이 보고서는 외부 변경과 분리하여 작성했으며 아직 커밋하지 않았다.

## 후속 수정 및 검증

- Docker 전체 드라이런: PASS, 166초. 검증 SQL은 호스트 파일로 저장하지 않고 임시 컨테이너의 0600 파일로 보관한다. 컨테이너 종료 시 함께 제거한다. `postgres-dry-run.json`에 82개 입력 파일의 SHA256 및 검사 결과를 보존했다. 운영 DB 연결·변경 0건이다.
- 무네트워크 UI: 36개 PASS. 9·8·8, 다중 조 배정, 3분과 송출 실제 열기, HQ 상태 필터 선택·재선택·전체 해제 및 카드 상태 필터링, 조별 정적 요약, 카드 상단 5단계 표시와 버튼 순서, 5단계 전이를 확인했다. 외부 요청 0건. `browser-fixture-report.json` 참조.
- 5단계 표시·정적 요약·필터 UI 수정은 공유 세션의 후속 커밋에 포함되었다. UI 검증 기준에는 6d69031, 4464680, 6af28fc 후속 변경을 포함했다.
- Astro 검사: 521개 파일, 오류 0·경고 0. 이후 개발 서버에 공유 node_modules의 실제 경로만 허용하도록 구성했다.
- 전체 테스트 최초 실행: 128개 파일 중 126개 통과, 2,229개 테스트 통과·2개 실패, 별도 1개 파일 로딩 오류. 실패한 배포 보고서 검사 2개는 컨테이너 파일 보관 계약 반영 후 해당 파일 100개 테스트 모두 통과했다.
- 기존 `scripts/decision-vote-console.test.mjs` 로딩 오류(SyntaxError)는 아직 미해결이다. Node 문법 검사와 직접 의존 모듈 import는 통과했으나 Vitest에서 재현된다. 전체 테스트 green으로 보고하지 않는다.
- 운영 브라우저 연결은 30초·45초 초기화 시간 초과로 검증을 재개하지 못했다. 운영 조 로그인·저장 후 재조회는 여전히 미완료다.
- 실행 로그: `.tmp-verify/0912-postgres-final.log`, `.tmp-verify/0912-visibility-fixture.log`, `.tmp-verify/0912-targeted-regressions.log`, `.tmp-verify/0912-visibility-tests.log`.

## 운영 실계정 저장 검증 — 13:01 KST

- `live-recording.json`: HQ 3명 모두 전체 9·8·8 조회 성공, 3분과 송출 실제 열기 성공.
- 회전된 비공개 코드로 1분과 1조 로그인 성공. 점검용 권고안 1건 저장 → 새로고침 후 동일 제목 재조회 → 대기·논의 중·초안 작성·조 확인·제출 완료 전이 성공. HQ에서도 제출 완료 상태를 재조회했다.
- 점검 권고안은 시민 의견과 구분한 문안으로 생성했으며, 완료 후 HQ가 보관했다. 임시 조 배정은 해제해 원래 상태로 복구했다. 조 및 HQ 연결은 로그아웃했다. 인증값은 산출물에 포함하지 않았다.
- 원본 이동 재검증: 13개 존재, 인벤토리 12개 SHA256 일치, 중복 HWP 2개 동일성 확인, Downloads 잔여 0개. 중복정리본은 이후 편집될 수 있는 작업본이므로 원본 보존 검증과 구분했다.
- e34074f 운영 배포 성공. 원격 테스트에서 기존 호스트 임시 SQL 파일을 가정한 자동화 테스트 1개가 실패하여 컨테이너 보관 계약으로 수정했다. 관련 26개 검사 통과. 접근성 검사에서 상태 요약의 group 역할과 접힌 운영 패널을 여는 검증 동작을 수정했다. 관련 자동화 테스트는 합계 57개 통과했다.
- 원격 전체 검사 재통과와 여러 조 동시 입력·재전송을 포함한 통합 드라이런의 최종 증거 확인은 남아 있다. 이 보고서는 전체 목표 완료를 선언하지 않는다.

## 운영 동시 저장 및 응답 유실 복구 — 13:12 KST

- `live-concurrent-recording.json`: 두 분과의 조가 별도 브라우저 세션에서 동시에 작성하고 새로고침 후 각각 재조회했다. 다른 분과의 기록이 조 화면에 노출되지 않음을 확인했다.
- 두 번째 저장은 서버 처리 후 응답만 차단했다. 같은 요청 ID로 재시도했고 HQ 재조회 결과 기록은 정확히 1건이었다. 첫 기록의 5단계 전이와 HQ 되읽기도 통과했다.
- 점검 기록 2건은 보관 처리했고 임시 배정 2건을 원상복구했다. 조 2개와 HQ 모두 로그아웃했다. 실제 시민 기록 삭제는 하지 않았다.
- f17b45c 원격 검사는 테스트 선택자 불일치 1개와 HQ 송출 버튼의 글자 대비 문제 2개(모바일·데스크톱)로 실패했다. 현재 화면 제목을 검사하도록 테스트를 맞추고 송출 버튼 배경을 어둡게 수정했다. 후속 원격 검사 통과 전에는 완료로 판정하지 않는다.

## 회귀검사 원인 정리

- 기존 CLI 테스트 로딩 오류는 Windows CRLF의 shebang이 Vite SSR 변환에서 import 뒤로 이동한 것이 원인이었다. 해당 CLI만 `.gitattributes`로 LF를 고정하자 기존 22개 테스트가 통과했다. 실행 코드와 데이터는 바꾸지 않았다.
- 공유 세션에서 HQ 산출물 탭을 숨겼다. 기본 진입은 진행상황판으로 유지하며 이전 저장 탭을 자동 복원하지 않는다. 기존 기록 검증은 HQ 인증이 필요한 명시적 경로 `/hq?ops=1&view=submissions`로 분리했다.
- 823c1a3 원격 전체 단위검사와 DB 검사는 통과했다. 같은 커밋의 접근성 검사는 46개 모두 통과했다. 기존 기록 회귀검사의 화면 경로 변경은 후속 de3540b에서 반영했다.
- 로컬 기존 기록 회귀는 기능 7개가 통과했으나 개발 서버 HMR WebSocket 시도로 격리 검사 1개가 실패했다. 로컬 결과를 최종 PASS로 쓰지 않으며 운영 빌드 CI 결과로 판정한다.
- 로컬 전체 단위검사 재실행은 결과 출력 없이 대기하여 중단했다. 전체 단위검사 증거는 동일 소스의 원격 CI를 사용한다. 로컬 중단을 성공으로 세지 않는다.
- de3540b 운영 빌드의 현장 입력 11개·기존 HQ 기록 8개 검사 모두 통과했다. 원격 전체 단위검사도 통과했으며 자동화 603개 중 1개는 `.gitattributes` 변경으로 기존 A4 초안의 해시가 달라져 실패했다. 생성기로 해당 파일 해시·크기 및 묶음 체크섬만 갱신했으며 관련 11개 검사 통과. DB 변경·운영 적용 승인 상태 변경은 없다.
