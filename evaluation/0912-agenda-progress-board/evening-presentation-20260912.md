# 1분과 9/12 저녁 발표 반영

## 사용자 확정 기준

- 1분과만 주제명 → 문제·배경 → 기대효과를 완성된 문장으로 기록하고 읽는다. 논의에 없는 내용을 자동 생성하지 않는다.
- 1조 6번, 2조 3번, 3조 4번, 4조 8번과 각 조의 새 주제 1개. 5조는 7번·5번.
- 19:20–19:40 조 내 공유·발표자 선정, 19:40–19:50 조별 두 주제를 합쳐 2분 발표, 19:50–20:00 내일 안내·폐회.
- 새 주제의 실제 문안은 조가 작성한다. 임의 주제·권고안 레코드는 만들지 않았다.

## 변경 및 보존

- 1분과 기대효과 입력을 기존 create/revise RPC의 expected_effect에 연결했다. 새 폼·수정·재전송 스냅샷·로컬 초안에 문장을 보존한다.
- 이전 초안에 기대효과 필드가 없으면 기존 서버값을 유지한다. 명시적으로 비운 값과 구분한다.
- 1분과 송출은 기대효과까지만 표시한다. 기존 권고 내용을 삭제하지 않는다. 발표 주제·시간 안내를 추가했다.
- 2·3분과 입력, DB 스키마, 권한, 최종 조 확인의 필수값은 변경하지 않았다. 오늘 발표 초안 저장과 최종 제출을 구분해 안내한다.
- 기존 fixture의 초안 저장 조건을 서버와 같이 제목 필수로 맞췄다. 조 확인에는 기존 세 필수값 검사를 유지한다.

## 검증

- 브라우저 fixture 42건 통과. 외부 HTTP 차단·WebSocket 차단, 운영 DB 요청 0건. 기대효과 저장 후 되읽기, 권고 내용 없이 초안 저장, 타 분과 입력 유지, 5단계·송출 확인.
- 전체 단위검사 128개 파일, 2256건 통과. 이후 추가한 초안 복원 검사를 포함한 관련 검사 32건 통과.
- 미전송 새 폼의 기대효과 직렬화·복원은 순수 함수 검사로 검증했다. fixture는 의도적으로 브라우저 재시작 복원을 꺼 두므로 브라우저 새로고침 검증으로 주장하지 않는다.
- 코드 검수: 입력 중 공백을 자르지 않고 저장 시에만 정리하도록 수정. null 서버값과 이전 초안의 누락값 호환 확인. 인증·DB 변경 없음.
- 화면 증빙: browser-fixture-report.json, team-recommendation-authoring.png. 타입검사 523파일 오류 0건. 로컬 실행 로그: evening-typecheck.log.

## 배포

- 운영 코드 d48430e136832284fc5b375b0e36c1687e6eb4eb. 배포 34685640431 성공. climate-assembly.org와 climate-assembly-wiki.pages.dev의 배포 manifest가 동일 커밋임을 확인했다.
- 실제 운영 주소의 네트워크 격리 fixture: 기능 검사 41건 통과. 마지막 외부 연결 시도 0건 검사는 차단된 요청 2건으로 실패했다. 운영 DB 요청은 0건. 전체 통과로 주장하지 않으며 evening-live-fixture-report.json에 실패를 그대로 보존했다.
- 로컬 네트워크 격리 fixture는 재실행해 42건 전부 통과했다.
- 차단된 외부 요청 출처는 static.cloudflareinsights.com이며 모두 차단했다. 접근성 CI 34685639492는 성공했다.
- 전체 CI 34685639475는 별도 자동화 검사에서 실패: 기존 s25의 agenda_edit_event 테이블이 compliance catalog에 누락되어 3건 실패, 600건 통과. 변경 전 b99dc3f의 CI 34685063218도 실패 상태였다. 이번 입력·송출 수정 범위 밖이므로 목록을 임의 수정하지 않았다.

## 19시 추가 심층 검증 — 발표 전 주의

검증 대상은 1분과 5개 조가 각각 두 주제에 제목·문제인식·기대효과를 작성하는 경로다. 기록 내용·배정·작업단계에 대한 운영 DB 쓰기는 하지 않았다.

### 범위 및 방법

- 운영 HQ 로그인으로 배정·열린 단계·연결 기기 수를 읽었다. 배포 manifest는 a024428이었다.
- 조 작성 화면은 실제 배포된 /mod 코드를 쓰되 인증·쓰기·되읽기 RPC를 브라우저에서 가로채 메모리 모델로 처리했다. 이는 실제 PostgreSQL 영구 저장 시험이 아니다.
- 5조 × 2주제 신규 저장, 새로고침 후 문장 일치, 미전송 신규 폼과 수정 초안의 복원, 응답 유실 재전송 중복 방지, HQ 10주제 송출을 검사했다.
- 발표화면을 열어둔 상태에서 조 화면의 기대효과를 수정·저장하면 폴링으로 갱신되는지도 검사했다.
- 위 기능 47건은 통과했다. 테스트 문안 10건은 메모리 안에서만 존재했고 운영 기록은 생성·수정하지 않았다. 테스트 HQ 세션은 로그아웃했다. 실제 조의 연결은 종료하지 않았다.

### 발견 1 — 주제 번호 재색인 오류 (발표 전 수정 필요)

AgendaProgressBoard.tsx의 displayAgendaOrdinal이 전달받은 목록의 위치(index + 1)를 표시한다. 조별 API 목록은 배정 주제만 포함하므로 원래 번호와 달라진다.

| 조 | 원래 발표 주제 번호 | 조 화면 표시 |
|---|---:|---:|
| 1조 | 6 | 2 |
| 2조 | 3 | 2 |
| 3조 | 4 | 3 |
| 4조 | 8 | 4 |
| 5조 | 7 | 3 |

문장의 저장 키와 송출 내용은 주제 ID로 연결돼 유지되지만 번호 안내가 혼동을 준다. 원래 ordinal 유지 수정·배포 승인 요청을 사용자에게 제시했다. 테스트 요청 범위를 넘어 앱을 임의 수정하지 않았다. 수정 전에는 번호만 보지 말고 주제명으로 대조해야 한다.

### 발견 2 — 현장 준비

- 1조 활성 기기 2대, 2~5조 각각 1대. 신규 기기 로그인 거부 문구가 재현됐고 서버 계약도 조당 2대 제한이다. 기존 기기는 끊지 않았다.
- 모든 조의 기본 발표 주제 6·3·4·8·7과 5조의 5번이 배정돼 있다.
- 조회 시점 4조는 추가 주제가 아직 배정되지 않았다. 실제 새 주제 제목으로 추가해야 한다. 5조는 기존 7·5번을 쓰므로 추가 주제가 필요하지 않다.

증빙: division1-evening-deep-test.json, division1-evening-login-probe.json, division1-evening-projector.png. 실행: automation/verify-division1-evening.mjs (자격증명은 비공개 외부 폴더 참조, 결과에는 포함하지 않음).
