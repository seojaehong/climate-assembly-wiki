# 현장 운영 내비게이션 및 조 접속코드 검증

검증일: 2026-07-25

## 구현 범위

- 사이트 상단/모바일 내비게이션에 `현장 운영` 메뉴 추가
  - 모더레이터 콘솔 `/mod/`
  - 본부 현황 `/hq/`
  - 사용법 `/mod-help/`
- 행사일 `MMDD`와 공식 전체 조 순번 `NN`을 결합한 6자리 접속코드 생성
- 공식 15개 조의 이름→순번 매핑을 코드에 명시하고 테스트로 고정
- 이미 존재하는 조를 위한 원자적 관리자 SQL 출력 기능 추가
- 유출된 개별 코드의 무작위 재발급 경로 유지

## 조 번호 기준

`OFFICIAL_TEAM_ROSTER`가 단일 기준이다.

- 1분과 1조~5조: 01~05
- 2분과 1조~5조: 06~10
- 3분과 1조~5조: 11~15

8월 29일 세션 코드는 `082901`~`082915`로 계획된다.

## 자동 검증

- Vitest: 7개 파일, 106개 테스트 통과
- Astro check: 오류 0건
- 프로덕션 빌드: 통과
- dry-run: 15개 조와 `082901`~`082915` 출력 확인

## DB 반영 상태

Supabase SQL Editor의 `postgres` 역할로 `--print-sync-sql`이 생성한 단일 트랜잭션을 실행했다.

- 실행 결과: `Success. No rows returned`
- 사후 조회: 15행
- 코드 범위: `082901`~`082915`
- 매핑 확인: 1분과 1조~5조 `01`~`05`, 2분과 1조~5조 `06`~`10`, 3분과 1조~5조 `11`~`15`

서비스 역할 키로 처음 시도했을 때는 `climate_vote.session` 조회에서 `permission denied for table session`이 발생했다. 이 실패는 첫 조회에서 발생해 변경 건수는 0건이었다. 스키마 권한은 변경하지 않았으며, 관리자 트랜잭션 실행으로 기존 15개 코드를 원자적으로 변경·검증했다.

실행 로그:

- `evaluation/nav-code-dry-run.log`
- `evaluation/2026-07-25-sync-0829-join-codes.log`
