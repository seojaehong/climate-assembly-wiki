# 9/12~13 정본 계획 반영 최종 준비 보고서

- 작업일: 2026-09-06
- 정본: `0. 기후시민회의 제6-7차 회의 추진계획안-ADR수정.hwpx`
- 정본 SHA-256: `00952e23145bb41953abd2da6414656ed502204b4a9758f1e8e6de3ae6099c67`
- 계획 계약: `docs/operations/0912-13-plan-contract.json` (`0912-13-adr-final-v1`)
- 적용 범위: 사용자 화면, 모더레이터 도움말, HQ·보고서 상태 문구, 공개 세션 문서, 자동화 일정, 합성 리허설, 런북, 추적성·릴리스 검증

## 완료한 준비

1. 정본의 참가자 162명, 이틀 공식 시간, `조별 권고안 초안` 상태와 8개 디지털 미러 체크포인트를 기계 판독 가능한 계약으로 고정했다.
2. 참가자 화면의 과거 `세 꼭지`, `최종 제출`, 해결책 비작성 안내를 현재 단계·조별 초안 중심 문구로 교체했다.
3. HQ, 내보내기 보고서, 플랫폼 현황과 역할별 도움말의 상태 표현을 `제출 완료`로 통일했다.
4. 공개 9월 12·13일 세션 문서와 자동화 명칭을 의결이 아닌 권고안 초안 작성 흐름으로 고쳤다.
5. 합성 리허설을 정본 8단계의 ID·순서·제목·시각과 직접 결속했다.
6. 9월 12일 20:00 야간 인계, 9월 13일 08:30 재개 점검, 17:00 미완성 초안 이관 절차를 런북에 추가했다.
7. 정적 검증에 `PLAN-CANONICAL-ALIGNMENT` 요구사항과 `canonical-plan-contract` 검사를 추가했다.

## 승인 대기 경계

- PM 결정 8건은 계획 계약에서 모두 `pending`으로 유지했다.
- 작업 기준은 PM 확인 전까지 `현장 카드 정본·디지털 미러`다.
- 과거 6개 주제를 담은 기존 s20 migration과 verifier의 바이트 해시를 동결했다.
- 기존 s20 파일 수정·운영 DB 적용·라이브 조회·배포는 수행하지 않았다.
- PM 결정값을 반영한 교정 SQL과 사용자 명시 승인이 있기 전에는 `productionTopicActivationBlocked: true`를 해제할 수 없다.

## 검증 결과

| 검증 | 결과 |
|---|---|
| Node 런타임 | v20.18.0 |
| `npm.cmd run check` | 507개 파일, 오류 0건, 기존 힌트 58건 |
| 루트 Vitest 단일 워커 전체 | 124개 파일, 2,221개 테스트 통과 |
| automation Vitest 단일 워커 전체 | 37개 파일, 602개 테스트 통과 |
| 계획·일정 집중 테스트 | 43개 테스트 통과 |
| 참가자 화면 집중 테스트 | 126개 테스트 통과 |
| `npm.cmd run verify:0912:static` | 요구사항 15개, 검사 15개 전부 통과 |
| `git diff --check` | 오류 없음 |

병렬 전체 테스트는 Windows OneDrive I/O 경쟁으로 개별 CLI 테스트가 시간 제한을 넘겨, 동일 전체 집합을 단일 워커로 다시 실행했다. 단일 워커 결과는 루트 2,221개와 automation 602개 모두 통과했다.

## 최종 판정

PM 답변 전 수행 가능한 준비는 완료했다. 정본 계획의 의미 정합성은 자동 검증으로 보호되며, 남은 작업은 PM 결정 8건 반영, 승인된 교정 migration·verifier 작성, 사용자 DB 변경 승인, 실제 명단·현장·라이브 개통 검증이다. 따라서 현재 상태는 **준비 작업 완료 / 운영 개통 차단 유지**다.
