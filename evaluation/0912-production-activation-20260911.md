# 9/12 운영 시스템 개통 최종 점검

- 점검일: 2026-09-11 KST
- 운영 세션: `0912-deliberation`
- 현재 판정: 배포 전 최종 검증 완료

## 운영 데이터

| 항목 | 확인 결과 |
|---|---:|
| 조 | 15개 |
| 주제 | 8개 |
| 열린 주제 | 1개 |
| 참석자·배정·출석 | 각 156건 |
| 활성 운영자 | 8명 |

조별 입장 코드는 예측 가능한 준비용 값을 그대로 쓰지 않고 15개 모두 회전했다. 값 자체는 저장소와 이 보고서에 남기지 않았으며, 비공개 운영 파일만 생성했다.

## 기능·보안 확인

- 15개 조 모두 코드 교환, 세션 조회, 주제 조회, 로그아웃 성공
- 로그아웃 뒤 토큰 재사용 거부 확인
- 구형 조 로그인·주제 조회·공용 HQ 해제 경로 거부 확인
- HQ 실제 로그인에서 15개 조와 8개 주제 표시 확인
- 모더레이터 실제 로그인에서 1번 주제 연결 및 서버 저장·읽기 확인
- 운영 검증용 임시 문장은 서버에서 삭제하여 원상복구
- P2a 권한 감사 중 발견한 구형 `set_active` 실행 권한은 즉시 회수하고 영구 마이그레이션과 검증 항목에 반영

전체 P2a 검증문은 뒤쪽에 합성 세션을 생성·변경하는 폐기형 fixture가 포함돼 있어 운영 DB에서는 그 구간을 실행하지 않았다. 실제 권한 표면을 검사하는 앞부분은 통과했으며, 합성 fixture 구간은 운영 데이터 보호를 위해 제외했다.

## 코드 검증

- `astro check`: 오류 0건, 경고 0건
- 관련 테스트: 6개 파일, 238개 테스트 통과
- 9/12 정적 준비 추적: 15개 요구사항 통과
- 전체 빌드: 9,493개 페이지 생성 완료
- 전체 테스트: 123개 파일·2,200개 테스트 통과. 변경하지 않은 `scripts/decision-vote-console.test.mjs` 한 파일은 Vitest 수집 시 구문 로드 오류가 남아 있으며 Node 구문검사는 통과했다.

## 배포 후 마지막 확인

저장 직후 브라우저 새로고침에서 서버에 저장된 문장이 잠시 `미저장`으로 보이던 경쟁조건을 수정했다. 배포된 revision에서 실제 저장 → 새로고침 → `저장됨` 유지 → 임시 데이터 삭제 순서로 한 번 더 확인하고 이 보고서의 판정을 확정한다.

상세 기계 판독 결과는 `evaluation/0912-production-activation-20260911.json`에 있다.
