# 모더레이터 콘솔 핫픽스 프로덕션 검증

- 검증 시각: 2026-07-25 00:51 KST
- 대상 도메인: `https://climate-assembly.org`
- 배포 커밋: `edb0403d55cafa107f9714248ed68f22485c8a55`
- 검증 코드: `422040` (`1분과 1조`)

## 최종 판정

`/mod` 접속 불가로 보고된 세 가지 브라우저 문제에 대한 핫픽스는 프로덕션에 반영되었고, 실제 Chrome에서 조 코드 입력과 입장까지 정상 동작했다.

다만 실제 폰 카메라로 QR을 스캔해 `/v`에서 한 탭 투표를 제출하는 물리 기기 E2E는 이번 검증 범위에 포함되지 않았다. 이 항목은 기존 리허설 체크리스트의 미완 항목으로 유지한다.

## 프로덕션 브라우저 검증

캐시 우회를 위해 `/mod?verify=20260725-edb0403`로 접속했다.

| 검증 항목 | 결과 | 근거 |
|---|---|---|
| 페이지 렌더링 | 통과 | 타이틀 `기후시민회의 모더레이터 콘솔 [/mod]` 확인 |
| 다크 모드 색 반전 차단 | 통과 | `prefers-color-scheme: dark` 환경에서 본문 배경 `rgb(255, 255, 255)`, 제목 `rgb(17, 24, 39)` |
| 코드 박스 클리핑 | 통과 | 브라우저 실화면에서 6개 박스 전부 표시, 문서 가로 overflow 없음 |
| 최초 입력 포커스 | 통과 | 활성 요소가 `조 접속코드 6자리 입력` |
| 페이지 클릭 후 포커스 복구 | 통과 | 입력창 밖을 클릭한 뒤 활성 요소가 다시 코드 입력창으로 복구 |
| 조 코드 입력 | 통과 | `422040` 입력 후 자동 입장 |
| 입장 결과 | 통과 | `1분과 1조` 홈 화면, 투표 만들기·타이머 UI 확인 |
| 앱 동작 중단 오류 | 미발견 | 입장과 홈 렌더링 정상. Chrome 확장 메시지 채널 종료 로그는 있었으나 앱 스택 오류나 UI 중단은 없었음 |

## 라이브 경로 확인

| 경로 | HTTP | 확인 내용 |
|---|---:|---|
| `/mod` | 200 | 모더레이터 콘솔 타이틀 |
| `/v` | 200 | 참가자 투표 타이틀 |
| `/hq` | 200 | 운영 현황 타이틀 |
| `/mod-help` | 200 | 사용법 타이틀 |
| `/robots.txt` | 200 | `/mod/`, `/v/`, `/hq/`, `/mod-help/` 색인 차단 |

## 자동 검증

- 모더레이터 관련 Vitest: 5개 파일, 76개 테스트 통과
- `git diff --check`: 통과
- `npm.cmd run check`: 실패
  - 이번 핫픽스에서 변경하지 않은 기존 파일의 TypeScript 오류가 남아 있다.
  - 주요 오류: `VoteCard.tsx`의 `Eyebrow` `style` prop 타입, `mod-console.test.ts`의 readonly 배열, `supabase/functions/chat/index.ts`의 Deno/JSR 타입 해석
  - 핫픽스 커밋은 `ModConsole.tsx`와 4개 페이지의 라이트 모드 메타만 변경했으며, 위 오류 파일은 해당 커밋의 변경 범위가 아니다.

## 남은 현장 게이트

1. 실제 폰 카메라로 QR 스캔
2. `/v` 진입
3. 한 탭 투표 제출
4. `/mod`와 `/hq` 실시간 집계 반영 확인

현장 게이트는 `docs/superpowers/specs/2026-07-24-mod-console-rehearsal-checklist.md`의 W4 최우선 항목으로 유지한다.
