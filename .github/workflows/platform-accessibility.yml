name: Platform accessibility audit

on:
  push:
    paths:
      - 'src/islands/platform/**'
      - 'src/islands/result/**'
      - 'src/islands/OntologyReviewConsole.tsx'
      - 'src/islands/OntologyReviewConsole.test.ts'
      - 'src/islands/canvas/PrivateTranscriptCapturePanel.tsx'
      - 'src/islands/canvas/PrivateTranscriptCapturePanel.test.ts'
      - 'src/islands/canvas/ontology-review-workspace.ts'
      - 'src/islands/canvas/ontology-review-workspace.test.ts'
      - 'src/islands/canvas/private-transcript-capture.ts'
      - 'src/islands/canvas/private-transcript-capture.test.ts'
      - 'src/components/**'
      - 'src/layouts/**'
      - 'src/lib/**'
      - 'src/pages/**'
      - 'src/styles/**'
      - 'automation/platform-accessibility-audit.mjs'
      - 'automation/tests/platform-accessibility-audit.test.mjs'
      - 'automation/verify-platform-design-blueprint.mjs'
      - 'automation/tests/verify-platform-design-blueprint.test.mjs'
      - 'automation/platform-accessibility-manual-evidence.mjs'
      - 'automation/tests/platform-accessibility-manual-evidence.test.mjs'
      - 'evaluation/platform-accessibility-manual-evaluation.json'
      - 'automation/package*.json'
      - 'package*.json'
      - 'astro.config.mjs'
      - 'tsconfig.json'
      - '.github/workflows/platform-accessibility.yml'
  pull_request:
    paths:
      - 'src/islands/platform/**'
      - 'src/islands/result/**'
      - 'src/islands/OntologyReviewConsole.tsx'
      - 'src/islands/OntologyReviewConsole.test.ts'
      - 'src/islands/canvas/PrivateTranscriptCapturePanel.tsx'
      - 'src/islands/canvas/PrivateTranscriptCapturePanel.test.ts'
      - 'src/islands/canvas/ontology-review-workspace.ts'
      - 'src/islands/canvas/ontology-review-workspace.test.ts'
      - 'src/islands/canvas/private-transcript-capture.ts'
      - 'src/islands/canvas/private-transcript-capture.test.ts'
      - 'src/components/**'
      - 'src/layouts/**'
      - 'src/lib/**'
      - 'src/pages/**'
      - 'src/styles/**'
      - 'automation/platform-accessibility-audit.mjs'
      - 'automation/tests/platform-accessibility-audit.test.mjs'
      - 'automation/verify-platform-design-blueprint.mjs'
      - 'automation/tests/verify-platform-design-blueprint.test.mjs'
      - 'automation/platform-accessibility-manual-evidence.mjs'
      - 'automation/tests/platform-accessibility-manual-evidence.test.mjs'
      - 'evaluation/platform-accessibility-manual-evaluation.json'
      - 'automation/package*.json'
      - 'package*.json'
      - 'astro.config.mjs'
      - 'tsconfig.json'
      - '.github/workflows/platform-accessibility.yml'
  workflow_dispatch:

jobs:
  audit:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: automation/package-lock.json
      - name: Install site dependencies
        run: npm install --no-package-lock
      - name: Install audit dependencies
        working-directory: automation
        run: npm ci
      - name: Install Chromium
        working-directory: automation
        run: npx playwright install --with-deps chromium
      - name: Validate tracked manual evaluation evidence
        working-directory: automation
        run: npm run audit:platform-accessibility-manual -- --verify ../evaluation/platform-accessibility-manual-evaluation.json --expected-base-url https://climate-assembly.org --repo-root ..
      - name: Build static site
        run: npm run build
      - name: Start preview server
        run: |
          npm run preview -- --host 127.0.0.1 > /tmp/platform-preview.log 2>&1 &
          for attempt in {1..30}; do
            if curl --fail --silent http://127.0.0.1:4321/platform/ > /dev/null; then
              exit 0
            fi
            sleep 1
          done
          cat /tmp/platform-preview.log
          exit 1
      - name: Audit WCAG 2.2 AA subset, skip links, and responsive overflow
        working-directory: automation
        env:
          PLATFORM_A11Y_BASE_URL: http://127.0.0.1:4321
          PLATFORM_A11Y_REPORT: ../.artifacts/platform-accessibility-audit.json
        run: |
          rm -f ../.artifacts/platform-accessibility-audit.json
          npm run audit:platform-accessibility
      - name: Verify auth, design, review, and publish interactions
        working-directory: automation
        env:
          PLATFORM_A11Y_BASE_URL: http://127.0.0.1:4321
          PLATFORM_DESIGN_REPORT: ../.artifacts/platform-design-blueprint-browser.json
        run: |
          rm -f ../.artifacts/platform-design-blueprint-browser.json
          node verify-platform-design-blueprint.mjs
      - name: Upload audit evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: platform-accessibility-audit
          path: |
            .artifacts/platform-accessibility-audit.json
            .artifacts/platform-design-blueprint-browser.json
          if-no-files-found: error
