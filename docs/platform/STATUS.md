# 플랫폼 트랙 — 상태·병합 전 게이트

- 갱신: 2026-08-26
- 브랜치: `main`
- **백엔드 스키마와 프론트엔드는 2026-08-10 사용자 승인으로 프로덕션에 배포됨.** 기존 `/mod`·`/b`·`/hq`·`/v` 경로는 유지한다.

## ✅ 프로덕션 배포 (2026-08-10) — labor_money, 8/29 무영향
사용자 명시 승인 후 Management API 적용. P1·P2 적용(HTTP 201). anon 검증: result_get→200 null·issue_list/items→invalid join code·result_publish→hq 인증 요구(G2). 신규 테이블(org·membership·invitation·issue·issue_link·result_page)·RPC 존재 확인.
- **8/29 무영향 보장**: 순수 additive(신규 테이블·함수·nullable 컬럼, 기존 s1~s5 무변경) · P1 RLS 휴면(활성화 GRANT 미실행) · **issue_invalidate_guard 트리거는 submission_item에 부착 제외**(검수 단계 시작 시 부착) · p1b backfill/NOT NULL 미적용 · 기존 mod_join·ballot_list 정상 · 테스트 데이터 0건.
- 후속: (검수 시작 시) issue_invalidate_guard 트리거 부착 + 필요시 p1b backfill.

## ✅ 프론트엔드 라이브 배포 (2026-08-10) — climate-assembly.org
사용자 승인("지금 배포") 후 feat/deliberation-saas-platform → main fast-forward 머지(8034e0c..62aef0b)·push·배포 워크플로. 라이브 검증:
- **/platform → 200** (PlatformShell 아일랜드 로드), **/platform/o/... 딥링크 → 200**, **/r/* → 200** (SPA fallback: public/_redirects `/platform/*`·`/r/*` → 호스트 200 rewrite, 404 앞 배치).
- **★ 8/29 라우트 무영향**: /mod·/b·/hq·/v·/mod-help 전부 200.
- 프론트는 supabase.ts fallback=labor_money(커밋 .env 없음) → 방금 적용한 프로덕션 백엔드에 연결.
- /r SPA 호스트: getStaticPaths가 `/r/_/` 1개만 emit(실토큰 미열거). 클라이언트가 window.location에서 토큰 파싱.

## 런칭 상태 = 백엔드+프론트 프로덕션 배포·검증 완료
운영 온보딩(다음): Supabase Auth 운영자 계정 + membership 행 생성해야 /platform 콘솔 staff 경로 사용 가능(참여자·공개 결과는 무기명/토큰이라 즉시). 검수 단계 시작 시 issue_invalidate_guard 트리거 부착.

## 구현 완료 (코드 완성 · 빌드/테스트 통과)

플랜 **Phase 1(분석·검수·공개 파이프라인)** + Phase 2 스키마 토대까지:

| 슬라이스 | 산출물 | 커밋 |
|---|---|---|
| 빌드 스펙 | `docs/platform/BUILD_SPEC.md` (데이터 연결 대전제) | 6d5283b |
| P1 멀티테넌시 스키마 | org·membership·invitation + org 파생 헬퍼 + RLS | b020c84 |
| P2 분석·검수·공개 스키마 | issue·issue_link·result_page + 검수/공개 RPC + issue_items | 8b82150·62aabfc |
| 앱 스켈레톤 | 데이터 트리 네비(하드코딩 메뉴 없음)·Auth 경계·스코프 라우팅 | 2af1ce2 |
| 공개 결과 페이지 | `/r/<token>` 매트릭스·랭킹·4×6·표대체본·HITL | 2af1ce2 |
| 검수 콘솔 | 4×6 코딩·재분류·병합·미분류함 본문·게이트 | 62aabfc |

**최근 검증(2026-08-12):** 전체 src·scripts Vitest 60개 파일·883건, automation Vitest 18개 파일·206건, Astro check 오류 0건(기존 hint 49건), Node 20 정적 빌드 7,914페이지가 통과했다. clean commit `bdb66322d48082c6f6c2c849345721268e9ae26a`의 격리 정적 빌드에서 로그인·인증 후 운영 셸·접근성 성명·미공개/공개 결과·온톨로지 검수 초기 화면 6경로를 데스크톱·모바일 두 뷰포트로 감사한 12개 케이스가 모두 통과했고 자동 위반·자동 판정 불가는 각각 0건이었다. 자동 판정 불가는 이제 해당 케이스와 CLI를 실패시킨다. 당시 수동 보조기술 평가 템플릿은 같은 6개 표면의 12개 케이스·48개 필수 검사를 정의했고 48개 모두 `not_run`이라 전체 상태는 `needs_review`였다. [test run 31526593241](https://github.com/seojaehong/climate-assembly-wiki/actions/runs/31526593241)과 [접근성 run 31526593222](https://github.com/seojaehong/climate-assembly-wiki/actions/runs/31526593222)이 성공한 뒤 사용자 도메인에서도 같은 12개 케이스를 재실행해 12/12 통과·자동 위반 0건·자동 판정 불가 0건을 확인했다. 두 추적 JSON은 clean 감사 소스의 commit을 `sourceCommit`으로 보존하지만 사용자 도메인이 배포 revision을 기계 판독 가능하게 노출하지 않으므로 원격 `targetRevision`은 `not_verified`로 구분한다. 격리 불변식(RPC org_id 미전달)은 유지한다.

**A4 추가 검증(2026-08-13):** 설계 청사진 schema v4는 실제 assembly 계약의 목적과 운영 방식(`consensus|vote`), 개회 전 준비도 필수 항목(`topics_open|teams_active|roster_loaded`), 미래 생성 RPC에 필요한 회차 title·slug와 표준 조 이름을 날짜·주제·정원 계층에 함께 보존한다. 목적 길이·운영 방식·준비도 vocabulary·빈/중복 준비도·회차 slug 형식과 중복을 fail-closed하고, 기존 schema v1/v2/v3 파일은 목적 미지정·합의형·기본 3개 준비도와 결정적인 회차·조 식별정보를 붙여 v4로 복원한다. 집중 Vitest 29건과 verifier contract 6건, 루트 전체 63개 파일·904건이 통과했고 Astro check 오류는 0건(기존 hint 49건), Node 20 정적 빌드는 7,914페이지를 생성했다. Windows automation 전체는 기존 Playwright timeout 10건을 포함해 220/230이었고 변경 verifier contract는 별도 통과했다. 실제 Chromium은 인증 fixture로 production `/platform` 셸을 연 뒤 공론화 이름·slug·목적·투표형·공개 주제/활성 조 준비도 선택→2회차 이름·slug 입력→역전 날짜 거부→미리보기 생성·편집 즉시 무효화→schema v4 JSON 다운로드를 실행했다. 모바일 360px에서는 이름 있는 미리보기 표 영역의 실제 폭 280px·내용 폭 620px, 포커스와 End 키 스크롤을 확인했다. 이어 잘못된 JSON이 현재 초안을 덮어쓰지 않고 거부되는지, 내려받은 유효 JSON이 공론화 목적·운영 방식·선택 준비도와 두 회차의 title·slug·날짜·주제·조 이름·계획 인원을 복원하는지, 복원 후 편집이 미리보기를 다시 무효화하는지 확인했다. 지연된 파일 읽기 중 발생한 최신 편집도 이전 import가 덮어쓰지 못하도록 generation 경합을 검증했다. 내려받은 파일은 입력 계층과 대조한 뒤 합계를 재계산하며 `dryRun:true`, `databaseMutationExecuted:false`, `requiresApproval:true`를 확인했다. production DB 접근·DB mutation 시도·브라우저 오류·fixture 실패는 모두 0건이었다. 실제 생성 RPC·서버 저장과 배포된 readiness RPC의 정책 적용은 계속 승인 뒤 범위다. 로컬 working-tree 증거는 감사 대상 소스 트리 SHA-256과 Chromium 버전을 포함해 `evaluation/2026-08-13-platform-design-blueprint-browser.json`에 보존하고, clean checkout 증거는 push 뒤 platform accessibility CI에서 재실행한다.

**M1 추가 검증(2026-08-12):** Canvas DB contract preflight 집중 Vitest 8건과 automation 전체 Vitest 17개 파일·199건이 통과했고 Astro check 오류는 0건(기존 hint 49건)이다. synthetic 구조 fixture로 column/type/FK/constraint·operation/role RLS·GRANT·realtime/replica identity·RPC 보안 패턴을 검증하지만, 정적 분석은 SQL 의미를 승인하지 않으며 항상 `not_ready`와 `verification.semantic_review_required`를 남긴다. 현재 저장소에서도 DB/API를 호출하지 않고 종료 코드 1을 반환한다. `evaluation/canvas-db-contract.json`은 source commit·대상 트리 SHA-256·20개 migration·blocker 54건을 보존한다. 이 working-tree 증거는 `sourceTreeClean:false`이며 migration 적용, live DB 상태 또는 M1 승인 완료 증거가 아니다.

**M3 추가 검증(2026-08-12):** Canvas bridge·검수 큐 seed 집중 Vitest 15건, automation 전체 18개 파일·205건, 전체 src·scripts Vitest 57개 파일·868건이 통과했다. Astro check는 오류 0건·기존 hint 49건이다. 첫 병렬 전체 실행에서 기존 Canvas browser 정상 케이스가 5초 제한을 넘겼지만 단독 재실행 3건과 automation 단독 전체 205건은 모두 통과했다. 검증은 local dry-run·plan/seed checksum fail-closed·source plan 재생성 대조·public 출력 거부·검수 선확정 차단·다회차 provenance를 포함하며 DB/API/migration 적용은 포함하지 않는다.

**M0 운영 표면 연결 검증(2026-08-12):** 진행자 라이브와 캔버스 상단에 공용 플랫폼 내비게이션을 연결해 라이브 입력·캔버스 작업대·온톨로지 검수 큐·온톨로지 그래프·그래프 사용설명서 5개 표면을 한 경로로 묶고 현재 위치를 `aria-current`로 표시한다. 안내 문구는 시민 발언과 논증 관계를 보존해 숙의·모더레이션을 지원하지만 회의의 결정을 대신하지 않는다는 경계를 명시한다. 실제 Chromium 읽기 전용 검증에서 캔버스 4개 노드 hydration, 작업대 1440×934, 라이브 초기화 완료와 안정화 이후의 오류·쓰기 차단, 라이브·캔버스 내비게이션 연결, 그래프·가이드 backing asset 2건, Supabase 예상 GET 3건을 확인했고 쓰기 요청·브라우저 오류는 0건이었다. 전체 src·scripts Vitest 58개 파일·870건, automation Vitest 18개 파일·205건과 Astro check 오류 0건(기존 hint 49건)이 통과했다. 이 결과는 로컬 working tree의 동작 증거이며 사용자 도메인 배포 확인은 push 뒤 별도 수행한다. `noindex`와 운영 메뉴는 접근통제가 아니므로 Auth 적용 전 live 입력은 계속 비식별 simulation·승인된 공개 가능 source에만 한정해야 한다. DB/API 데이터와 migration은 변경하지 않았다.

**M3/M4 로컬 검수 작업대 검증(2026-08-12):** 공용 운영 내비게이션에 `/ko/moderator/ontology-review/`를 추가하고 sealed Canvas review plan과 정확한 원 snapshot을 브라우저 메모리에서만 대조하는 사람 검수 화면을 연결했다. 실제 Chromium은 plan·snapshot 업로드, node 수정 승인과 node·relation·cluster 반려 plan 다운로드, 같은 node ID를 가진 새 plan 재로딩 때 이전 입력 제거, 두 번째 plan의 node 2건·relation 2건·cluster 1건 승인 및 다운로드를 완료했다. 사람 검수된 Claim의 Evidence, Proposal의 Condition, 고립된 Concern 연결이 빠졌을 때만 결론이 아닌 진행 보조 질문을 만들며 source session·node ID를 함께 표시한다. 실제 두 번째 plan에서는 조건 관계가 없는 Proposal 질문 1건의 화면 출현까지 확인했다. 결과의 canonical 검수 시각·snapshot 43 출처·원문·선택 역할·관계 유형·대표 Issue·검수자와 `databaseMutationExecuted:false`, `publicGraphWritten:false`를 확인했다. 같은 실행에서 DB 쓰기 요청과 브라우저 오류는 0건이었고 graph·guide backing asset도 각각 2xx였다. 전체 src·scripts Vitest 60개 파일·882건, automation Vitest 18개 파일·205건, Astro check 오류 0건(기존 hint 49건), Node 20 정적 빌드 7,914페이지가 통과했다. working-tree Chromium 증거는 `evaluation/2026-08-11-canvas-dev-browser-evidence.json`에 `sourceTreeClean:false`로 정직하게 기록했으며 clean checkout 재실행은 push 뒤 CI에서 확인한다. 이후 Supabase Auth gate를 추가했고 세 검수 흐름의 reviewer를 현재 Auth 사용자 UUID에서 파생한 canonical `auth-user:<lowercase uuid>`로 결속해 자유입력을 제거했다. 결정 함수와 exporter도 임의 alias·변조 reviewer를 거부하고 이메일은 export하지 않는다. 다운로드 파일의 외부 서명·독립 신원 검증, DB queue/RLS/RPC, migration, 공개 graph 반영은 여전히 미구현·승인 대상이다.

### Phase A 점진 구현 (2026-08-11)

- **A2 기관 토큰 origin 결속 (2026-08-17)**: 탭별 기관 컨텍스트 헤더는 설정된 Supabase와 protocol·host·port가 정확히 같은 `/rest/v1/` 요청에만 붙인다. Auth 경로, 유사 호스트, HTTP downgrade, URL credential, 상대 경로에는 토큰을 전달하지 않으며 CI가 해당 회귀 테스트를 직접 실행한다. production DB·Auth·membership·GRANT는 변경하지 않았다.

**추가 실행 검증:** Canvas browser verifier Vitest 3건이 읽기 전용 정상 렌더, 비인증 드래그·쓰기 요청 차단, GitHub의 재현 가능한 Node 20 콜드 실행 계약을 검증했다. 루트 `package-lock.json`을 추적하고 Canvas 관련 변경 시 `npm ci` 뒤 강제 Astro 콜드 시작과 실제 브라우저 검증을 실행한다. 실제 Node 20 개발 서버의 비식별 증거 JSON과 스크린샷은 `evaluation/2026-08-11-canvas-dev-browser-evidence.json`과 `evaluation/2026-08-11-canvas-dev-browser.png`에 보존했다.

- **M1 Supabase contract preflight**: `automation/canvas-db-contract.mjs`가 production Canvas의 `session/participant/agenda/agenda_link/agenda_edit_log/rounds/attendance` operation과 attendance RPC를 20개 추적 migration의 최종 column/FK·RLS·operation/role policy·GRANT·realtime·SECURITY DEFINER 패턴과 읽기 전용으로 대조한다. 주석뿐인 SQL과 뒤 migration의 DROP/REVOKE/RLS disable을 증거로 세지 않고, matrix 밖 신규 table operation도 blocker로 만든다. 다만 정규식 기반 검사는 RLS/함수 본문 의미를 증명하지 않으므로 어떤 입력에도 M1 `ready`를 발급하지 않으며 `verification.semantic_review_required`를 유지한다. 6개 base table의 migration-owned 계약, 일부 attendance RPC의 explicit PUBLIC 회수, contract 사용자 승인, rollback SQL·stage rehearsal·role별 권한 테스트가 남아 있다. 정책·write failure·rollback 초안은 `docs/platform/CANVAS_DB_CONTRACT.md`에 분리했고 DB/API·환경변수·mutation은 사용하지 않았다. 이 진단은 live DB 부재나 M1 완료를 주장하지 않고, migration 작성·적용은 사용자 승인 뒤 별도 진행한다.
- **M2 캔버스 쓰기 안정화**: 진행자 캔버스의 이동·추가·보관·연결·편집·분류·투표·AI 보조·로그아웃 쓰기를 공통 결과 계약으로 감싸 실패를 로그와 화면 `alert`로 노출한다. 생성·연결·투표·변경 이력은 client-generated stable ID로 insert하고, `23505` 응답은 같은 ID의 저장 행을 다시 읽어 기대 payload 전체와 일치할 때만 기존 커밋 성공으로 인정한다. 본문 편집은 기존 text를 조건으로 UPDATE해 다른 진행자의 최신 편집을 덮지 않으며, UPDATE 성공 뒤 감사 기록이 실패하면 동일 audit ID로 감사 insert만 안전하게 이어간다. 이동·분류·부모 변경처럼 조건부 갱신이 아닌 쓰기는 응답 유실 뒤 저장 재시도를 제공하지 않고 현재 상태 새로고침으로 서버 정본을 재조정해 다른 진행자의 변경을 덮지 않는다. 동시 성공은 먼저 발생한 실패 알림을 덮지 않는다. 초기 snapshot과 realtime payload를 런타임 검증하고 malformed payload·조회 실패·channel 오류는 쓰기를 잠그는 연결 오류로 전환한다. 인증과 realtime이 모두 준비된 경우에만 드래그·연결·삭제·편집과 저장된 재시도를 허용한다. Astro·React·Tailwind 개발 통합을 공통 Vite 6 계열로 고정하고 JSON data island를 명시적 inline으로 분리한 뒤, Node 20 냉간 dependency scan과 실제 Chromium에서 CanvasBoard hydration·라이브 snapshot/realtime ready·로그인 전 비드래그 상태를 읽기 전용으로 확인했다. 본문 UPDATE와 변경 이력 INSERT는 현재 두 요청이어서 단일 DB transaction은 아니며, 브라우저가 종료되기 전에 화면 재시도를 완료해야 감사 기록이 복구된다. 이를 단일 원자 RPC로 바꾸는 migration은 별도 사용자 승인 대상이며, 이번 변경은 DB 스키마·데이터·권한을 직접 변경하지 않았다.
- **M3 온톨로지 검수 큐 계약·seed dry-run**: `docs/platform/ONTOLOGY_REVIEW_QUEUE_CONTRACT.md`가 batch/item/event/exclusion 후보 필드, 검수 상태 불변식, 조직 범위 RLS, 원자 seed·decision RPC와 rollback 순서를 draft로 고정한다. `canvas-ontology-bridge.mjs --seed-plan ... --output-seed ...`는 sealed review plan을 같은 snapshot 원본으로 먼저 검증한 뒤 node/relation/cluster와 제외 provenance를 future DB field의 local seed plan으로 정규화한다. batch source는 `canvas_snapshot`과 stable snapshot UID로 명시하고, 모든 item은 `proposed`로 시작해 source UID·nullable transcript chunk ID·cited UID·원 Canvas 관계·원문과 편집 text·moderator-only metadata·빈 reviewer/timestamp를 보존한다. seed는 canonical self-checksum을 포함하고 `--verify-seed`가 같은 snapshot·plan에서 재생성한 전체 내용까지 대조하지만 외부 서명·작성자 진위·승인 증거는 아니다. 출력은 public 경로를 거부하고 `dryRun:true`, `databaseMutationExecuted:false`, `requiresApproval:true`를 유지한다. 실제 migration/rollback SQL, table/RLS/RPC 적용, stage semantic test와 reviewer 인증은 사용자 승인 전이라 아직 만들거나 수행하지 않았다.
- **M4 Canvas→온톨로지 검수 브리지**: `automation/canvas-ontology-bridge.mjs`가 기존 snapshot JSON의 agenda·agenda_link를 로컬 검수 계획으로 변환한다. node kind·relation은 자동 확정하지 않고 모두 `proposed`로 시작하며 회차별 group과 action parent를 검수 메타데이터로 보존한다. 중복 ID·교차 회차·잘못된 부모·빈 활성 캔버스는 fail-closed하고, 보관 의제·연결은 `excluded`에 원 ID와 사유를 남긴다. 계획은 원 snapshot exact-byte SHA-256과 canonical self-checksum으로 우발 변경을 탐지한다. 검수자는 kind·relation 결정과 함께 표시 label/text를 다듬을 수 있고, 내부 export는 원문 SHA-256을 별도로 보존한다. 사람이 node·relation·cluster를 모두 승인/반려한 뒤에만 현재 workshop graph JSON 형식의 내부 export를 만들며, node는 `is_public:false`, meta는 별도 공개 검토 필요 상태를 유지한다. CLI는 저장소 전체 `public` 디렉터리 직접 출력을 거부하고 DB·API·환경변수에 접근하지 않는다. reviewer 인증·외부 서명·실제 공개 반영과 DB ontology 저장은 아직 포함하지 않는다.
- **M5 Graph DB 읽기 어댑터**: `/workshop-graph/`가 정적 `sources.json`을 필수 fallback으로 먼저 읽고, 선택적 DB catalog가 성공했을 때만 `review_state=approved`·`is_public=true` snapshot을 `DB` source로 추가하는 읽기 어댑터를 사용한다. DB catalog는 20초 timeout·1회 retry를 적용하고 row 수·node 역할/label·edge relation·ID·endpoint 관계가 잘못되거나 내부 node가 공개·사람 검수 상태가 아니면 catalog 전체를 fail-closed해 정적 source를 유지한다. query·credential·fragment가 포함된 endpoint도 요청 전에 거부한다. 선택 source의 실제 node/edge 수와 DB row 수를 표시하며 `cited`·`cited_uids`에 유효한 출처 ID가 없는 node는 advisory/footer에서 알린다. source 전환·polling은 generation guard로 늦은 응답을 폐기하고, 실패는 로그와 live 안내로 노출하며 마지막 정상 source 선택·URL·그래프를 유지한다. 현재 승인 graph snapshot table/RPC·공개 RLS가 없어 endpoint를 설정하지 않았고 정적 fallback만 활성 상태다. DB 계약 생성과 첫 live snapshot은 별도 사용자 승인 대상이다.
- **M6 권고 후보·품질 신호 연결**: `/workshop-graph/`가 `meta.recommendations`와 `meta.quality`를 strict 읽기 전용 모델로 정규화해 권고 후보와 품질 신호를 별도 표시한다. 권고는 명시적 `recommendation_candidate`·`draft`만 허용하고, 후보·소수 우려·품질 신호마다 출처 UID·전사 chunk ID·인용 UID를 서로 합치지 않고 보존한다. 화면은 “결정 아님”, “진실 판정 아님”, “사람 검수 필요”를 항상 표시하고 소수 우려와 각 provenance ID 목록을 별도로 펼쳐 볼 수 있다. 품질 label은 승인된 신호 종류만 허용하며, 모호한 기존 `quality` 내부 메모, 결정형 권고, 무출처·중복 ID·중복 출처·원문처럼 보이는 비 opaque ID는 자산 표시를 fail-closed하고 로그와 live advisory를 남기되 그래프 본체는 유지한다. 열린 보조 패널은 유효 자산 A→B 전환 때 즉시 새 자산으로 바뀌고, 자산이 없거나 형식이 틀리면 이전 보조 내용만 제거한다. 개요·고정 노드 상세처럼 보조 자산과 무관한 패널은 보존한다. 현재 추적 정적 payload에는 이 strict 계약을 만족하는 권고 후보가 없어 production 자산 버튼은 표시되지 않는다. provenance ID를 실제 원문 화면으로 연결하는 역링크는 승인된 ID→item 매핑이 없어 아직 구현하지 않았으며, 첫 실제 8/29 분석 산출 연결·사람 검수·DB 적재와 함께 별도 승인 뒤 진행한다. 브라우저 fixture 검증은 `evaluation/2026-08-11-workshop-graph-advisory-browser.json`에 기록했다.
- **R0 전사 fixture 계약**: `transcript-ontology-fixture.mjs`가 비식별 synthetic time-coded chunk와 사람이 검수한 node/relation 후보를 현재 workshop graph JSON 형식으로 결정적으로 변환한다. stable candidate ID, 허용 ontology vocabulary, 관계 endpoint, chunk 인용, 짧은 synthetic speaker pseudonym, 역할형 reviewer alias와 canonical 검수 시각을 fail-closed 검증하고 `cited`/`cited_uids`를 보존한다. graph source는 전체 fixture canonical SHA-256을 포함하고 verifier는 fixture에서 graph 전체를 재생성해 대조하며, CLI는 기존 파일과 실제 경로 기준 `public` 출력을 거부한다. 집중 Vitest 12건과 automation 전체 218건이 통과했으며, 예제와 생성 결과는 `automation/fixtures/transcript-ontology-reviewed.example.json`, `evaluation/transcript-ontology-r0-graph.json`에 남긴다. 이는 내부 `is_public:false` fixture 계약일 뿐이며 실제 마이크/STT·원문 보관·reviewer 인증·DB/API·공개 graph 반영은 구현하거나 승인하지 않았다.
- **R1 합성 검수 live graph prototype**: publication metadata가 명시된 별도 synthetic fixture만 `public/workshop-graph/data/live-*.json`으로 생성하며, 검수 이후 역할형 승인자·시각과 exact public 경로를 fail-closed한다. 추적 source `live-transcript-reviewed-fixture`는 현재 source adapter에서 cache-busting polling으로 반복 로드되고 node/relation의 `cited`·`cited_uids`는 synthetic transcript chunk UID를 가리킨다. 공개 payload에는 raw time-coded chunk table·화자 pseudonym·시작/종료 시각이 없고 화면은 이를 실제 시민 발언이 아닌 합성 검수 데모로 명시한다. 실제 Chromium이 production graph 화면에서 두 번째 polling payload와 합성 데모 footer 반영을 확인한다. 집중 19건, automation 전체 225건, 루트 전체 883건, Astro check 오류 0, Node 20 build 7,914 pages가 통과했다. 실제 마이크/STT·시민 발언·reviewer 인증·자동 publication·DB/API 쓰기는 포함하거나 승인하지 않았다.
- **R2 moderator 전사 후보 검수 prototype**: 기존 비공개 `/ko/moderator/ontology-review`에서 synthetic fixture뿐 아니라 R4 검수 완료 batch와 그 exact-byte SHA-256·capture/session/audio SHA에 결속된 provider-neutral node/relation 후보를 브라우저 메모리로 검수한다. 생성 workspace와 private plan은 Auth reviewer·검수 시각·원 STT candidate provenance를 보존하며, 입력 두 파일이나 판단이 바뀌면 이전 workspace·다운로드를 폐기한다. 생성 당시 exact R4 결속 fixture도 로컬로 내려받아 R3 CLI 입력으로 보존하고, 모든 판단 뒤 그 fixture 재생성 대조를 통과한 private plan과 같은 Auth reviewer·`live-*` source ID·canonical plan SHA-256·모든 판단 이후 승인 시각에 결속된 R3 publication approval artifact를 각각 내려받는다. Chromium verifier는 R4 batch 다운로드→SHA-bound 후보 업로드→R4 결속 fixture 다운로드→R2 판단→plan·approval 다운로드를 실제 production React 화면에서 실행하며, 다운로드한 fixture exact bytes를 그대로 R3 graph builder에 전달한다. clean source commit `5c18c1209e73e193bf42e851be53f5c0f39bd0ec` 실행은 `evaluation/2026-08-14-r4-r3-transcript-ontology-fixture-browser.json`과 PNG에 pass, fixture SHA-256 결속, write 0건, page error 0건을 기록한다. 루트 Vitest 63개 파일·981건, automation 21개 파일·265건, Astro check 오류 0건, Node 20 정적 빌드 7,914페이지가 통과했다. 파일 생성과 graph dry-run 모두 DB·API·public graph를 쓰지 않으며, provider extraction 호출·approval artifact 외부 서명·독립 계정 소유 증명은 포함하지 않는다.
- **R3 합성 검수 결과 graph source**: R2 private plan과 별도 publication approval fixture를 exact 원본 SHA-256·canonical plan hash·승인자·승인 시각으로 결속해 graph를 결정적으로 생성·재검증한다. CLI는 동일 builder의 exact 후보를 먼저 `public` 밖에 no-overwrite preview로 쓰고 재생성 검증하는 `--output-reviewed-preview`/`--verify-reviewed-preview`를 제공한다. public output은 검증할 `--reviewed-preview`를 필수로 받아 다시 검증한 그 payload만 no-overwrite로 승격하며 direct output 우회와 변조 preview를 fail-closed한다. `--output-reviewed-bundle-report`/`--verify-reviewed-bundle-report`는 graph를 만들기 전에 R4 결속 fixture·R2 plan·approval 세 파일을 전수 검증하고 exact-byte/canonical SHA, handoff ID와 건수만 비식별 report로 기록·재계산한다. preview와 bundle report는 symlink/junction을 포함한 public 경로를 거부하고 `databaseMutationExecuted:false`, `publicGraphWritten:false`를 유지한다. 추적 synthetic 예제 report는 `evaluation/transcript-ontology-reviewed-bundle-report.example.json`이며 node 2·edge 1, 반려 0을 기록한다. 기존 live graph도 같은 입력에 대해 canonical verify를 통과했고 gate 이전 생성본이라 덮어쓰지 않았다. 검수 결정은 canonical Auth ID 또는 정확한 legacy synthetic ID, 공개 승인은 canonical Auth ID 또는 제한된 synthetic 역할 alias만 허용하고 임의 식별자는 거부한다. source item 집합과 provenance를 전수 대조하고 승인·수정 승인만 포함하며 반려 수를 남기고, raw 전사 표·speaker·시각과 원시 신원은 공개 payload에서 제거한다. R3 집중 31건, automation 전체 21개 파일·266건과 Astro check 오류 0건이 통과했다. 이는 synthetic 정적 publication prototype일 뿐 실제 시민 발언·자동 publication·DB/API 쓰기나 retention 정책을 구현하거나 승인한 것이 아니다.
- **R4 브라우저 음성·전사 검수 prototype**: moderator 검수 경로가 명시적 세션 메모리 처리 동의와 회차·테이블/분과·전사 언어 입력 뒤 MediaRecorder 녹음과 256MB 이하 로컬 테이블 녹음 파일을 모두 받는다. 로컬 파일은 운영자가 확인한 녹음 시작 시각, 브라우저가 20초 안에 읽은 길이, exact SHA-256·MIME·bytes와 `sessionId`·`roomId`·`language`·`captureMethod`를 capture에 결속하고 파일명·경로·음성 bytes는 batch에 넣지 않는다. 종료 시각이 import 시점보다 미래인 입력도 즉시 거부한다. 음성 Blob/File과 local object URL은 새 녹음·파일, 동의 철회, 페이지 종료 때 폐기되며 DB·서버·public·브라우저 영구저장소로 전송하지 않는다. provider-neutral `private-stt-candidates` schema v2 JSON은 현재 source context·audio SHA-256·duration과 전부 일치하고 raw audio/임의 metadata가 없을 때만 기존 local draft를 교체하며, `candidateSetId`와 source UID를 schema v2 review batch까지 보존한다. 음성 또는 후보 import 중 현재 선택이 바뀌면 늦은 결과를 폐기한다. 수동 또는 imported time-coded chunk·speaker pseudonym·전사 문구를 moderator가 모두 검수해야 음성 bytes가 없는 `private-transcript-review-batch`를 내려받을 수 있고, 검수 뒤 문구가 바뀌면 판단과 다운로드를 다시 잠근다. export는 capture source와 chunk UID·candidate provenance·시간·화자 가명·판단 audit·원문 보존 규칙·summary를 실제 chunk에서 다시 계산해 변조 session을 fail-closed한다. 이 batch는 exact-byte SHA-256과 source binding이 일치하는 provider-neutral ontology 후보와 함께 R2 로컬 검수 workspace로 전달되며 room·language·capture method는 R2 handoff와 R3 bundle report까지 보존된다. extraction provider는 호출하지 않는다. 검수 결정과 exporter는 현재 Supabase Auth 사용자 UUID에서 파생한 canonical `auth-user:<lowercase uuid>`만 허용하며 이메일은 export하지 않는다. 브라우저 verifier는 MediaRecorder 실패·중복·동의 철회·늦은 권한 응답을 확인한 뒤 실제 1초 synthetic WAV를 파일 input으로 가져와 1,000ms·16,044 bytes capture, 출처 맥락, STT 후보, 재검수와 batch download를 검증한다. 집중 45건, 루트 전체 63개 파일·989건, automation bridge 31건, browser 5건과 Astro check 오류 0건이 통과했다. Windows automation 병렬 전체는 기존 접근성 5초 timeout 8건과 단독 통과한 live polling 15초 경계 1건을 제외한 257/266건이 통과했다. clean source commit `2d2cf329090dc80461b6856f722857881d9b7623`의 production React 검증은 `evaluation/2026-08-14-private-transcript-source-context-browser.json`과 PNG에 source context 결속·R2/R3 handoff·batch download 성공, 차단된 write 0건, page error 0건을 기록한다. 이전 local file import 증거는 commit `31a49d1ea994ca22ffde5a9c980f07c0b6c39415`의 `evaluation/2026-08-14-private-audio-file-import-browser.json`과 PNG에 남아 있고, commit `477fa82e4bedc8b2ad524f3930dbfc5a381dd456`의 이전 STT import 증거도 `evaluation/2026-08-14-private-stt-import-browser.json`과 PNG에 남아 있다. 실제 시민 발언·외부 STT/extraction 호출·provider credential·지속 저장·retention·다운로드 batch 외부 서명·자동 publication·DB/API는 포함하거나 승인하지 않았다.
- **R4 화자 미식별 provenance 보강**: Stage 2의 화자 미식별 경로를 정확한 `speaker-unknown`으로 표준화했다. R4 UI는 이를 기본값으로 보여 임의 화자를 추정하지 않고, 수동·STT import validator와 R2 workspace는 짧은 `speaker-a`형 가명 또는 `speaker-unknown`만 허용한다. review batch와 비공개 R2 인용에는 해당 값을 보존하지만 R3 공개 graph에서는 화자 표기와 raw time range를 계속 제거한다. 집중 46건, 루트 전체 63개 파일·990건, automation bridge 32건, browser 5건과 Astro check 오류 0건이 통과했다. Windows automation 전체는 기존 접근성 Playwright 5초 timeout 8건을 제외한 259/267건이 통과했다. clean source commit `9c27d6947e9f6d55ddf44df1976c557f8e3e00ea`의 production React 증거는 `evaluation/2026-08-14-private-transcript-unknown-speaker-browser.json`과 PNG에 화면 표시·batch 보존·공개 graph 비노출, write 0건과 page error 0건을 기록한다. 실제 화자 식별·개인 이름·외부 STT·DB/API·publication은 추가하지 않았다.
- **R2 후보 카드 진행 질문 제안**: 전사 node 후보 카드는 현재 Habermas 역할 7종에 맞춘 “함께 확인할 진행 질문”을 카드 안에 표시하고 역할 선택이 바뀌면 즉시 다시 계산한다. 문구는 범위·근거·조건·쟁점 연결·확인 기준·가치 긴장·추가 확인을 moderator가 함께 살피도록 제안할 뿐 회의 결정이나 진실 판정을 대신하지 않으며 reviewed plan·DB·public graph에는 추가되지 않는다. 역할 mapping과 잘못된 역할 거부, 카드 렌더 집중 26건, 루트 전체 63개 파일·992건, 실제 browser contract 1건과 Astro check 오류 0건이 통과했다. Windows automation 전체는 기존 접근성 Playwright 5초 timeout 8건을 제외한 259/267건이 통과했다. clean source commit `343c643cccccf710cc09128ca6e357cb0022fd02`의 production React 증거는 `evaluation/2026-08-14-transcript-candidate-prompt-browser.json`과 PNG에 후보 질문·비결정 경계, R4→R2 검수/download, write 0건과 page error 0건을 기록한다.
- **R2 후보 검수 보류**: 전사 node/relation 후보 카드에 `나중에 검수`를 추가했다. 보류 결정은 Auth reviewer와 canonical 검수 시각을 audit로 남기지만 완료 건수에는 포함하지 않고 reviewed plan·publication approval 다운로드를 계속 차단한다. 보류 또는 node 초안 변경은 이미 승인된 연결 판단을 `proposed`로 되돌리며, relation 승인은 두 endpoint node가 모두 승인·수정 승인 상태일 때만 허용한다. 집중 Vitest 28건, 루트 전체 63개 파일·994건, Canvas browser 5건과 Astro check 오류 0건이 통과했다. Windows automation 전체는 기존 접근성 Playwright 5초 timeout 8건을 제외한 259/267건이 통과했다. clean source commit `6e0d43a95ef8c3ce78cff3dd6e1883566f8b0820`의 production React 증거는 `evaluation/2026-08-14-transcript-candidate-defer-browser.json`과 PNG에 보류 진행률·다운로드 차단·재판단 후 export, write 0건과 page error 0건을 기록한다. Stage 4의 후속 확인·소수 의견 분리·병합 판단은 아직 구현하지 않았고 DB/API/publication을 변경하거나 승인하지 않았다.
- **R2 후보 후속 확인 요청**: 전사 node/relation 후보 카드의 제안 질문을 moderator가 실제 `후속 확인 요청` 상태로 기록할 수 있다. 선택한 질문·Auth reviewer·canonical 시각은 브라우저 세션 workspace에 남지만 완료 건수로 세지 않고 plan·publication approval을 잠근다. node 후속 확인은 이미 승인됐거나 후속 확인 중인 연결 relation을 `proposed`로 되돌리고, 응답·추가 근거를 확인한 뒤 초안을 수정하거나 승인·수정 승인·반려로 재판단해야 해제된다. pending 질문 필드는 완료 reviewed plan과 공개 graph에 포함하지 않아 기존 R3 계약을 유지한다. 집중 Vitest 31건, 루트 전체 63개 파일·996건, Canvas browser 5건과 Astro check 오류 0건이 통과했다. Windows automation 병렬 전체는 기존 접근성 5초 timeout 8건과 단독 통과한 Canvas readiness 음성 테스트의 15초 경계 1건을 제외한 258/267건이 통과했다. clean source commit `c3d7431616edd17b202b22b128d2fe65347e8166`의 production React 증거는 `evaluation/2026-08-14-transcript-candidate-follow-up-browser.json`과 PNG에 질문·진행률·다운로드 차단, 보류 전환, 재판단 후 R3 handoff, write 0건과 page error 0건을 기록한다. Stage 4의 기존 node 병합 action은 아직 구현하지 않았고 DB/API/publication을 변경하거나 승인하지 않았다.
- **R2 소수 우려 보존**: node 카드의 `소수 우려로 표시`는 현재 후보를 명시적 `Concern`과 `minorityConcern:true` 초안으로 바꾸고, 이전 node 판단과 연결 relation 판단을 무효화해 수정 승인을 다시 요구한다. 완료 reviewed plan은 이 사람 검수 신호를 원 source·인용·audit과 함께 보존하며, R3 builder는 승인된 Concern만 공개 node의 `minority_concern:true`와 graph 집계로 옮긴다. 반려 node, non-Concern marker, marker를 변경하고 재판단하지 않은 workspace는 fail-closed하며 공개 graph 상세 화면은 `소수 우려 보존` 배지를 표시한다. 집중 루트 Vitest 33건, R3 bridge 34건, 루트 전체 63개 파일·999건과 Astro check 오류 0건이 통과했다. Windows automation 전체는 기존 접근성 5초 timeout 8건과 Canvas readiness 음성 테스트의 15초 경계 1건을 제외한 260/269건이 통과했고, 정상 Canvas Chromium 경로는 pass했다. clean source commit `65893e69badc9dc637c99c91a59482da817a0a1b`의 production React 증거는 `evaluation/2026-08-14-transcript-candidate-minority-browser.json`과 PNG에 표시→재판단→private plan→R3 graph 전달, write 0건과 page error 0건을 기록한다. Stage 4의 기존 node 병합 action은 아직 구현하지 않았고 실제 시민 데이터·DB/API/publication을 변경하거나 승인하지 않았다.
- **R2 기존 node 병합 판단**: 같은 R4→R2 batch의 동일 역할 node 중 이미 승인·수정 승인한 대상을 moderator가 선택해 중복 후보를 병합할 수 있다. private plan은 중복 후보를 삭제하지 않고 `reviewStatus:merged`, `mergeTargetId`, 원 source label/text·UID·인용·Auth audit을 보존한다. 병합 대상 내용을 바꾸거나 병합 source를 다시 독립 판단하면 관련 병합과 relation 판단을 `proposed`로 되돌리고, target 반려와 병합 후 self-loop relation도 fail-closed한다. R3 builder는 병합 source node를 공개 graph에서 생략하되 target에 cited UID와 source UID를 합치고 relation endpoint를 target으로 재지정하며 `meta.merged_nodes`에 건수를 남긴다. 집중 루트 Vitest 35건, R3 bridge 36건, Canvas browser 5건, 루트 전체 63개 파일·1,001건과 Astro check 오류 0건이 통과했다. Windows automation 전체는 기존 접근성 5초 timeout 8건과 Canvas readiness 음성 테스트의 15초 경계 1건을 제외한 262/271건이 통과했다. clean source commit `a86af5afd766467c58660438c7d5770b9ea0fbfc`의 production React 증거는 `evaluation/2026-08-14-transcript-candidate-merge-browser.json`과 PNG에 병합→대상 수정 무효화→재병합→private plan→R3 graph 전달, write 0건과 page error 0건을 기록한다. 실제 시민 데이터·DB/API·public graph 파일은 변경하거나 승인하지 않았다.
- **Stage 5 운영자 graph 초안 보기**: 인증된 전사 후보 검수 작업대가 현재 브라우저 메모리 workspace를 1초마다 읽어 승인·수정 승인 node와 유효한 relation만 `운영자용 graph 초안 보기`에 표시한다. 병합 source는 별도 공개 node로 보이지 않고 target의 인용·source UID에 합쳐지며, 미검수·보류·후속 확인·반려 후보와 self-loop 관계는 제외하거나 fail-closed한다. 화면은 항상 `검수 중 초안 · 공개 아님`과 local-only·publication review 경계를 표시하고 DB·API·public graph 파일을 쓰지 않는다. 대상 수정으로 판단이 무효화되면 다음 poll에서 node·병합·relation도 함께 제거된다. 집중 Vitest 37건, 루트 전체 63개 파일·1,003건, Canvas browser 5건과 Astro check 오류 0건이 통과했다. clean source commit `2160bf6705e885d5c79ff651a7b1ddf4903b8b7d`의 production React 증거는 `evaluation/2026-08-14-transcript-moderator-draft-browser.json`과 PNG에 초안 node 0→1→0→1 polling, 병합 provenance, write 0건과 page error 0건을 기록한다. Stage 5 public/presentation mode는 기존 별도 publication approval·reviewed R3 snapshot 경계를 유지하며 실제 시민 데이터 publication과 DB adapter는 여전히 별도 승인 대상이다.
- **Stage 5 공개·발표 reviewed snapshot 게이트**: `/workshop-graph/`의 모든 live source는 manifest에서 `reviewed_snapshot`을 선언해야 하며, public loader가 node와 relation 모두의 공개·검수 상태, source review/publication metadata, manifest source ID, 승인 시각과 element counts를 로드 전에 fail-closed 검증한다. 정적 source는 public `data/*.json` 단일 파일, 2D 포함 허용 view, boolean visibility/menu를 요구하고 live source는 ID와 같은 `data/live-*.json` 및 양의 정수 polling 간격까지 요구해 외부 URL·상위 경로·source/file drift를 첫 요청 전에 거부한다. 각 live node·relation은 공백·중복이 없는 canonical `cited`/`cited_uids`를 하나 이상 보존해야 하며, 전사 인용이 없는 운영자 생성 항목만 `moderator_created:true`로 예외를 명시할 수 있다. public payload는 `reviewed-summary-only`, raw transcript 미포함, audio 미포함을 선언하고 모든 node를 `reviewed_summary`로 표시해야 한다. raw chunk·transcript·speaker·time range·audio 필드가 node/relation 아래에 있으면 static live와 optional DB snapshot 모두 화면 반영 전에 거부한다. 일반 `reviewed_snapshot`은 `사람 검수 완료 스냅샷`과 `LIVE · 검수 완료`로 표시하고, `synthetic_reviewed_demo`는 같은 검수 완료 상태와 함께 실제 시민 발언이 아닌 합성 데모임을 구분한다. source 선택 그룹은 둘을 포괄하는 `검수 완료 스냅샷`으로 표시한다. reviewed-summary node 상세는 source 종류와 무관하게 사람 검수 요약과 출처 UID만 표시하고 `공개 원문 미포함` 경계를 명시한다. 기존 R1/R3 builder와 두 추적 live snapshot도 같은 정책으로 재생성·검증했다. source adapter 집중 55건, R1/R3 bridge 36건, 루트 전체 63개 파일·1,042건과 Astro check 오류 0건(기존 hint 49건)이 통과했다. 직전 Windows automation 전체 병렬 실행은 Playwright 리소스 경합으로 접근성 8건과 Canvas readiness 1건이 각 5초·15초 제한에서 timeout되어 262/271건이었고, 접근성 파일은 확장 timeout에서 15/15건, Canvas 파일은 정상·차단 경로 4/5건이 통과했으며 readiness 음성 1건은 구현의 15초 대기와 테스트 자체 15초 제한이 같은 기존 경계로 남았다. commit `1a516a1334a3ac171b29a040f511e3090d2114c0`의 실제 Chromium 증거 `evaluation/2026-08-14-workshop-graph-reviewed-snapshot-browser.json`과 PNG는 추적 R2 source의 node 2개·relation 1개가 모두 공개·검수·출처·reviewed summary 상태이고, served 파일 해시·manifest mode·content policy·private transcript 필드 부재·합성 안내가 일치하며 검수 요약·출처 UID 표시와 공개 원문 제외 안내를 확인했다. 별도 일반 reviewed snapshot fixture의 footer·advisory·pill과 중립적 source 그룹도 검수 완료 상태로 표시됨을 기록한다. 이는 synthetic fixture와 일반 reviewed 표시 계약의 로컬 증거이며 실제 시민 데이터 publication, raw transcript 공개, DB/API 쓰기 또는 자동 승인 기능을 추가하지 않았다.
- **R5 검수 온톨로지 기반 진행 제안**: Canvas 검수 작업대가 승인·수정 승인된 node/relation/cluster만 읽어 근거 없는 Claim, 조건 없는 Proposal, Issue와 분리된 Concern, 의미 연결이 빠진 Evidence 군집, `opposes`로 드러난 Value 긴장의 다섯 질문을 브라우저 메모리에서 계산한다. 각 질문은 이유·session/agenda/node·관련 node·원문을 표시하고 live region으로 개수를 갱신한다. 출처·관련 node는 중복 제거된 실제 링크로 제공하며 실행하면 정확한 focusable 검수 카드로 이동한다. 문구는 모두 moderator에게 함께 확인해 보도록 제안하며 합의·진실·우선순위를 결정하지 않는다. 집중 18건과 루트 전체 896건, Astro check 오류 0, Node 20 build 7,914 pages가 통과했다. 단위 렌더 검증은 출처·관련 node의 결정적 href와 focusable target을 고정했고, 실제 production Chromium은 규칙 목록·질문·출처 카드 포커스·download, write 0건·page error 0건을 확인했다. Windows automation 전체는 기존 Playwright timeout 10건을 포함해 220/230이었으며 Linux CI를 최종 기준으로 남긴다. DB·browser storage·review plan·public graph에는 쓰지 않는다.
- **분석코어→검수 계획 호환**: `platform-analysis-import.mjs`가 실제 Python 분석코어의 의도적으로 빈 recommendation title과 문자열형 minority 형상을 provenance map schema v2의 `candidateMappings`로 받는다. 사람이 준비한 후보·소수 우려 표시명과 소수 우려별 cited UID가 모두 있어야 하며 누락·중복·남는 index·소수 우려 원문 SHA-256 불일치는 fail-closed한다. source UID→전사 chunk→submission item/cluster 관계와 recommendation `time_span`은 plan provenance에 보존한다. CLI create/verify가 동일 원본 바이트 해시와 canonical plan을 다시 확인하며 출력은 계속 DB 미변경 dry-run·AI draft·사람 검수 필요 상태다. 실제 8/29 산출물과 ID 매핑은 행사 뒤 별도 승인 입력이 필요하다.
- **분석코어 민감 파일 repository 격리(2026-08-26)**: 첫 실전 전 analysis import CLI가 raw analysis·provenance map·검수 전 verify plan을 실제 경로 기준 repository 밖의 기존 일반 파일로만 읽고, 생성 plan도 실제 상위 경로가 repository 밖일 때만 사용자 전용 모드로 기록하도록 강화했다. 저장소 내부 직접 경로와 내부를 가리키는 symlink/junction, 누락·디렉터리 입력, output symlink·hard-link를 원문·ID·경로 노출 없이 거부한다. DB·API·실제 8/29 데이터는 사용하거나 변경하지 않았다.
- **분석코어 import bounded input(2026-08-26)**: 첫 실전에서 손상·오선택 파일이 importer 메모리를 무제한 점유하지 않도록 analysis·provenance map·검증 plan을 각각 16MiB 이하의 비어 있지 않은 JSON으로 제한했다. 읽기 전 파일 크기와 읽은 뒤 byte 수가 다르거나 상한을 넘으면 parse·plan write 전에 비식별 오류로 중단한다. 새 plan의 직렬화 결과도 같은 상한을 넘으면 기록하지 않아 도구가 만든 파일을 검증 모드가 읽지 못하는 상태를 차단한다. 같은 CLI option이나 `--force`를 두 번 준 경우도 마지막 값을 조용히 선택하지 않고 경로를 출력하지 않은 채 거부한다. 실제 8/29 파일·DB·API·credential은 읽거나 변경하지 않았다.
- **분석코어 provenance schema drift 차단(2026-08-26)**: provenance map schema v1/v2 root와 source·candidate·minority mapping에 exact-field allowlist를 적용했다. `clusterID` 오타를 `clusterId:null`로 조용히 처리하거나 candidate 내부 메모·잘못된 citation 필드를 버리는 경로를 plan 생성 전에 차단하며, 오류에는 알 수 없는 필드명·값·원문·ID를 넣지 않는다. 실제 Python analysis 본체의 확장 필드는 이 overlay 계약과 분리해 기존 호환성을 유지한다.
- **A1·A2 활성화 preflight**: `automation/platform-activation-preflight.mjs`가 DB 변경 없이 12개 NOT NULL 대상 테이블의 `org_id` 완전성과 권위 있는 상위 assembly/session/topic 조직 경로의 일치, 활성 조직별 관리자·HQ 역할, 활성 membership의 활성 조직 귀속과 이메일 확인 완료·비익명 Auth 사용자 존재, 다중 조직 사용자와 만료 전 미바인딩 HQ 세션을 집계한다. 출력은 비식별 count와 blocker만 남기며 `ready` 외 상태는 fail-closed한다. 별도 승인 전용 `platform_p1c_activation_preflight.sql` 초안은 원시 table/Auth 행을 노출하지 않고 service-role 전용 `platform_activation_preflight()`에서 모든 계수를 한 SQL snapshot으로 계산한다. CLI는 exact schema·계수 합·blocker 정합성을 다시 검증한다. schema v2 `ready` evidence는 report 전체와 commit·정확한 script SHA-256·A2 실행 소스 clean 상태·run/key ID를 외부 키 HMAC으로 결속하고, 검증 CLI가 현재 HEAD/hash·tracked/untracked migration·rollback·verifier·승인 host·HMAC·기본 10분 freshness를 다시 확인한다. 한 statement snapshot이어도 진단 후 변경을 막지는 않으므로 승인된 활성화 직전 쓰기 동결 상태에서 즉시 재실행해야 한다. 현재 `labor_money` 실측 증거는 RPC 적용 전 원시 SELECT 시도의 `not_verified / read_access_unavailable`이며 데이터 준비·Auth 활성화 완료 증거가 아니다. preflight RPC·production migration·HMAC secret/key ID·임의 GRANT·backfill·Auth 계정/membership은 적용하거나 생성하지 않았다.
- **A4 승인 전 설계 청사진**: assembly 설계 화면에서 공론화 이름·slug·선택 목적·운영 방식·개회 전 필수 준비도와 여러 회차의 이름·slug·날짜·주제·조 수·예상 참여자 수를 로컬 React 상태로 작성한다. 순수 검증기는 실제 assembly mode와 readiness vocabulary, 목적 길이, 최소 1개 준비도, 실제 달력 날짜, 회차 순서, 회차 slug 형식·중복, 주제 공백·중복, 양의 정수와 무인 조 발생을 fail-closed하고, readiness를 정본 순서로 보존하며 회차→주제·표준 이름 조 계층과 조별 계획 인원을 결정적으로 만든다. 검증된 결과만 `dryRun:true`, `databaseMutationExecuted:false`, `requiresApproval:true`가 포함된 schema v4 JSON으로 미리보기·다운로드할 수 있으며, 같은 JSON을 다시 불러와 로컬 편집을 이어갈 수 있다. schema v1/v2/v3는 목적 미지정·합의형·기본 3개 준비도와 필요한 식별정보를 결정적으로 보완해 v4로 승격한다. 가져오기는 크기·정확한 schema·승인 경계·준비도·계층·합계를 다시 검증하고 잘못된 파일은 기존 초안을 보존한 채 원문 비노출 오류로 거부한다. 브라우저 저장소·DB·RPC는 사용하지 않으며 실제 assembly/session/topic/team 생성, 배포된 `readiness_check`의 선택 정책 적용, 재편집 가능한 서버 저장, 권한·동시성은 §5-4 결정과 DB 변경 승인 뒤 별도 구현한다.
- **A3 승인 전 기관 접근 계획**: 기관 루트의 `접근 관리` 화면에서 이메일 초대와 기존 Supabase Auth 사용자 UUID의 역할 부여를 `org_admin / operator / hq / facilitator` 정본으로 작성한다. canonical 기관 UUID·이메일·사용자 UUID·중복 역할을 fail-closed 검증하고, 검증된 결과만 `dryRun:true`, `authAccountsCreated:false`, `invitationsSent:false`, `databaseMutationExecuted:false`, `requiresApproval:true` JSON으로 내려받는다. 내려받은 파일은 같은 화면에서 다시 열 수 있지만 exact schema·현재 기관 ID/표시명·허용 필드·canonical 값·dry-run 경계를 모두 재검증한 뒤에만 목록과 미리보기를 복원한다. 다른 기관·변조·알 수 없는 필드·256KiB 초과 파일은 기존 초안을 바꾸지 않고 거부하며, 파일 읽기 중 최신 편집은 generation 경계로 늦은 import보다 우선한다. 입력은 React 메모리에만 두며 브라우저 저장소·DB·RPC·메일을 사용하지 않는다. 실제 production Chromium은 synthetic Auth fixture로 기관 루트→접근 관리 이동, 잘못된 이메일·중복 초대 거부, 초대 1건·기존 계정 역할 1건 미리보기, 편집 즉시 무효화, canonical 기관을 포함한 JSON 다운로드와 재진입 후 초안 0건을 확인했다. 이어 다른 기관 파일·대용량 파일 거부, 지연 import 중 최신 편집 보존, 유효 파일의 초대 1건·membership 1건 복원, 복원 후 편집 무효화, 다시 진입했을 때 import 초안 0건을 확인했다. 브라우저 저장소에는 synthetic Auth 세션 키만 있었고 DB mutation·fixture 실패·page error는 모두 0건이었다. verifier contract 8건, 루트 전체 64개 파일·1,054건, automation 전체 21개 파일·273건, Astro check 오류 0건(기존 hint 49건), Node 20 정적 빌드 7,914페이지가 통과했다. local working-tree 증거는 `.artifacts/platform-access-plan-import-browser.json`에 schema 10·`sourceTreeClean:false`로 보존했고 clean checkout 증거는 push 뒤 platform accessibility CI artifact로 확정한다. 실제 Auth 계정 생성, invitation/membership 행 추가, RLS/GRANT 활성화와 관리자 RPC는 사용자 승인 및 프로비저닝 뒤 범위다.
- **A5 자동 감사 기반**: `automation/platform-accessibility-audit.mjs`가 실제 Chromium에서 axe-core WCAG 2.2 AA 태그, 건너뛰기 링크 포커스와 수평 넘침을 데스크톱 1440×1000·모바일 360×800 뷰포트로 검사한다. 플랫폼 로그인·인증 후 셸·접근성 성명·미공개/공개 결과·온톨로지 검수 초기 화면 6경로의 로컬 정적 빌드와 사용자 도메인 12케이스 증거를 각각 `evaluation/platform-accessibility-responsive-audit.json`, `evaluation/platform-accessibility-audit.json`에 저장한다. 두 보고서는 검사 checkout의 전체 commit과 감사 대상 경로의 clean 상태를 기록하고, 미커밋 source 또는 GitHub Actions commit과 실제 HEAD 불일치가 있거나 자동 판정 불가가 남으면 실패한다. 정상 build의 postbuild가 `/deployment-revision.json`에 전체 source commit만 담은 no-store 정적 manifest를 생성하며, 감사기는 같은 origin의 exact JSON·최종 URL·MIME·SHA가 checkout과 일치할 때만 `targetRevision:verified`를 기록한다. `.github/workflows/platform-accessibility.yml`은 관련 변경마다 동일한 2개 뷰포트 감사를 재실행한다.
- **A5 배포 revision artifact 결속(2026-08-26)**: revision writer는 `CF_PAGES_COMMIT_SHA`→`GITHUB_SHA`→로컬 checkout 순으로 전체 SHA를 선택하고 authoritative 환경값이 존재하지만 형식이 틀리면 fallback하지 않는다. manifest writer는 고정된 `dist/deployment-revision.json` 밖으로 쓸 수 없고 schema v1 `{schemaVersion,sourceCommit}`만 게시한다. 자동 감사와 수동 backup 배포 verifier는 20초 제한으로 256 byte 이하 same-origin 최종 JSON을 읽어 exact field·전체 소문자 SHA를 대조하며, 누락·redirect·HTML·추가 field·다른 commit을 브라우저 판정 전에 거부한다. 이는 정적 artifact source 결속이며 실제 보조기술 수동 평가나 독립 시각 증명은 아니다.
- **A5 수동 평가 증거 게이트**: `evaluation/platform-accessibility-manual-evaluation.json`은 6개 제품 표면과 KWCAG 표면 간 공통 검수를 데스크톱·모바일 스크린리더 프로필로 평가하는 14개 케이스·82개 필수 검사를 추적한다. `automation/platform-accessibility-manual-evidence.mjs`는 누락·중복·불완전 환경정보를 거부하고, `pass|fail|blocked`로 실행한 모든 검사에 실제 관찰 메모를 강제하며 `not_run`의 선입력 메모를 거부한다. 모든 검사가 관찰 기록을 갖춘 `pass`가 되기 전에는 `needs_review` 또는 `fail`을 유지한다. 통과 증거 뒤 공통 `src/layouts`를 포함한 접근성 대상 소스가 바뀌면 해당 증거를 stale로 거부한다. 현재는 실제 수동 평가 전이라 82개 모두 `not_run`이며 품질인증 완료를 주장하지 않는다.
- **A5 수동 pass current-tree 결속(2026-08-26)**: 수동 증거 검증기가 기준 commit부터 `HEAD`까지의 변경뿐 아니라 현재 checkout의 staged·unstaged·untracked 접근성 대상 변경도 함께 검사한다. 따라서 아직 commit되지 않은 UI 변경이나 새 파일 위에서 과거 `pass`를 재사용할 수 없으며, 경로·관찰 내용은 오류나 요약 stdout에 노출하지 않는다. 이 fail-closed 경계는 `pass`에만 적용하고 현재 82개 `not_run` 템플릿의 `needs_review` 상태는 유지한다.
- **A5 수동 증거 시간축 결속(2026-08-26)**: `generatedAt`이 검증 시점보다 미래인 evidence와 실행한 case의 `testedAt`이 템플릿 생성보다 빠르거나 검증 시점보다 미래인 evidence를 구조 검증 단계에서 거부한다. 따라서 ISO 형식만 맞춘 역행·미래 관찰 기록을 수동 평가 결과로 사용할 수 없다. 현재 추적 템플릿의 82개 검사는 모두 `not_run`이고 평가시각도 비어 있어 `needs_review`를 유지한다.
- **A5 수동 pass 대상 commit 사건순서 결속(2026-08-26)**: `pass` evidence를 repository에서 검증할 때 대상 commit의 Git committer timestamp를 직접 해석하고 모든 case의 `testedAt`이 그 시각 이상인지 확인한다. 템플릿 생성 이후라는 조건만 만족한 채 아직 존재하지 않았던 commit을 평가했다고 기록한 backdated pass는 clean tree에서도 거부한다. 이 시각은 Git 이력의 사건순서 경계이며 실제 배포 완료시각이나 원격 배포 revision을 증명하지 않는다. 현재 82개 검사는 모두 `not_run`이고 실제 스크린리더·모바일 보조기기 평가는 미실행 상태다.
- **A5 KWCAG 2.2 기준별 증거 게이트(2026-08-26)**: `platform-accessibility-kwcag-coverage.mjs`가 `KS X OT0003:2022` 원문의 33개 검사항목 ID·순서를 exact 계약으로 고정하고 각 항목을 axe 자동 판정, 본문 바로가기 초점, 반응형 넘침, 키보드 스크롤과 수동 검사 ID에 연결한다. 기준 누락·중복·순서 변경, 존재하지 않는 증거 참조, 미커밋 자동 감사 소스를 거부한다. clean Node 20 CI run `32894750675`에서 12개 Chromium 자동 케이스와 자동 증거 4개가 통과했고 위반·자동 판정 불가·미매핑은 없었다. 수동 평가 82개가 `not_run`이므로 33개 항목은 모두 `needs_review`, `certificationClaimed:false`를 유지한다. CI artifact와 `evaluation/platform-accessibility-kwcag-coverage.json`의 byte SHA-256이 일치하며 production DB·Auth·membership·RLS·GRANT·배포는 변경하지 않았다.
- **A5 접근성 성명 증거 연결(2026-08-26)**: `/platform/accessibility/`가 KWCAG 2.2 33개 검사항목의 기준별 증거 JSON을 자동·반응형·수동 증거와 함께 공개한다. 수동 평가 82개 미실행과 33개 항목 `needs_review`를 화면에 명시해 자동검사를 품질인증·전수 준수로 오인하지 않도록 했다. commit `10ae40d` 정적 미리보기의 데스크톱·모바일 12개 Chromium 케이스는 모두 통과했고 axe 위반·자동 판정 불가는 없었다. clean build·배포 증거는 push 후 CI로 분리한다.
- 인증 셸과 공개 결과는 실제 production 컴포넌트에 CI 전용 읽기 응답을 주입하고 readiness selector 도달을 필수로 한다. fixture 이름·준비 상태·axe `incomplete`·뷰포트·문서 폭을 JSON에 보존한다. 스크린리더·실제 모바일 보조기기 전수 수동평가와 공식 품질인증은 완료로 간주하지 않는다.
- **A7 XAI 산정 설명**: 공개 결과가 현재 스냅샷의 쟁점·미분류 원문·참여 조·합의 분모·HITL 검수 건수를 사용해 범위→집계→분류→검수 과정을 수치로 설명한다. 선택적 공개 원문 계약이 있으면 쟁점과 원문 카드 사이의 역링크도 제공하지만, 현재 production payload에는 원문 데이터가 없어 실제 게시 완료로 간주하지 않는다.
- **A7 검수 원문 cluster 보존**: `issue_items`의 각 연결을 issue·cluster·연결자 단위로 보존하고, 재분류·연결 해제 전에 replace-all `issue_link_set`이 결과 cluster를 손실 없이 표현할 수 있는지 전체 계획을 검증한다. 암묵적 보존 대상에 서로 다른 cluster가 섞이면 첫 RPC 전에 작업을 차단하고, 대상 전체에 적용할 cluster를 명시한 경우에만 target을 일괄 변경한다. 기록 표·CSV의 M:N provenance 형상은 유지하며 DB migration·데이터 변경은 수행하지 않았다.
- **A7 검수 조회·쓰기 경합 차단**: 검수 콘솔의 `issue_list`·`issue_items` 병렬 조회와 저장·검수 확정·재분류·병합 쓰기를 요청 generation·현재 topic·mutation serial에 결속해, 주제 또는 참여 코드 전환 뒤 도착한 이전 응답이 현재 쟁점·원문·쓰기 코드·busy·알림 상태를 덮지 못하게 했다. 동기 ref 잠금으로 빠른 중복 쓰기를 첫 await 전에 차단하고, 쓰기 중 참여 코드 입력과 재조회도 잠근다. 주제 변경 시 선택·입력·활성 코드·이전 알림을 초기화하고 현재 요청 종료 뒤 참여 코드 입력을 제거한다. 부분 조회 실패는 가능한 읽기 결과와 원인 안내를 함께 표시하며, data와 notice가 모두 없거나 예상 밖 예외가 발생하면 로그와 화면 alert로 fail-closed한다. 쓰기 실패는 `role=alert`, 성공은 polite `role=status`로 구분하고 이전 알림 timeout이 새 알림을 지우지 못하게 했다. 집중 30건, 루트 전체 63개 파일·935건과 Astro check 오류 0건(기존 hint 49건)이 통과했다. DB·RPC·데이터는 변경하지 않았다. 로컬 정적 빌드는 기존 prebuild의 Node 20 WebSocket transport 요구와 현재 Windows Node 24 Astro 조기 종료 때문에 완료되지 않아 push 뒤 Linux CI를 최종 빌드 기준으로 남긴다.
- **A7 검수 경합 Chromium 게이트**: 인증 fixture가 production 데이터 트리와 검수 탭을 통해 실제 `ReviewConsole`을 열고 `issue_list`·`issue_items`를 로드한다. 같은 이벤트 루프에서 검수 버튼을 두 번 실행해 synthetic `issue_review`가 정확히 한 번만 전송되는지 확인하고, 응답을 지연한 채 다른 주제로 이동해 참여 코드·busy·쟁점·성공 안내가 이전 응답으로 복원되지 않는지 fail-closed한다. 기존 설계 청사진 브라우저 검증과 같은 clean Linux workflow에서 재빌드 후 실행하며 모든 Supabase 요청은 fixture로 차단되어 production DB 접근·변경은 없다.
- **A7 공개 mutation 중복 차단**: 공개 콘솔은 React 렌더보다 먼저 동기 operation lock을 획득해 같은 이벤트 루프의 발행·공개 해제 이중 클릭을 각각 한 요청으로 제한한다. 작업 중 HQ 토큰과 결과 제목을 잠그고 성공·실패·예외 뒤에는 lock과 busy를 항상 해제해 재시도를 허용한다. production 인증 브라우저 게이트는 synthetic `result_publish`·`result_get`·`result_unpublish` 응답을 지연해 발행·해제 버튼을 각각 두 번 실행하고, RPC 1회·재조회 검증·공개 카드 제거·브라우저 오류 0건을 요구한다. 이는 현재 `result_page` 동시 publish DB 제약의 클라이언트 완화이며, 서버 단위 원자성·고유 제약 변경은 계속 별도 승인 대상이다.
- **A1 인증 요청 경합 차단**: 운영진 로그인과 로그아웃이 React 재렌더 전에 공용 동기 operation lock을 획득해 같은 이벤트 루프의 중복 Auth 요청을 차단한다. 작업 중 로그인 입력과 실행 버튼을 잠그고 완료·예외 뒤 lock과 busy를 항상 해제한다. 로그아웃 응답이 data와 notice를 모두 누락해도 성공으로 처리하지 않고 로그와 접근 가능한 alert로 fail-closed한다. production Chromium은 지연된 synthetic password 로그인 응답에 form submit을 두 번 실행해 Auth 요청 1회, 입력 잠금, 실패 뒤 재시도 가능, 브라우저 오류 0건을 요구한다. 실제 Auth 계정·membership 활성화나 권한 변경은 포함하지 않는다.
- **A1 인증 세션 최신성 보장**: 초기 `getSession`과 `onAuthChange` 후속 조회를 단일 generation에 결속해 이전 요청의 늦은 로그인·미로그인·오류 응답이 최신 인증 상태와 안내를 덮지 못하게 한다. 현재 조회만 session과 notice를 함께 갱신하고, 현재 예외는 로그와 로그인 화면 안내로 fail-closed하며 unmount cleanup은 남은 응답을 무효화한다. 실제 계정·membership·DB·RLS는 변경하지 않았다.
- **M2 Canvas 인증 세션·요청 최신성 보장**: 라이브 Canvas의 별도 Auth 훅도 초기 `getSession`을 generation에 결속한다. 후속 `onAuthStateChange` 로그인·로그아웃이 발생하면 초기 조회를 즉시 무효화해, 늦은 이전 session·null·오류가 현재 운영자 상태나 로그인 안내를 덮지 못하게 한다. 현재 초기 조회 오류만 로그와 화면 alert로 드러내고 unmount 뒤 callback도 폐기한다. 로그인과 로그아웃은 같은 동기 operation lock을 첫 await 전에 획득해 React 재렌더 전 Enter·클릭 중복 요청을 차단하며, 로그인 중 입력과 로그아웃 버튼을 잠그고 완료·예외 뒤 lock과 busy를 항상 해제한다. 모든 Canvas 작업 결과·재시도·투표/참여 QR·AI 추천도 시작 당시 Auth generation에 결속해 로그아웃·사용자 교체 뒤 늦은 결과를 폐기하고, 새 세션 첫 렌더부터 이전 알림과 재시도 closure를 숨긴다. 세션 경계에서는 선택·QR·추천·로그인 자격정보를 제거하며 성공 로그인 직후에도 이메일·비밀번호 state를 비운다. 집중 CanvasBoard 19건, 루트 전체 63개 파일·964건, Canvas browser verifier 정상·음성 4건과 Astro check 오류 0건(기존 hint 49건)이 통과했다. 로컬 production 개발 서버 Chromium은 지연된 synthetic 로그인 실패에 같은 이벤트 루프 제출 2회를 실행해 Auth 요청 1회, 입력 잠금, 실패 뒤 재시도 가능, 데이터 write 0건, 브라우저 오류 0건을 확인했다. 실제 계정·DB·RLS는 변경하지 않았고 clean Node 20 Chromium은 push 뒤 CI에서 같은 경계를 다시 확인한다.
- **M3·R2·R4 검수 작업대 Auth 격리**: `/ko/moderator/ontology-review/`는 Supabase Auth 세션 전에는 로그인 폼만 렌더하고 마이크·파일·전사·검수 컨트롤을 마운트하지 않는다. 로그인과 로그아웃은 Canvas와 같은 동기 operation lock으로 중복 요청을 차단하고, 인증된 작업대는 Auth 사용자 ID를 React key로 사용해 로그아웃·사용자 교체 시 File·음성 Blob URL·전사 초안·검수 workspace를 함께 unmount한다. 세 검수 흐름의 reviewer는 현재 세션 사용자 UUID에서 canonical `auth-user:<lowercase uuid>`로 파생해 자유입력을 제거하고 이메일은 export하지 않으며, UUID가 유효하지 않으면 작업대를 열지 않는다. 결정 함수·private exporter·Canvas reviewed-plan CLI도 같은 형식만 허용하고 임의 alias와 변조된 reviewer를 fail-closed한다. R3 공개 graph는 원시 UUID를 제거하고 검수 신원 종류만 남긴다. 자동 접근성 감사는 읽기 전용 staff fixture로 인증 후 화면만 감사하며 수동 평가 계약은 로그인 전 비노출·같은 읽기 전용 인증 검수자 ID·로그아웃 후 폐기를 별도 검사한다. reviewer 계약 집중 72건, 루트 전체 63개 파일·970건, Astro check 오류 0건(기존 hint 49건)이 통과했다. Windows automation 병렬 전체는 기존 접근성 Playwright 5초 timeout 8건과 단독 통과한 Canvas readiness 음성 테스트의 15초 경계 1건을 제외한 252/261건이 통과했다. 로컬 Node 24는 저장소가 요구하는 Node 20 범위 밖이라 정적 build가 메시지 없는 exit 1로 종료했으며 clean Node 20 빌드는 push 뒤 CI를 최종 기준으로 남긴다. 실제 production Chromium은 인증 전 작업대 비노출, 로그인 입력 잠금·중복 Auth 요청 1회, 세 패널의 동일 인증 검수자 ID, 음성·전사·HITL 다운로드의 동일 reviewer, 로그아웃 요청 1회 뒤 세션 state 폐기, write 0건·page error 0건을 `evaluation/2026-08-14-ontology-review-auth-identity-browser.json`과 `evaluation/2026-08-14-ontology-review-auth-identity.png`에 기록했다. 후속 canonical reviewer 검증도 clean source commit `7d4e501`에서 같은 결속·다운로드·격리 계약과 write 0건·page error 0건을 `evaluation/2026-08-14-ontology-review-reviewer-contract-browser.json` 및 PNG에 기록했다. 수동 접근성 matrix는 같은 commit으로 재결속했지만 12 case·50 check가 모두 `not_run`이라 계속 `needs_review`다. 실제 계정·membership·DB·RLS와 다운로드 파일의 외부 서명·독립 신원 검증은 변경하거나 구현하지 않았다.
- **A2 사용자별 기관 트리 격리**: 인증 사용자가 바뀌면 기존 기관 트리와 안내를 즉시 비우고 `my_orgs`·`org_tree`를 다시 읽는다. 다중 소속은 URL 값을 신뢰하지 않고 명시적 기관 선택 전까지 트리를 잠근다. 승인된 `platform_p1c_org_selection.sql` 초안은 탭별 opaque 토큰을 JWT `session_id`·Auth 사용자·활성 membership·활성 org와 서버에서 결속하고 RLS가 한 기관만 반환하도록 하며, 토큰 원문은 DB에 저장하지 않는다. 선택 컨텍스트는 12시간 뒤 만료되고 RLS가 즉시 거부하며 다음 기관 선택이 만료 행을 정리한다. UI는 선택 성공 뒤에만 토큰을 현재 탭 `sessionStorage`에 넣어 요청 헤더로 전달하고 로그아웃 시 제거한다. 동기 lock으로 같은 event loop의 중복 선택을 먼저 차단하며, Chromium 게이트는 두 번의 연속 `change` 이벤트가 `org_select` RPC 한 번만 만드는지 검증한다. 이전 사용자·이전 선택의 늦은 응답은 폐기하고 null·notice 누락·예외는 화면 안내와 로그로 fail-closed한다. migration·rollback·정적 SQL 계약·React 회귀 테스트를 추가했지만 실제 Supabase 적용, backfill, NOT NULL, staff GRANT, Auth 계정·membership·데이터 변경은 수행하지 않았다.
- **A2 PostgreSQL semantic rehearsal**: throwaway Postgres 16에 P1→P1C→P2를 함수 본문 검증 활성 상태로 적용했다. 확장 rehearsal이 legacy `session` table에 tenant policy는 있지만 RLS enable이 빠진 결함을 발견해, P1C가 activation grant 전에 5개 staff table RLS를 모두 명시적으로 활성화하도록 정정했다. 이후 다중 membership 미선택 거부, `org_select` 발급, Auth user·JWT `session_id`·header token 결속, assembly/session/topic/submission/ballot의 선택 org 단일 노출, operator의 선택 org 내부 쓰기와 교차 org 쓰기 차단, facilitator 쓰기 차단, 다른 session·user·token과 만료 token 차단, 다음 선택 시 만료 context 정리를 통과했다. rollback은 먼저 별도 activation table GRANT를 회수하고 휴면 검증을 통과한 뒤 P1C 객체를 되돌려 기존 다중 org 거부·membership-wide 휴면 policy를 복원한다. 적용 후 verifier는 GRANT 전 휴면 모드와 별도 승인 GRANT 후 활성 모드에서 객체·RLS·정책 역할·본문·함수 실행 속성·권한을 읽기 전용으로 확인하고, 잘못된 policy-table 결속·RLS 해제·`org_context` 30일 TTL·DELETE 권한·`USING (true)` 약화·`SECURITY INVOKER` 변조 같은 오구성은 실패시킨다. `supabase/**` 변경은 CI의 임시 PostgreSQL 16에서 P1→P1C→P2, 약화 정책 거부, 활성·휴면 검증과 rollback을 다시 실행한다. 이 검증은 로컬 일회성 DB만 사용하며 production Supabase migration·GRANT·data를 변경하지 않는다.
- **A1·A2 사용자 종료 격리**: 인증 사용자 ID를 AppShell React key로 사용해 Auth 사용자가 바뀌는 첫 렌더부터 이전 트리·입력·공개 초안 state를 재사용하지 않는다. 성공한 로그아웃은 Auth 이벤트 재조회보다 먼저 세션을 닫고 URL을 `/platform` 루트로 되돌리며, 같은 탭의 legacy HQ 토큰과 운영자 표시명도 `sessionStorage`에서 제거한다. 저장소 제거 실패는 로그와 다음 로그인 화면 안내로 드러낸다. 집중 42건, 루트 전체 63개 파일·950건, verifier contract 6건과 Astro check 오류 0건(기존 hint 49건)이 통과했다. clean Linux Chromium 게이트는 저장 HQ 토큰 복원→공개 초안 입력→로그아웃을 실행하고 토큰·표시명·초안·이전 트리·콘솔·경로가 모두 제거되는지 요구한다. 실제 Auth 계정·membership·DB·RLS는 변경하지 않았다.
- **A7 이행추적 표시 계약**: 공개 결과의 선택적 `issue.implementation`을 `기관 검토 중 / 이행 계획 수립 / 이행 중 / 이행 완료 / 미이행 사유 공개`로 표준화하고, 책임 기관·갱신 시각·공개 설명을 필수로 검증한다. 완료·미이행에는 HTTPS 공개 근거가 필수이며 미등록과 잘못된 값은 각각 별도 상태로 fail-closed한다. 웹 패널·표 대체본·DOCX가 같은 뷰 모델을 쓰고, 권고는 자문이며 상태가 정책 효과 판정이 아님을 함께 표시한다. 집중 62건, 루트 전체 918건, Windows 브라우저 두 파일을 제외한 automation 222건, 공개 결과 감사 route 계약 1건과 Astro check 오류 0건이 통과했다. 현재 RPC/DB 스키마·실제 공개 snapshot은 변경하지 않았으므로 실제 이행 데이터 게시, 데이터 계약·migration·운영 검수는 계속 사용자 승인 대상이다.
- **A7 이행추적 publish plan preflight**: `platform-implementation-plan.mjs`가 현재 공개 snapshot과 운영진 검수 기관 응답의 scope·issue ID·허용 상태·책임 기관·UTC 시각 순서·역할형 검수자·HTTPS 근거를 대조한다. 일부 쟁점만 갱신해도 나머지를 보존한 전체 `atomicResultBody`를 재구성하고 기존 이행값까지 다시 검증하며, 승인되지 않은 필드는 내부 메모 유출 위험 때문에 거부한다. plan은 전후 body와 쟁점별 값 SHA-256, canonical self-checksum, 전체 입력 재생성 검증을 제공하지만 `dryRun:true`, `databaseMutationExecuted:false`, `publicPayloadWritten:false`, `requiresApproval:true`다. 집중 15건, Windows 브라우저 두 파일을 제외한 automation 237건, 루트 전체 918건과 Astro check 오류 0건이 통과했다. 실제 기관 응답, 검수자 인증, 외부 서명, DB 저장 위치, atomic publish RPC·migration과 공개 게시를 구현하거나 승인한 것은 아니다.
- **A7 이행 상태·record 단일 정본**: `implementation-status-contract.json` schema v2가 fallback 2종과 추적 상태 5종의 label·설명·AA 색상·추적 여부·HTTPS 근거 필수 여부, 책임 기관·공개 설명·시각·근거 URL 길이, canonical UTC 형식과 URL credential 금지를 함께 정의한다. 공개 웹·표·DOCX는 이 계약에서 TypeScript 상태·표시 토큰·record 경계를 파생하고, Node publish preflight는 같은 파일을 읽어 schema·fallback·메타데이터·record 제약을 fail-closed 검증한 뒤 모든 추적 상태와 비정규 날짜·경계 초과 입력을 실행 테스트한다. 따라서 UI와 승인 plan의 vocabulary·시간·필드·완료 근거 규칙이 독립적으로 drift하지 않는다. 웹 집중 42건, plan 집중 17건, 루트 전체 922건, Windows 브라우저 두 파일을 제외한 automation 239건과 Astro check 오류 0건이 통과했다.
- **A7 공개 출처 plan preflight**: `platform-result-source-plan.mjs`가 캡처된 `result_get`과 스코프 내 `issue_items`를 로컬에서 대조해 쟁점별 조 집합·cluster 보정 분모·전체 미분류 건수를 재계산한다. 불일치·주제 누락·중복 링크·교차 주제 연결·DB vocabulary 밖 값을 plan 생성 전에 fail-closed하고, 출력에는 원문 대신 issue/item/submission/cluster 안정 ID와 content SHA-256만 보존한다. plan은 canonical self-checksum과 전체 재생성 검증을 제공하지만 `dryRun:true`, `databaseMutationExecuted:false`, `publicPayloadWritten:false`, `requiresApproval:true`이며 외부 서명·검수자 인증·공개 승인 증거가 아니다. 집중 10건, Windows browser 두 파일을 제외한 automation 222건, 루트 전체 904건과 Astro check 오류 0건이 통과했다. 무필터 Windows automation에서는 기존 Playwright 제한 10건이 timeout되어 별도 경계로 기록했다. 실제 시민 데이터 캡처, `result_page` 변경, 공개 역링크와 이행추적은 수행하지 않았고 atomic publish payload·public read route·DB/RPC 변경은 계속 사용자 승인 대상이다.
- **A7 공개 원문 역링크 UI 계약(2026-08-26)**: 공개 결과의 선택적 `issue.source_references`는 exact 9개 필드, opaque reference key, 조·순번·`core|extra`, 최대 2,000자 원문, SHA-256 형식, canonical UTC 검수시각, `reviewed` 공개상태와 `org_admin|hq` 역할을 모두 만족하고 쟁점 자체도 검수 완료일 때만 표시한다. 하나라도 잘못되거나 중복 key·승인 밖 필드가 있으면 해당 묶음 전체를 숨기고 확인 필요 상태만 남긴다. 승인 원문은 쟁점→원문 카드→쟁점의 키보드 포커스 가능한 역링크, 조·종류·순번·검수일을 제공한다. 접근성 browser fixture도 원문 섹션이 렌더된 뒤 전체 axe·skip focus·반응형 검사를 실행하도록 바꿨고 수동 matrix에 데스크톱·모바일 원문 왕복 검사를 추가했다. 집중 웹 54건, Chromium 감사 15건, automation 전체 27개 파일·422건, 루트 전체 64개 파일·1,072건, Astro check 오류 0건이 통과했다. UI는 digest 형식만 확인하며 원문과 digest의 실제 결속은 승인된 publish preflight/RPC가 맡아야 한다. production payload·DB·RPC·migration·실제 시민 원문·공개 게시에는 접근하거나 변경하지 않았다.
- **A7 DOCX 원문 역링크 계약(2026-08-26)**: 공개 결과 보고서 모델이 웹에서 이미 검증된 원문만 이어받아 쟁점·원문별 결정적 bookmark를 만들고, 쟁점→첫 원문과 각 원문→쟁점의 Word internal hyperlink를 직렬화한다. 원문 카드에는 조·종류·순번·공개 검수일·검수 역할·payload SHA-256을 보존한다. 미검수 쟁점이나 잘못된 원문 묶음은 본문을 완전히 버리고 확인 필요 안내만 문서에 남긴다. 원문이 없는 기존 payload의 문서 구조와 섹션 번호는 유지하고, 원문이 있을 때만 별도 근거 절을 추가한다. 집중 DOCX 29건, 루트 전체 64개 파일·1,077건, automation 전체 27개 파일·422건과 Astro check 오류 0건(기존 hint 49건)이 통과했다. 합성 DOCX 구조 감사는 high 0건이며 새 원문 메타를 정의 목록으로 구성해 기존 표 경고를 늘리지 않았다. 일반 CI의 path filter에도 `src/islands/result/**`를 추가해 이후 결과 웹·DOCX 변경이 clean 전체 테스트를 우회하지 못하게 했다. 로컬 LibreOffice가 없어 PNG 시각 렌더는 실행하지 못했고 clean CI와 실제 Word·보조기기 수동 왕복은 별도 증거다. production payload·DB·RPC·migration·실제 시민 원문·공개 게시에는 접근하거나 변경하지 않았다.
- **A7 원문 공개 publish preflight(2026-08-26)**: 기존 `platform-result-source-plan.mjs`에 명시적 `replace_all` 공개 모드를 추가했다. 모든 연결된 issue/item에 공개 또는 보류 결정을 요구하고, 공개 발췌를 canonical 원문 UTF-8 bytes와 SHA-256으로 결속하며, 공개 결과 집계 drift·미검수 쟁점·역할형 검수자·시각 순서·보류 발췌 null·exact 공개 9필드 계약을 fail-closed 검증한다. 승인된 원문만 넣은 전체 `atomicResultBody`, 전후 body SHA-256, 원문별 검수 patch, 입력 hash와 self-checksum을 재생성 검증한다. 원문을 포함하는 plan은 symlink/junction 해석 뒤 repository 밖 경로에만 no-overwrite·사용자 전용 모드로 저장하고 stdout에는 원문·검수자 ID를 싣지 않는다. 집중 23건, automation 전체 27개 파일·435건, 루트 전체 64개 파일·1,077건과 Astro check 오류 0건(기존 hint 49건)이 통과했다. 이는 local dry-run이며 production payload·DB·RPC·migration·실제 원문 캡처·공개 게시에는 접근하거나 변경하지 않았다.

## 코드·스키마 점검 (2026-08-29)

읽기 전용 전수 점검 결과는 `AUDIT_2026-08-29.md`에 있다. 신규 트랙(platform·submission·attendance)에서 「구현됐다고 적혀 있는데 아닌 것」은 나오지 않았고, `DECISIONS.md`의 divergence 3건은 실측과 일치했다. 지적은 레거시 `public.cv_*` 뷰 4종과 `cv_set_active`에 몰려 있다(anon 전권 DML·RLS 우회·사용처 없음). **행사 당일이라 손대지 않았고 회수 마이그레이션은 승인 대기다.**

## 병합 전 게이트

### ✅ G1. SQL 파싱·계약 검증 — 종료 (2026-08-09)
throwaway Postgres16(Docker)로 **실제 파싱 + 함수 본문 검증 + 계약 스모크 + negative** 전부 통과. 재현 하네스 `supabase/verify/`. 발견된 이식성 이슈 1건(P1 gen_random_bytes 미한정) 정정. 상세 `supabase/verify/README.md`.
- 남은 라이브 확인: 실제 Supabase(전용 DB/병합)에 적용 시 anon RPC 계약 재확인(`result_get`→200 null=적용됨).

### ✅ G2. publish 권한 상향 — 종료 (2026-08-09)
`result_publish`/`result_unpublish`를 조 join_code → **HQ 토큰(attendance scope='hq') 서명**으로 상향. 조 코드 publish 차단(컨테이너 검증: "attendance authorization required"), HQ 토큰만 성공(`published_by=hq:actor`). 플랜 §2-3 부합.
- Phase 2: HQ 공유비밀 → membership 인증 + `org_of_token` org 일치 검사 추가(현재 레거시 HQ 토큰은 org null 가능).

### ✅ 라이브 HTTP E2E — 통과 (2026-08-09)
PostgREST+JWT+RLS throwaway 스택으로 **플랫폼 UI 실 전송(supabase-js→PostgREST→RLS/RPC)** 재현. issue_upsert/items/link/review·**RLS 테넌트 격리(자기 org만)**·anon 401·**G2 조코드 거부/HQ토큰 성공**·result_get body(ResultView 계약 일치) 전부 통과. 상세 `supabase/verify/e2e_http.md`. **= "빌드된다"를 넘어 "실 HTTP로 돌아간다" 증명.**

### G3. org_id NOT NULL 전환 — 마이그레이션 작성됨
`platform_p1b_backfill.sql`(기본 org + backfill + NOT NULL). 단일 테넌트 가정. **데이터 있는 실 프로젝트 적용 시 실행**(빈 상태에선 NOT NULL만).

### 프로덕션 런칭 = 배포 행위 (구현 갭 아님)
전용 Supabase 프로젝트(사용자 계정) + Auth + Cloudflare SPA rewrite. **턴키 런북 `docs/platform/PROVISIONING.md`.** 스키마·RLS·파이프라인·보안은 검증 완료.

## Phase 2 진입 전 사용자 결정 (플랜 §5)

1. **테넌시 모델** — A row-level(권장) / B schema / C db-per-tenant (데이터 소재지 요건 시 C)
2. **Supabase Auth 범위** — 운영자·기관관리자만 vs 진행자까지 (참여자 무기명은 불변)
3. **HQ 공유비밀(`climate2026`) → membership 전환 시점** — Phase 2 선행조건(G2와 연동)
4. **셀프서비스 범위** — 완전 마법사(gongron급) vs 설계는 SQL·게이트만 UI (= 설계 마법사 Phase 3 착수 여부)
5. **호스팅·격리** — 단일 Supabase vs 기관별 분리
6. **플랫폼화 착수 자체** — Phase 1까지(vertical 완성) vs Phase 2+ 진행. 재사용 수요 확인 전제

## 보류 중 (결정 대기)

- **설계 마법사(Phase 3)**: 승인 전 로컬 청사진 작성·검증·JSON export·검증된 JSON 재개는 구현했다. 실제 assembly/session/topic/team 생성 RPC, 서버 저장·불러오기와 기관별 권한은 플랜상 Phase 2(tenancy) 이후이며 §5-4 결정과 DB 변경 승인이 필요하다.
- **분석코어 어댑터**: consensus/DQI 산출의 recommendation candidate와 minority concern을 `origin=ai`, `review_status=draft`인 검수 전용 import plan으로 변환하는 로컬 dry-run 어댑터를 구현했다. source UID를 기존 submission item UUID·cluster UUID에 매핑하고 누락·중복·무출처·스키마 불일치를 fail-closed하며 DB/API를 호출하지 않는다. schema version 2 계획은 두 입력 파일의 정확한 바이트 SHA-256과 canonical self-checksum을 포함하고, `--verify-plan`은 동일 입력으로 계획을 재생성해 불일치·우발 변경을 로컬에서 탐지한다. 외부 secret·서명이 없는 self-checksum이므로 작성자 진위나 의도적 재생성에 대한 tamper-evident 증거는 아니다. 실제 issue 적재(service_role), 8/29 산출물 첫 실행과 사람 검수는 별도 승인·실물 확보 후 진행한다.
- **라이브 프로비저닝**: 전용 DB + Supabase Auth + Cloudflare Pages SPA fallback rewrite(딥링크). 병합 결정 시.
- **A6 자동 export 훅**: 행사일 snapshot workflow에 승인 게이트와 off-DB Drive payload export 경로를 구현했다. repository variable `PLATFORM_SNAPSHOT_ENABLED`는 기본 `false`라 현재 동작과 Drive JSON 형상은 기존 `cv_snapshot_now` 그대로다. 승인 후 정확히 `true`로 켜면 기존 snapshot을 먼저 보존하고 `platform_snapshot_now` 행의 실제 payload를 재조회해 같은 Drive JSON에 담는다. 활성 export에는 GitHub run·commit·snapshot ID·key ID와 platform 행을 외부 secret 기반 HMAC-SHA256으로 결속한 audit manifest가 붙으며, 키·key ID·필수 provenance 누락 시 platform RPC 전에 실패한다. 과거 archive 검증을 위한 키 외부 백업·회전·폐기 절차와 `--verify`·`--rehearse` 읽기 전용 점검을 RUNBOOK에 고정했다. `--rehearse`는 서명·구조·건수에 더해 archive 내부 ID/FK·고유키·투표 문항 허용 척도·필수 여부 boolean과 응답 client ID의 DB 길이 범위를 검사하고, 모든 응답 키가 archive에 존재하는 같은 ballot 문항인지 전수 확인한다. 복원 순서와 `org`·주제·조·회차·공론화 외부 부모의 중복 제거 건수만 출력하며 DB나 Drive를 읽거나 쓰지 않는다. `result_page`의 다형 scope도 topic/session/assembly별 부모 건수로 분리한다. 현재 payload에는 `org`, `discussion_topic`, `team`, `session`, `assembly` 부모 collection이 없어 이 결과는 독립 복원 완료가 아닌 `databaseRestoreExecuted: false` 복구 preflight다. 부모 collection을 snapshot 계약에 추가하는 DB 변경은 계속 사용자 승인 대상이다. `finalize-report`도 Drive archive를 pagination 포함 실측해 KST 행사 일정에서 만든 정확한 UTC timestamp 집합, 각 capture의 필수 페이지 파일 완비, capture/snapshot timestamp 유일성을 확인한 뒤 건수를 Sheets/Discord에 기록한다. 범위 밖 capture·빈 snapshot·5% 초과 누락·알림 실패는 fail-closed한다. 7·8·9·10월 정본 워크숍 5건은 capture·snapshot의 지원 가능한 5분 cron과 종료 4시간 뒤 finalize cron에 정확히 대응하며, automation source-contract 테스트가 세 workflow의 누락·시간 드리프트, 잘못된 날짜·시각·5분 비정렬, 정본 이름·round ID·날짜 중복/정렬과 finalize cron→concurrency/job 이름 매핑 누락을 차단한다. 각 시점이 별도 증거인 capture·snapshot은 concurrency coalescing을 사용하지 않고, finalize만 워크숍별 group으로 중복 대기 실행을 합친다. scheduled finalize는 cron의 연례 반복이 과거 워크숍을 다시 갱신하지 않도록 정본 KST 당일·다음 날만 허용하며 수동 명시 실행은 별도로 유지한다. Drive 부모 ID는 일정 파일 placeholder가 아니라 GitHub secret 한 곳에서만 주입한다. 동일 일자·워크숍 Sheets 행은 재실행 때 update한다. 회차별 정본 집계가 없는 최종 표 수는 0으로 과장하지 않고 `미집계`로 남긴다. 실행당 DB 행이 1개에서 2개로 늘기 때문에 secret 구성과 활성화 역시 계속 사용자 승인 대상이다. 합성 archive의 실제 격리 PostgreSQL 복원 rehearsal은 구현됐고, 원본 부모 데이터까지 포함한 독립 복원·PITR/WAL 설정·사용자 행위용 별도 운영 감사로그는 아직 미구현이다. 기록 화면 CSV도 이를 대체하지 않는다.

**A6 ballot 행 복원 제약 검증(2026-08-26):** archive recovery preflight가 `ballot.title`의 PostgreSQL 공백 제거 후 1~200자, `status`의 5개 허용 상태, `ballot_item.statement`의 공백 제거 후 1~300자, `ordinal`의 PostgreSQL `integer` 범위를 복원 전에 검사한다. Unicode 길이는 DB character 의미로 계산하고, 실패 오류에는 제목·문항·ID를 넣지 않는다. 집중 36건, automation 전체 381건, 루트 전체 64개 파일·1,060건이 통과했다. 이는 서명된 로컬 archive의 읽기 전용 검사이며 DB 연결·격리 복원·Drive 변경·PITR/WAL 검증을 수행하지 않는다.

**A6 submission 행 복원 제약 검증(2026-08-26):** 같은 recovery preflight가 `submission.status`의 4개 허용 상태와 `submission_item`의 PostgreSQL `integer` 순서, `core|extra` 종류, 공백 제거 후 1~2,000자 본문, nullable 2,000자 근거를 검사한다. Unicode 길이는 DB character 의미로 계산하고 실패 오류에는 참가자 원문·근거·ID를 넣지 않는다. 집중 43건, automation 전체 388건, 루트 전체 64개 파일·1,060건이 통과했다. 명시되지 않은 nullable 근거와 provenance는 DB default 복원 가능성을 보존한다. PostgreSQL `jsonb NOT NULL`은 SQL `NULL`만 금지하고 JSON 값 `null`은 허용하므로 archive의 provenance가 JSON `null`이라는 이유만으로 거부하지 않는다. DB 연결·격리 복원·Drive 변경·PITR/WAL 검증은 수행하지 않았다.

**A6 분석·공개 결과 행 복원 제약 검증(2026-08-26):** recovery preflight가 `issue.label`의 공백 제거 후 1~200자, nullable `stance` 6종·`frequency_class` 4종, 필수 `origin` 2종·`review_status` 3종, `issue_link.linked_by` 2종과 `result_page.title`의 공백 제거 후 1~300자를 검사한다. result body의 JSON `null`은 유효한 JSONB 값으로 보존한다. 실패 오류에는 쟁점·공개 결과 제목이나 ID를 넣지 않는다. 집중 51건, automation 전체 396건, 루트 전체 64개 파일·1,060건이 통과했다. 이 검증은 DB 연결·격리 복원·공개 게시를 수행하지 않는다.

**A6 archive 테넌트 관계 검증(2026-08-26):** archive 안에서 확인 가능한 `issue_link → submission_item → submission` 경로가 issue와 같은 discussion topic·조직인지 검증하고, nullable 익명 응답 org는 허용하되 명시된 `ballot_response.org_id`는 상위 ballot 조직과 같아야 한다. 성공 결과에는 비식별 `checkedTenantRelationships` 건수만 추가하고 불일치 오류에는 조직·topic·행 ID를 넣지 않는다. 집중 54건, automation 전체 399건, 루트 전체 64개 파일·1,060건이 통과했다. archive에 없는 session·topic·org 부모의 권위 관계까지 증명하는 것은 아니며 실제 격리 DB 복원도 수행하지 않는다.

**A6 archive UUID 형식 검증(2026-08-26):** PostgreSQL snapshot JSON이 내보내는 형식에 맞춰 행 ID, archive 내부 FK, 외부 `org`·topic·team·session·assembly 참조와 nullable cluster·응답 org가 소문자 8-4-4-4-12 canonical UUID인지 복원 전에 검사한다. 형식 오류와 부모 미존재를 분리해 보고하며 오류에는 실제 UUID나 원문을 넣지 않는다. 집중 57건, automation 전체 26개 파일·402건, 루트 전체 64개 파일·1,060건이 통과했다. 이는 서명된 archive의 읽기 전용 preflight이며 DB 연결·실제 복원·Drive 변경·PITR/WAL 검증을 수행하지 않는다.

**A6 격리 PostgreSQL 복원 rehearsal(2026-08-26):** HMAC 검증과 읽기 전용 preflight를 통과한 archive만 일회성 `verify` 데이터베이스용 SQL로 변환한다. SQL은 데이터베이스 이름과 대상 8개 테이블의 빈 상태를 이중 확인하고, archive에 없는 상위 부모를 합성 fixture로 만든 뒤 실제 migration 테이블에 복원 순서대로 삽입한다. PostgreSQL 16에서 각 collection 1행, 내부 link와 ballot answer를 포함한 합성 서명 archive가 FK·check·unique·trigger 아래 모두 복원됐고, 실측 건수 일치 후 전체 transaction rollback과 잔존 0행을 확인했다. CI도 같은 migration chain과 실행을 반복한다. 이 검증은 원래 부모 내용·권위 관계, PITR/WAL, 운영 감사로그, 운영 DB 적용을 대체하지 않으며 production credential이나 시민 데이터를 사용하지 않는다.

**A6 final submission 복원 trigger 경계(2026-08-26):** 실제 archive의 `final` submission 아래 item은 정상 운영용 잠금 trigger가 막기 때문에, 격리 restore SQL이 시작 시 정확한 `submission_item_lock_guard`의 활성 상태를 확인하고 item 삽입 구간에만 해당 trigger를 비활성화한 뒤 즉시 재활성화하도록 보강했다. PostgreSQL 16 부정 rehearsal은 trigger가 이미 비활성화된 DB를 삽입 전에 거부했고, 테스트 전용 ballot trigger가 제목을 바꾼 경로는 `row mismatch: ballot`으로 거부했다. 정상 rehearsal은 final 상태·최종화 시각·주체를 가진 submission과 item을 함께 복원하고, 8개 collection의 모든 composite row를 archive 기대 행과 identity key로 null-safe 대조해 metadata·본문·응답 drift와 기대 행 누락이 없음을 확인한 뒤 `archiveRowsVerified:true`, `businessTriggerRestored:true`, rollback 후 대상 행 0건, trigger 활성 상태를 확인했다. 다른 trigger와 FK·check·unique 제약은 우회하지 않는다.

**A6 archive schema drift 손실 방지(2026-08-26):** recovery preflight가 payload의 허용 collection과 대상 8개 collection의 허용 열을 현재 migration 계약으로 검사한다. 복원기가 새 collection을 누락하거나 PostgreSQL `jsonb_populate_recordset`이 모르는 열을 무시해 nullable metadata 오타·schema drift를 성공으로 오인하는 경로를 복원 SQL 생성 전에 fail-closed한다. 알 수 없는 collection과 각 collection의 알 수 없는 열을 주입한 음성 테스트는 모두 거부됐고 오류에는 collection·열 이름이나 값을 넣지 않는다. 기존 실제 격리 PostgreSQL archive의 `ballot.subgroup`을 포함한 전체 열 계약은 유지한다. production DB·Drive·archive는 변경하지 않았다.

**A6 signed archive envelope schema drift 차단(2026-08-26):** HMAC 검증기가 archive 최상위 `{legacy,platform,audit}`, platform snapshot 행, audit와 integrity manifest에 exact-field 계약을 적용하고 최상위 필수 키와 platform의 `id|source|payload`를 요구한다. read-only file verifier도 payload collection과 선언 count에 알 수 없는 필드를 허용하지 않아, 새 snapshot 열·manifest 내부 메모·추가 collection/count를 서명만 맞는다는 이유로 조용히 버리지 않는다. 격리 PostgreSQL용 archive 생성 fixture도 실제 export와 같은 `legacy` envelope를 만들며 단위 회귀가 이를 고정한다. 오류에는 알 수 없는 필드명·값·archive 원문을 넣지 않는다. focused 64건, automation 전체 27개 파일·441건, 루트 전체 64개 파일·1,077건과 Astro 330개 파일 오류·경고 0건(기존 hint 49건)이 통과했다. production DB·Drive·archive·credential은 읽거나 변경하지 않았다.

**A6 legacy+platform archive HMAC schema v2(2026-08-26):** 신규 자동 export audit는 기존 platform-only schema v1에서 schema v2 `legacy+platform+provenance`로 올라가 기존 투표·의제 snapshot RPC 결과와 platform snapshot 행을 같은 HMAC에 결속한다. legacy가 바뀌면 검증이 실패하고, 빈 legacy receipt는 platform RPC 전에 거부한다. 과거 v1은 platform 복구 호환을 유지하되 verifier·preflight·restore 준비 결과가 `integrityTarget:platform+provenance`, `legacyIntegrityVerified:false`를 명시해 legacy까지 검증됐다고 과장하지 않는다. 새 v2는 같은 결과가 `legacyIntegrityVerified:true`다. focused 67건, automation 전체 27개 파일·444건, 루트 전체 64개 파일·1,077건과 Astro 330개 파일 오류·경고 0건(기존 hint 49건)이 통과했다. production DB·Drive·archive·secret은 읽거나 변경하지 않았다.

**A6 v1 platform-only 명시적 호환 경계(2026-08-26):** 저수준 `verifySnapshotArchiveIntegrity()`는 기본적으로 legacy까지 결속한 schema v2만 성공시키며, schema v1은 `{allowPlatformOnlyV1:true}`를 명시하지 않으면 `false`다. file verify·rehearse는 과거 platform 복구를 위해 이 경로를 내부에서 명시적으로 선택하고 기존과 같이 `legacyIntegrityVerified:false`를 출력한다. 따라서 범위 정보 없이 boolean 하나만 본 호출자가 v1을 전체 archive 무결성으로 오해하는 경로를 차단했다. production DB·Drive·archive·secret은 읽거나 변경하지 않았다.

**A6 off-DB append-only export 충돌 검증(2026-08-26):** 수동 OneDrive exporter가 같은 이름의 파일 존재만 보고 무조건 skip하던 경로를 강화했다. 기존 파일은 현재 source row의 JSON 직렬화와 byte 단위로 정확히 같을 때만 skip하며, 다르면 식별값·payload를 오류에 넣거나 어느 쪽도 덮어쓰지 않고 fail-closed한다. 변조 sentinel 회귀 테스트는 기존 성공 동작을 먼저 재현한 뒤 거부 동작으로 고정했다. 집중 12건, automation 전체 27개 파일·422건, 루트 전체 64개 파일·1,060건과 Astro check 오류 0건이 통과했다. 실제 Supabase·OneDrive·credential에는 접근하지 않았고 PITR/WAL 및 운영 감사로그는 여전히 미구현이다.

**A2 권한 활성화 초안 분리(2026-08-17):** staff 직접 권한을 기본 P1→P1C→P2 migration chain과 주석에서 분리한 `platform_p1c_org_selection_activation.sql`을 추가했다. 이 초안은 schema USAGE, membership SELECT, assembly/session/topic/submission/ballot의 SELECT·INSERT·UPDATE만 발급하고 DELETE나 org_context 직접 접근을 허용하지 않는다. 기본 driver는 이 파일을 실행하지 않고 semantic rehearsal만 별도 적용→활성 검증→권한 rollback→휴면 검증 순서를 실행한다. production migration·GRANT·Auth·membership·data는 변경하지 않았다.

**A2 권한 원자성 검증(2026-08-17):** activation과 대응 권한 rollback을 각각 하나의 transaction으로 묶었다. PostgreSQL 16 semantic rehearsal은 활성화 대상 테이블 누락 시 앞서 실행된 schema·membership GRANT가 남지 않는지, rollback 대상 누락 시 기존 활성 권한이 부분 회수되지 않는지를 음성 경로로 검증한다. 실제 production 권한은 변경하지 않았다.

**A2 count-only preflight RPC 초안(2026-08-17):** service-role 전용 `platform_activation_preflight()`가 12개 `org_id` 대상·상위 조직 경로·Auth 상태·역할 커버리지를 비식별 계수로만 반환하는 별도 migration·rollback 초안을 추가했다. 읽기 전용 `STABLE` 함수와 `statement_timestamp()`가 호출 statement의 고정 snapshot·기준 시각을 공유하며, CLI는 exact response schema, 테이블별 합, blocker·summary 일치를 다시 검증하고 원시 ID·알 수 없는 필드·DB 오류 내용을 거부한다. 별도 post-apply verifier는 함수 보안 속성·고정 설정·service-role 단독 권한과 실제 count-only 응답을 readiness와 분리해 읽기 전용 검증한다. schema v2 approval evidence는 A2 CLI·automation lockfile·전체 migration/rollback/verifier의 clean 상태를 서명하고 tracked 변경과 untracked SQL을 fail-closed한다. 집중 automation 45건, Astro check 318파일 오류 0건, 일회성 PostgreSQL 16의 blocker→ready·`provolatile='s'`·권한·post-apply verifier·rollback 의미 검증이 통과했다. 전체 Windows automation은 변경 관련 테스트를 포함한 274건이 통과했고 기존 실브라우저 timeout 10건이 남았으며, Linux CI를 최종 전체 기준으로 삼는다. 초안은 기본 migration chain에 포함하지 않았고 production DB·Auth·membership·GRANT·data는 변경하지 않았다.

**A2 활성화 승인 묶음(2026-08-17):** `platform-a2-activation-bundle.mjs`가 P1·P1C·P2 선행 스키마, count-only preflight 설치·post-apply 검증·fresh HMAC evidence 검증, staff GRANT 적용·활성 검증, GRANT 회수·휴면 검증·preflight 제거·제거 확인의 정확한 순서와 13개 source 참조의 byte SHA-256을 `evaluation/platform-a2-activation-bundle.json`에 고정한다. verifier는 canonical checksum과 현재 source 전체 재구성을 함께 비교해 자체 재서명된 순서·파일 변경도 거부하며, builder·manifest 자체도 ready evidence clean-tree와 CI trigger 범위에 포함된다. 기존 추적 manifest는 자동으로 덮어쓰지 않고 별도 candidate diff 승인 뒤에만 교체한다. 집중 automation 56건과 throwaway PostgreSQL 16의 적용→활성→GRANT 회수→preflight 제거 verifier→P1C rollback 의미 검증이 통과했다. Windows automation 전체는 새 10건을 포함해 290/299건이 통과했고 기존 Playwright 병렬 timeout 9건은 Linux CI 최종 검증 경계로 남겼다. 이 manifest는 `dryRun:true`, `databaseMutationExecuted:false`, `requiresApproval:true`인 검토 자료이며 production SQL·Auth·membership·GRANT·traffic은 실행하지 않았다.

**A2 migration 초안 승인(2026-08-17):** 사용자는 `platform_p1c_org_selection.sql` 설계 초안과 대응 rollback·UI·정적 계약 테스트의 저장소 채택을 승인했다. 이 승인은 production 적용 승인이 아니며 count-only preflight RPC 설치, backfill·NOT NULL, staff GRANT, Auth 계정·membership 생성, 데이터 변경과 traffic open은 계속 별도 승인 대상이다. 승인 기록 과정에서 DB·Auth·migration 실행은 수행하지 않았다.

**A3 접근 프로비저닝 계획·executor core(2026-08-17):** 기관 접근 화면과 CLI가 하나의 추적 JSON contract로 schema·4개 역할·256KiB·dry-run 경계를 공유한다. 저장소 밖 기관 접근 계획을 읽는 `platform-access-provisioning-plan.mjs`는 exact source byte hash, deterministic operation ID, lookup-before-mutation·중단·감사 receipt·부분 성공 reconciliation 정책을 가진 계획만 생성하며 checksum과 원 입력 전체 재생성으로 변조를 검증한다. 저장소·symlink/junction 내부 경로, 무단 덮어쓰기, 비canonical·중복·빈 계획, mutation 표시를 fail-closed하고 stdout/error에는 이메일·Auth UUID·credential을 노출하지 않는다. Adapter-independent executor core는 exact plan과 15분 HMAC 승인, stable lookup, 순차 apply, response-loss 재조회, 이미 적용된 작업 건너뛰기, unresolved mutation 자동 재시도 금지, 첫 실패 뒤 pending 보존과 비식별 receipt persistence를 실행 검증한다. A3·preflight 집중 automation 51건과 UI 집중 58건, Astro check 322파일 오류 0건이 통과했다. Windows automation 전체는 309/317건이 통과했고 기존 접근성 Playwright 5초 병렬 timeout 8건은 Linux CI 최종 검증 경계로 남겼다. 현재 invitation table에는 안정 unique/operation ledger가 없으므로 production Supabase/Auth adapter와 CLI는 연결하지 않았고, 실제 계정·초대·메일·membership·DB·GRANT를 실행하지 않았다.

**A4 설계 프로비저닝 dry-run(2026-08-17):** 설계 화면과 Node CLI가 추적 `design-blueprint-contract.json`에서 schema v4, 운영 방식·준비도 vocabulary, slug·크기 한계와 승인 경계를 공유한다. 저장소 밖 청사진의 exact bytes를 읽어 assembly→session→topic/team parent 순서, 안정 reference·operation ID와 원문 SHA-256을 가진 plan을 만들고, verifier가 checksum과 원 청사진 전체 재생성을 함께 대조한다. 회차 순서·중복 slug·준비도 정본 순서·실제 날짜·표준 조 이름·계층·통계·알 수 없는 필드가 어긋나면 원문 비노출 오류로 중단한다. live base에는 session title/slug가 있지만 생성 migration이 없고 team에는 ordinal/stable business key가 없다는 실제 차이를 반영해 migration-owned session base, team stable identity, join code 정책, idempotent ledger, design RPC의 다섯 blocker를 schema v2 plan에 고정했다. 권장 서버 계약과 승인 질문은 `A4_DESIGN_PROVISIONING_CONTRACT.md`에 분리했다. `readyForExecution:false`, `serverContractImplemented:false`, `databaseMutationExecuted:false`는 유지한다. A4 UI 집중 29건, plan 집중 10건, 루트 전체 64개 파일·1,060건과 Astro check 324파일 오류 0건이 통과했다. Windows automation 전체 25개 파일·327건도 통과했다. 접근성 실브라우저 테스트 10건에는 Windows Chromium 초기화·감사 시간을 반영한 60초 개별 제한을 적용하고, 정적 계약 테스트의 기본 제한은 유지했다. production adapter·executor·migration은 추가하지 않았고 DB/API/environment credential에 접근하지 않는다.

**A4 migration 초안 승인·리허설(2026-08-25):** 사용자 승인에 따라 `platform_p3_design_provisioning.sql` 초안과 대응 rollback, 읽기 전용 preflight/post-apply verifier, PostgreSQL 의미 테스트, byte SHA-256 approval bundle을 추가했다. 초안은 session 필수 열 계약을 재확인하되 기존 행을 추정 backfill하거나 NOT NULL로 바꾸지 않고, team에 nullable ordinal과 `(session_id, ordinal)` stable identity·양수 capacity 제약을 둔다. schema v2 preflight는 기존 team 행이 있을 때도 shape·중복·capacity가 안전하면 `readyForAdditiveMigration:true`를 반환하고, migration 뒤 승인된 ordinal mapping과 session/team 필수값·부모/org 정합성이 끝나기 전까지 `readyForActivation:false`를 유지해 migration-before-column과 backfill-before-activation의 순환을 해소한다. `design_provisioning_operation` ledger와 `design_provision(jsonb, bytea)`는 원 청사진 bytes의 길이·SHA-256을 재계산하고, 기관을 `org_of_uid()`에서 파생해 `org_admin|hq`를 재검증하며 exact plan/checksum/operation hash/부모 순서/기존 resource 일치를 확인한 뒤 plan 전체를 한 transaction으로 처리한다. join code는 transaction 안에서 6자리로 생성하고 제한된 충돌 재시도 뒤 전체 rollback하며 ledger에는 저장하지 않는다. `design_provisioning_status(jsonb)`는 mutation plan·원본 bytes 없이 비식별 reconciliation query만 받는 `STABLE` read-only RPC다. 현재 기관 `org_admin|hq`와 ledger checksum/type/resource org를 재검증해 일부 누락은 `pending`, 전체 일치는 기존 resource·team join code의 `replayed` 응답, 충돌은 안정 오류로 반환한다. 두 RPC와 ledger는 public/anon/authenticated/service_role 모두 권한이 회수된 휴면 초안이다. 기본 rollback은 ledger 행이나 non-null team ordinal이 있으면 객체 제거 전에 `design_provisioning_rollback_requires_data_plan`으로 전체 transaction을 거부해 감사기록·mapping의 조용한 손실을 막는다. PostgreSQL 16 격리 DB에서 legacy team 보존→additive-ready→migration 후 activation 차단→test-only mapping 후 activation-ready, source bytes 불일치 거부, 정상 생성, exact replay, read-only pending/complete reconciliation 무변경, checksum·role 거부, payload·parent 충돌, join-code 소진, 늦은 summary 실패의 전체 transaction rollback, 각 RPC의 unsafe authenticated GRANT 거부, populated rollback 거부와 객체 보존, exact-scope throwaway cleanup 뒤 최종 rollback을 검증했다. plan blocker는 production 적용 미승인과 migration/RPC/ledger/join-code 미활성화로 갱신했으며 `readyForExecution:false`는 유지한다. production DB·Auth·membership·GRANT·backfill·NOT NULL·traffic·executor에는 접근하거나 변경하지 않았다.

**A4 실행 승인 artifact·claim lifecycle·receipt 계약(2026-08-25):** design plan 모듈에 adapter-independent HMAC 승인 seal/verifier, authorization claim·terminal finalization core와 비식별 execution receipt seal/verifier를 추가했다. exact plan/source, organization UUID·host, 같은 canonical Auth 승인자/실행자와 `org_admin|hq` 역할, approval/execution ID, key ID와 최대 15분 유효기간을 결속한다. adapter는 approval state와 live membership/org/host를 한 snapshot으로 읽고 CAS claim·finalize에서 expected snapshot을 원자적으로 재검증해야 하며, core는 각 응답의 transaction 시점 context를 다시 확인한다. scoped `revokedAt`, 다른 execution/org/host/actor/role을 거부하고 동일 진행 중 claim과 동일 terminal outcome만 복구한다. 유효 시간 안에 시작된 claim은 승인 만료 뒤에도 감사 상태를 닫을 수 있지만 `completed↔failed` 덮어쓰기와 terminal state 재-claim은 거부한다. in-memory test double의 실제 동시 claim·finalize는 각각 하나만 `new`, 같은 경쟁은 `reconciled`, 이후 호출은 `existing`으로 수렴한다. receipt는 승인된 review plan에서 동일 operation/source의 실행형 RPC checksum을 결정적으로 파생해 approved/executed checksum을 함께 결속하고, 성공 RPC의 exact response를 검증한 뒤 resource UUID·join code를 제거한다. 실패는 allowlist 코드와 rollback 확인만 봉인한다. injected lifecycle core는 claim 전·직후 receipt lookup, RPC adapter, append-only receipt, terminal finalize를 순서대로 조율한다. 단 하나의 `new` claim 소유자만 RPC를 호출하고 receipt 없는 기존·reconciled claim은 자동 재호출 없이 reconciliation을 요구하며, append 응답 유실 뒤 저장된 receipt가 있으면 이를 복구해 RPC를 다시 호출하지 않는다. 별도 명시적 reconciliation lifecycle은 기존 active claim에서만 ledger·operation lookup 결과를 받아 `pending`이면 열린 상태를 보존하고 완료 결과만 비식별 receipt로 봉인·재조회한 뒤 종결한다. 새 claim이나 execution adapter는 호출하지 않으며 adapter 원시 오류도 노출하지 않는다. 저장·RPC 미확정 상태에서는 claim을 열어 둔다. 저장소 밖 로컬 durable rehearsal store는 marker로 경계를 고정하고 approval별 immutable SHA-256 chain journal과 execution별 append-only receipt를 사용해 adapter 재시작, concurrent CAS 단일 claim, 동일 receipt replay, 다른 receipt 충돌과 journal 변조·junction escape 거부를 검증한다. unclaimed local revocation과 synthetic membership context 무효화도 같은 journal CAS에 기록해 revocation/claim 경쟁에서 하나만 성공하고, claim 뒤 membership 비활성화 시 finalize가 거부되어 claim이 열린 상태를 유지함을 확인했다. approval transition은 marker·record에 `immutable_hard_link_v1`을 결속하고 다음 immutable sequence record의 hard-link 원자 게시를 lock-free CAS로 사용해 persistent lock을 남기지 않는다. crash temp는 존재하면 owned regular file인지 검사하되 다른 publisher가 열거 직후 정상 unlink한 ENOENT는 경쟁 오류로 보지 않는다. 같은 expected snapshot의 독립 Node 프로세스 6개 경쟁에서도 claim record 하나만 게시됐다. read-only 전체-store audit는 root layout, 존재하는 모든 journal chain과 receipt schema·filename·현재 claim 연결을 비식별 건수로 검사하고 예상 밖 entry·숨은 journal 변조·연결되지 않은 receipt를 거부한다. 기본 keyless 결과는 서명 미검증을 명시하고, 합성 HMAC key와 exact key ID를 호출 인자로 함께 제공한 rehearsal은 모든 현재 receipt canonical digest를 상수시간 비교해 위조를 거부한다. receipt 0개는 검증 증거로 세지 않으며 외부 anchor가 없어 삭제 entry 완전성은 계속 미검증이다. fixture context는 동일 identity의 `active→inactive`만 허용해 revision 없는 snapshot의 ABA 재일치를 막는다. fixture context와 local revocation은 authoritative live evidence가 아니고 journal hash는 외부 서명이 아니다. 추적 A4 bundle은 builder, durable store, A4 plan/bundle 집중 테스트, CI workflow와 LF 규칙을 포함한 17개 artifact를 Git-canonical byte hash로 결속하고 current source 재구성 검증으로 stale manifest를 차단한다. PostgreSQL CI readiness는 소켓 응답이 아니라 throwaway `verify` DB의 실제 query 성공으로 판정한다. 승인 발급 CLI·실제 production key·외부 anchor·production-grade durable approval/receipt state·row-versioned live membership CAS·production adapter는 추가하지 않아 review plan의 `readyForExecution:false`, `serverContractImplemented:false`, `databaseMutationExecuted:false`를 유지한다.

**A4 off-store inventory checkpoint 리허설(2026-08-26):** 로컬 durable store의 현재 approval ID·journal record 수·tail hash와 receipt approval/execution ID·digest는 메모리 내부 canonical inventory에만 포함하고, 외부 보관 후보 checkpoint에는 key ID·생성시각·비식별 건수·HMAC만 남긴다. 합성 checkpoint key로 봉인한 뒤 audit에 exact checkpoint/key ID와 canonical 검증 시각을 주입하면 approval 디렉터리 삭제와 이후 claim tail 추가를 거부하고, 원상복원된 exact inventory와 기본 10분 freshness가 모두 맞을 때만 `catalogCompletenessVerified:true`, `checkpointFreshnessVerified:true`가 된다. checkpoint 누락·부분 설정·digest 변조·검증 시각 누락·10분 초과·미래 checkpoint·0 이하 또는 24시간 초과 최대 나이는 fail-closed한다. store 파일·DB·RPC·credential은 변경하지 않는다. 이는 생성시점 snapshot의 로컬 계약이며 실제 외부 보관, production key custody/rotation, 독립 timestamp authority는 미구현이다.

**A4 분리 checkpoint anchor 리허설(2026-08-26):** rehearsal store와 repository 밖의 별도 빈 절대경로에 exact marker와 `checkpoints/`를 갖는 local anchor store를 추가했다. 생성시각 해시별 checkpoint record는 hard-link로 불변 게시하고 즉시 재조회해 canonical byte 의미가 일치해야 성공한다. 동일 checkpoint는 `existing`, 저장 뒤 응답 유실은 `recovered`, 동일 생성시각의 다른 checkpoint는 원본을 보존한 conflict로 수렴한다. 재시작 read-back, record SHA-256 변조, repository 내부 경로, malformed adapter와 source store/anchor 분리를 집중 회귀로 확인했다. Key material·approval/execution 식별값, production credential·DB·RPC는 사용하거나 저장하지 않는다. 같은 호스트의 local filesystem rehearsal이므로 운영 외부 durable anchor, anchor 자체 삭제 검출, 독립 timestamp와 production key custody는 여전히 미구현이다.

**A4 terminal claim 시각 순서 검증(2026-08-26):** 로컬 durable journal adapter가 terminal claim의 `finalizedAt` canonical 형식뿐 아니라 `finalizedAt >= claimedAt`을 append 전에 강제한다. claim보다 1ms 앞선 completed claim 부정 테스트는 기존에는 journal에 게시됐지만 이제 식별값을 노출하지 않는 validation 오류로 중단된다. production DB·RPC·credential에는 접근하지 않았고 rehearsal 전용 경계는 그대로다.

**A4 checkpoint 사건시각 검증(2026-08-26):** off-store checkpoint의 caller 주입 `createdAt`이 현재 journal state와 receipt에서 관찰되는 `claimedAt|finalizedAt|revokedAt|startedAt|completedAt` 최댓값보다 빠르면 seal 단계에서 거부한다. 이전 코드가 만든 유효 HMAC의 backdated checkpoint도 audit에서 inventory 대조 뒤 같은 temporal 오류로 차단한다. claim·revocation·receipt 각각의 1ms 역행과 exact-boundary 성공을 리허설했다. timestamp가 없는 초기 authorization/context 전이의 실제 발생시각과 독립 timestamp authority는 여전히 증명하지 않는다.

**A4 receipt-claim 사건 순서 검증(2026-08-26):** durable receipt append가 현재 journal claim의 approval/execution/plan/status뿐 아니라 `startedAt >= claimedAt`을 확인한다. 같은 local store에 receipt가 있으면 terminal finalize는 `finalizedAt >= completedAt`을 강제하고, 직접 파일로 복원한 모순 receipt도 read-only 전체-store audit에서 거부한다. 다른 claim snapshot으로 서명한 early receipt, receipt 완료보다 1ms 이른 finalize, terminal journal 뒤 직접 복원한 late receipt를 각각 차단했다. local receipt가 없는 terminal claim은 외부 receipt adapter 가능성을 위해 허용하며 production transaction 원자성은 별도 blocker다.

**A4 lifecycle finalization clock 검증(2026-08-26):** adapter-independent lifecycle core가 검증된 receipt의 `completedAt`과 terminal finalize에 사용할 trusted clock을 직접 비교한다. `claim/start 13:05 → receipt complete 13:07 → finalize 13:06:59.999` 비단조 clock은 receipt를 보존하고 claim을 active로 유지하며, 기존 receipt replay도 같은 오류를 반환한다. clock이 정확히 13:07에 도달하면 RPC 재호출 없이 `existing_receipt`로 종결한다. 이 검증은 새 실행·append 응답 유실·기존 receipt·명시적 reconciliation이 공유하는 경계에 적용된다.

**A4 RPC 직전 authorization 재검증(2026-08-26):** 새 claim을 획득하고 직후 receipt가 없음을 확인한 lifecycle도 execution adapter 호출 전에 authorization snapshot을 다시 읽는다. active membership·organization·actor·role·host와 exact in-flight claim이 유지될 때만 RPC를 호출한다. claim 응답 뒤 membership이 비활성화된 회귀 경로는 기존에는 RPC를 1회 호출한 다음 finalize에서 중단됐지만, 이제 execution·receipt 없이 claim만 열린 상태로 남는다. 이 adapter-level 재조회는 production RPC transaction 내부의 live Auth/membership 검증을 대체하지 않으며 DB·RPC·credential은 변경하지 않았다.

**Phase A 활성화 결정 패킷(2026-08-25):** 상위 아키텍처 플랜 §5의 미결정 6건을 현재 A2~A4 구현과 대조해 `PHASE_A_ACTIVATION_DECISION_PACKET.md`로 정리했다. 권고안은 현 비공공 managed 배포와 각 물리 deployment 내부의 row-level 논리 테넌시, 관리자·운영자·HQ 우선 Auth와 facilitator 후속 전환, staff traffic 전 HQ membership 전환, 설계 마법사와 authorized execution 분리, 비공공 managed 유지와 공공 CSAP 적격성 확인 후 topology 별도 결정, 갈등관리 수행사 화이트라벨을 우선한 최소 범위의 Phase 2+ 지속이다. 각 결정에 대안·위험·재검토·완료/중단 기준과 역할표·근거 commit을 붙였다. 제품 결정, count-only 진단, Auth·membership, P1C/A4 휴면 migration, mapping·backfill, A2 GRANT, A4 RPC 권한, staff traffic을 독립 승인으로 나눴으며 `not_ready`는 scoped remediation 뒤 preflight 재실행으로만 해소한다. `2026-08-25-phase-a-condition-audit.md`는 D3 방향을 기존 정본으로 확인하고 D4의 2인 승인 의무가 현재 없음을 기록한다. 진행자 전환 시점, 공공 데이터·CSAP 등급·provider 적격성·tenancy topology, named pilot·owner는 조건부다. 독립 검수는 Gate 경계가 production 권한으로 넓어지지 않음을 확인했고, 외부조건은 미해결 상태로 명시했다. 이 문서 작성으로 production 변경 권한은 부여되지 않는다.

**A4 교차-plan replay 차단(2026-08-26):** A4 ledger의 기존 operation은 request hash·type뿐 아니라 전체 plan checksum까지 exact 일치할 때만 replay한다. 같은 operation payload를 다른 source/plan에서 재사용하면 실행 성공 뒤 reconciliation checksum이 충돌하는 상태가 생기므로 첫 operation에서 `design_operation_conflict`로 중단하며, PostgreSQL 16 격리 리허설은 ledger와 assembly/session/topic/team 건수가 바뀌지 않음을 확인했다. 독립 parent-conflict fixture도 새 assembly 안의 중복 session ordinal로 분리해 두 계약을 각각 검증한다. A4 bundle 집중 8건, automation 전체 26개 파일·402건, 루트 전체 64개 파일·1,060건이 통과했다. production migration·DB·GRANT는 적용하지 않았다.

**A4 join-code CSPRNG 강화(2026-08-26):** 여러 운영 RPC에서 capability로 쓰이는 6자리 team join code 생성기를 PostgreSQL `random()`에서 `extensions.gen_random_bytes(4)` 기반 CSPRNG로 교체했다. 32-bit 공간을 6자리로 축소할 때 균등 상한 밖 값을 폐기하는 rejection sampling을 적용하고, post-apply verifier가 함수 volatility·search path·CSPRNG·편향 제거·비암호학적 random 부재를 확인한다. PostgreSQL 16 semantic rehearsal은 정상 6자리 생성, 강제 충돌 소진 뒤 전체 transaction rollback과 secure 함수 복원을 확인했다. production migration·DB·GRANT는 적용하지 않았다.

**A4 operation identity 유일성 검증(2026-08-26):** mutation RPC가 plan의 operation ID와 canonical resource ref를 각각 전수 중복 검사한 뒤에만 Auth·ledger lookup과 resource mutation으로 진행한다. 이전에는 같은 session operation을 두 번 넣고 summary를 맞추면 한 resource를 `applied`·`replayed`로 중복 응답하면서 ledger는 한 행만 남아 reconciliation이 거부될 수 있었다. PostgreSQL 16 격리 리허설은 중복 plan을 `design_plan_invalid`로 차단하고 ledger·assembly 무변경을 확인했으며 post-apply verifier도 두 uniqueness 조건을 compiled function에서 검사한다. production migration·DB·GRANT는 적용하지 않았다.

**A4 JSON scalar 타입 엄격화(2026-08-26):** mutation RPC와 read-only reconciliation RPC가 exact field 이름뿐 아니라 boolean·number·string JSON 타입을 각각 검사한다. 이전의 `->>` 텍스트 비교는 `readyForExecution` 같은 실행 플래그나 operation count를 JSON 문자열로 바꾸고 checksum을 다시 계산한 입력도 같은 값으로 받아들였지만, 이제 lookup·mutation 전에 안정 오류 코드로 거부한다. PostgreSQL 16 격리 리허설과 compiled-function post-apply verifier가 두 경로의 타입 위조 차단을 확인했다. production migration·DB·GRANT는 적용하지 않았다.

**A4 canonical plan 의미 계약(2026-08-26):** mutation RPC가 client verifier와 같은 operation grammar를 재검증한다. assembly 뒤 연속 session, 각 session의 연속 topic→연속 team 순서, 실제 달력 날짜와 날짜 비감소, trim된 text, session 내 topic prompt 유일성, `${ordinal}조` team 이름, topic·team 최소 1건, session 누적 정원 100,000명, 전체 생성 항목 10,000건을 강제한다. PostgreSQL 16 격리 리허설은 invalid calendar, 날짜 역행, ordinal gap, child 순서 역전, 중복 prompt, 잘못된 team 이름, 정원 초과와 불완전 session을 안정 오류로 거부하고 전체 transaction rollback을 확인했다. parent-conflict·join-code exhaustion·late summary rollback fixture도 canonical plan으로 재구성해 기존 failure mode를 계속 증명한다. production migration·DB·GRANT는 적용하지 않았다.

**A4 비활성 team join-code 재노출 차단(2026-08-26):** mutation exact replay와 read-only reconciliation이 ledger resource의 기관 일치뿐 아니라 team `status='active'`를 다시 확인한다. 정상 생성 뒤 disabled로 전환한 team은 각각 `design_resource_conflict`, `design_reconciliation_conflict`로 닫혀 join code를 응답하지 않고 ledger·resource도 바꾸지 않는다. PostgreSQL 16 격리 리허설과 compiled-function post-apply verifier가 두 경로를 확인했다. production migration·DB·GRANT는 적용하지 않았다.

**A4 권한 판정 우선 실행(2026-08-26):** mutation/status RPC는 null·root object 여부만 확인한 직후 현재 Auth 사용자, 선택 기관, 활성 membership·org, `org_admin|hq` 역할을 먼저 검증한다. 권한 없는 `operator`가 malformed plan/query를 보내도 checksum·source digest 또는 최대 10,025개 operation 배열 검증 전에 `design_role_forbidden`으로 중단한다. PostgreSQL 16 격리 리허설과 compiled-function 순서 verifier가 이를 확인했다. production migration·DB·GRANT는 적용하지 않았다.

**A4 동시 exact plan 멱등 수렴(2026-08-26):** mutation RPC는 checksum·source 검증 뒤 첫 ledger/resource lookup 전에 기관 ID 기반 transaction advisory lock을 획득한다. test-only 지연 trigger와 두 독립 PostgreSQL 연결로 같은 신규 plan을 경합시킨 결과 하나는 `applied`, 다른 하나는 lock 대기 뒤 `replayed`가 되고 resource 한 세트와 ledger 4건만 남았다. post-apply verifier는 lock key와 source 검증 이후·operation loop 이전 순서를 compiled function에서 확인한다. production migration·DB·GRANT는 적용하지 않았다.

**A4 authorization row-lock 경계(2026-08-26):** mutation RPC가 권한 판정에 사용한 active membership과 organization 행을 `FOR SHARE`로 transaction 종료까지 유지한다. PostgreSQL 16의 별도 연결에서 plan INSERT를 1초 지연한 동안 membership 역할 변경과 organization `suspended` 전환을 각각 시도하자 둘 다 250ms lock timeout으로 밀렸고, 두 plan은 정상 적용된 뒤 권한 행이 active 상태로 남았다. post-apply verifier도 compiled function의 두-row lock을 요구한다. 이는 휴면 migration 초안 리허설이며 production DB·membership·org·GRANT는 변경하지 않았다.

**A4 비-draft 설계 resource 채택 차단(2026-08-26):** mutation RPC의 신규 채택과 exact replay는 assembly·session·discussion topic의 payload·부모·기관뿐 아니라 서버 생성 상태 `draft`도 대조한다. 정상 생성 뒤 assembly/session을 `active`, topic을 `open`으로 각각 바꾼 PostgreSQL 16 부정 rehearsal은 모두 `design_resource_conflict`로 중단됐고, post-apply verifier는 compiled function에서 세 상태 조건을 확인한다. 이미 활성화·종료된 자원을 새 설계 성공으로 기록하지 않으며 production migration·DB·GRANT는 적용하지 않았다.

**A4 제약 정본 검증 강화(2026-08-26):** post-apply verifier가 session/team/ledger의 8개 제약을 schema 전체의 이름 count가 아니라 정확한 대상 table·`check|unique|primary key` 종류·PostgreSQL canonical definition으로 대조한다. team의 양수 capacity 제약을 shadow table의 동명 제약으로 바꾼 경로와 정확한 team에 `capacity >= 0`으로 완화한 경로를 각각 거부했고, 원래 `capacity > 0` 복구 뒤 verifier와 전체 semantic rehearsal이 통과했다. production migration·DB·GRANT는 적용하지 않았다.

**A4 column·FK 정본 검증 강화(2026-08-26):** post-apply verifier가 session/team/ledger의 필수 15개 column을 정확한 PostgreSQL type·nullable·default로 대조하고, session의 assembly/org 및 ledger의 org FK를 canonical 참조 정의로 확인한다. `team.ordinal`을 `bigint`로 바꾼 경로와 `session→assembly` FK를 제거한 경로를 각각 거부했으며, integer/FK 복구 뒤 verifier와 전체 semantic rehearsal이 통과했다. production migration·DB·GRANT는 적용하지 않았다.

**A4 migration 초안 현재상태 감사(2026-08-26):** 사용자 승인 범위의 additive migration·populated rollback guard·적용 전 count-only readiness·legacy/mapping fixture·post-apply schema/권한 verifier·PostgreSQL 의미 리허설·approval/receipt/reconciliation core·저장소 밖 durable rehearsal·current-source bundle을 현재 commit에서 요구사항별로 재대조했다. A4 집중 61건과 artifact 17개 bundle checksum `b7c971e74d4335a1173ccf11b6cd3a11e54ef624bde1856aa34f6c31fc747b9e`, GitHub Actions run `32910614144` 성공을 확인했다. 승인된 초안 범위는 충족됐으며 bundle의 `productionApplyApproved:false`, `databaseMutationExecuted:false`는 유지된다. production migration·mapping/backfill·Auth/membership·RPC 권한·executor·traffic은 별도 Gate A/B-A4 승인 전까지 실행하지 않는다.

**A7 원문·검수 입력 repository 격리(2026-08-26):** result source CLI가 raw `issue-items`를 모든 모드에서, reviewer identity와 공개 결정을 담은 `reviews`를 publication 모드에서 repository 밖의 기존 일반 파일로만 받도록 강화했다. 경로를 읽기 전에 실제 경로로 해석해 저장소 내부 직접 경로와 내부를 가리키는 symlink/junction, 누락·디렉터리 입력을 식별값이나 원문 노출 없이 거부한다. publication plan의 repository 밖 no-overwrite·사용자 전용 파일 경계와 원문 비노출 stdout은 그대로 유지된다. 집중 24건과 automation 전체 27개 파일·436건이 통과했으며 production payload·DB·RPC·migration·실제 시민 원문·검수 입력에는 접근하거나 변경하지 않았다.

**A7 원문 공개 계약 identity 결속(2026-08-26):** provenance plan과 replace-all publication plan을 schema v2로 올리고, UI·DOCX·publish preflight가 공유하는 `source-reference-contract.json`의 schema v1과 canonical SHA-256을 승인 artifact에 결속했다. 로더는 contract root·record exact field, 공개 9필드 순서, `core|extra`, `reviewed`, `org_admin|hq`, 양의 길이·순번 경계를 시작 시 검증한다. verifier는 identity가 없는 legacy plan과 checksum을 다시 계산한 contract identity 위조도 현재 입력 재생성 대조에서 거부한다. production payload·DB·RPC·migration·실제 원문·검수 입력에는 접근하거나 변경하지 않았다.

**A7 이행 상태 계약 identity 결속(2026-08-26):** 이행추적 publish plan을 schema v2로 올리고 웹·표·DOCX·preflight가 공유하는 `implementation-status-contract.json` schema v2와 canonical SHA-256을 artifact에 결속했다. plan self-checksum을 다시 계산해도 contract digest를 바꾼 artifact는 현재 result·responses 재생성 대조에서 거부하며, identity가 없는 legacy schema v1도 명시적으로 거부한다. 상태·record loader의 기존 exact-field·fallback·색상·HTTPS·timestamp 검증은 유지된다. production payload·DB·RPC·migration·실제 기관 응답에는 접근하거나 변경하지 않았다.

**A3 접근 계획 계약 identity 결속(2026-08-26):** 기관 접근 provisioning plan을 schema v2로 올리고 브라우저·CLI가 공유하는 `access-plan-contract.json` schema v1과 canonical SHA-256을 `accessPlanContract`에 결속했다. verifier는 현재 contract와 source access plan에서 전체 artifact를 다시 생성하므로 plan checksum을 다시 계산한 contract digest 위조와 identity가 없는 legacy provisioning plan schema v1을 모두 거부한다. 브라우저가 내보내는 source access plan schema v1은 유지하며 과거 provisioning plan만 현재 source로 재생성해야 한다. production DB·Auth·초대·메일·membership·GRANT에는 접근하거나 변경하지 않았다.

**A3 append-only receipt recovery 계약(2026-08-26):** executor adapter가 append-only persistence capability와 `runId`별 read/append를 함께 제공하도록 강화했다. Core는 operation lookup 전에 저장된 receipt의 HMAC·plan checksum·approval ID·run ID를 검증해 exact replay면 mutation 없이 반환한다. 봉인된 terminal receipt는 원 approval 서명과 발급시각을 다시 검증하므로 실행 창 만료 뒤 재시작에서도 mutation 없이 복구하지만, 저장본이 없으면 현재 trusted clock 기준 approval freshness를 통과해야 신규 실행한다. 신규 실행도 append 응답을 신뢰하지 않고 authoritative read-back과 생성 receipt가 canonical하게 같을 때만 성공한다. append가 저장 뒤 응답을 잃은 경로는 같은 receipt를 복구하고 같은 run 재실행은 lookup·apply·append를 반복하지 않으며, 다른 run receipt와 누락·위조·충돌은 mutation 전에 거부한다. Adapter 객체는 검증 경계에서 복제해 사후 변조를 격리한다. 이는 in-memory adapter contract 회귀이며 production invitation ledger·메일 provider·외부 append-only 저장소·Auth adapter는 구현하거나 연결하지 않았다. 조직 접근의 4개 role은 staff membership이고 시민 participant token 역할은 별도 경계임을 문서에 명시했다.

**A3 로컬 durable receipt store(2026-08-26):** `platform-access-provisioning-durable-store.mjs`가 repository 밖의 기존 빈 절대경로에 local-only marker와 receipt directory를 만들고, HMAC·plan checksum·key ID·run ID가 일치하는 비식별 receipt를 임시파일→hard-link로 원자 게시한다. Adapter를 새로 만든 process restart 회귀에서 append 저장 뒤 응답 유실을 복구하고 승인 창 만료 뒤 같은 run을 lookup·apply 없이 반환했다. 같은 run의 다른 유효 receipt는 `conflict`로 원본을 보존하며 relative·repository 내부·nonempty 초기 경로와 receipt directory junction 탈출을 거부한다. 이 store는 Supabase·Auth·메일·credential을 읽거나 변경하지 않고 invitation operation ledger·authoritative authorization·외부 anchor·production key custody를 제공하지 않으므로 production adapter가 아니다.

**A4 key rotation registry 정책·lifecycle 계약(2026-08-26):** `platform-design-provisioning-key-registry.mjs`가 approval 발급·검증, receipt 검증, injected 실행·reconciliation lifecycle을 registry entry의 exact key ID·revision과 `active|verify_only|retired`에 결속한다. 신규 발급은 active snapshot 뒤 `authorizeIssuance()`가 같은 revision을 CAS 승인하고 registry `authorizedAt`이 approval 창 안일 때만 가능해 회전·만료 경쟁을 fail-closed한다. 과거 approval·receipt는 발급 중단 전 생성되고 검증 종료 전인 active/verify-only key만 허용하며 retired·cutoff 위반·malformed envelope/entry는 key를 저수준 HMAC 함수에 전달하기 전에 거부한다. Registry-backed lifecycle은 key를 한 번만 읽고 첫 trusted clock을 정책과 claim/reconciliation에 동일하게 적용하며 직접 key 우회를 거부한다. 완료·finalize 시각도 같은 검증 종료시각을 다시 검사해 verify-only cutoff 뒤에는 receipt를 만들지 않고 claim을 열린 상태로 둔다. Verify-only 회귀는 승인 만료 뒤 active claim을 명시적 reconciliation으로 종결하되 신규 RPC 자동 재실행은 하지 않는다. Registry 응답과 오류에서 key material을 반환하거나 노출하지 않고 Supabase·credential·production endpoint를 사용하지 않는다. 이는 합성 adapter 정책 회귀이며 실제 KMS/HSM·secret manager, key 생성·보관·회전 작업, production adapter 연결과 production key custody는 여전히 별도 blocker다.

**A4 revision-bound live authorization 계약(2026-08-26):** 기존 live context boolean 재검증에 SHA-256 revision과 `revisionedLiveAuthorization:true` capability를 추가했다. Revisioned claim은 현재 context revision을 `authorizationRevision`에 결속하고 execution 직전, receipt 봉인 전, reconciliation과 finalize에서 같은 값을 요구한다. In-memory provider는 context 전이마다 generation-bound revision을 바꾸고 stale expected revision을 CAS conflict로 반환하므로 membership 또는 organization이 `active→inactive→active`로 같은 값에 돌아와도 RPC 전·RPC 후·reconciliation에서 열린 claim으로 중단한다. Legacy in-memory adapter는 순수 core 회귀 호환으로 남지만 revisioned lifecycle wrapper는 side effect 전에 거부한다. Key registry 결합 wrapper도 revision capability를 key read보다 먼저 확인한다. 집중 테스트는 정상 실행·terminal claim, stale snapshot, malformed/legacy adapter, pre-execution ABA, execution 중 ABA, reconciliation ABA와 key material 비노출을 다룬다. 이는 test double 계약이며 실제 Supabase row-version transaction, authoritative revocation, production executor/status adapter·DB·Auth·GRANT·traffic은 구현하거나 변경하지 않았다.

**A4 RPC authorization fence 왕복 계약(2026-08-26):** production-bound execution/reconciliation wrapper가 revisioned authorization뿐 아니라 `revisionFencedExecution:true` 또는 `revisionFencedReconciliation:true` capability를 요구한다. Core는 RPC/ledger lookup 직전 검증한 approval·execution ID와 revision을 exact fence로 전달하고 adapter 응답이 같은 revision을 되돌릴 때만 결과를 처리한다. Unfenced adapter는 authorization·receipt·key registry read 전에 거부하며 다른 revision 응답은 receipt 없이 claim을 active로 보존한다. Legacy 저수준 core는 기존 회귀와 호환되지만 production-bound wrapper로 승격되지 않는다. 실제 Supabase RPC transaction·status lookup이 fence를 소비하는 adapter, DB·Auth·GRANT·traffic은 구현하거나 변경하지 않았다.

**A4 revision-bound durable receipt 계약(2026-08-26):** fence 왕복을 통과한 실행·reconciliation 결과만 receipt의 optional `authorizationRevision`에 같은 SHA-256을 넣고 HMAC으로 결속한다. Production-bound 기존 receipt 복구는 현재 live snapshot/claim revision을 먼저 읽어 receipt와 exact 일치할 때만 finalize한다. 필드가 없는 legacy receipt와 다른 revision으로 유효하게 서명된 receipt는 실행 adapter를 재호출하지 않고 claim을 열린 상태로 둔 채 거부한다. 일반 receipt verifier와 저수준 lifecycle의 legacy 호환은 유지하되 production evidence로 승격하지 않는다. Durable store와 key registry envelope는 optional revision을 exact schema로 검증하고 claim linkage가 다르면 append/audit를 거부한다. 실제 production durable store·RPC/status adapter·DB·Auth·GRANT·traffic은 구현하거나 변경하지 않았다.

**A4 휴면 SQL authorization revision fence(2026-08-26):** JS production-bound lifecycle이 요구하는 exact authorization fence를 migration 초안의 mutation 3-인자·reconciliation 2-인자 overload도 받도록 연결했다. 휴면 helper는 현재 Auth 사용자·기관과 active `org_admin|hq` membership 집합, org/membership PostgreSQL row version을 정렬해 SHA-256 revision을 만들며 두 overload는 권한 행을 transaction 종료까지 `FOR SHARE`로 잠근다. malformed·stale fence는 plan/ledger 처리 전에 안정 오류로 거부하고, reconciliation은 fence와 조회 조건의 approval/execution ID도 exact 일치시킨 뒤 정상·pending·completed 응답에 같은 revision을 echo한다. PostgreSQL 16 격리 리허설은 별도 transaction의 `org_admin→operator→org_admin` ABA 뒤 과거 mutation/reconciliation fence 재사용 차단, 정상 echo, 기존 idempotency·rollback을 확인했다. helper와 overload는 public/anon/authenticated/service_role 모두 revoke 상태다. row version은 짧은 live fence이며 durable 감사 ID가 아니고 production wiring·live authorization/approval store·credential·GRANT·DB 적용은 여전히 없다.

**A4 비활성 Supabase fenced RPC adapter 초안(2026-08-26):** `createSupabaseDesignProvisioningRpcAdapters()`가 외부에서 이미 인증된 Supabase client를 주입받아 `climate_vote.design_provision`과 `design_provisioning_status`의 fenced overload만 호출한다. Mutation은 contract 상한 1,000,000 source bytes를 PostgreSQL hex `bytea`로 직렬화하고 두 경로 모두 exact fence·query identity, 20초 abort signal, 명시적 무재시도, 응답 authorization revision 일치를 강제한다. Sparse·비JSON·과대 payload는 schema client 접근 전에 거부하고 원시 Supabase 오류·throw 값은 안정 오류로 치환한다. SQL이 요구하는 approval/execution UUID v4는 production-bound lifecycle도 authorization/receipt read와 claim 전에 거부해 열린 claim을 만들지 않는다. Method-chain unit fixture에 더해 실제 Supabase JS client와 custom fetch를 연결한 격리 통합 테스트가 `/rest/v1/rpc/design_provision|design_provisioning_status`, `Content-Profile: climate_vote`, exact JSON body·hex `bytea`, abort signal과 HTTP 503 단일 호출·오류 비노출을 확인했다. Factory는 환경변수·URL·key를 읽거나 client를 만들지 않으며 생성만으로 RPC를 호출하지 않는다. 통합 fixture도 외부 네트워크·DB·실제 credential을 사용하지 않았다. 실제 Auth session·live authorization CAS·approval/receipt/key store·CLI·GRANT·DB 적용·traffic 연결은 여전히 없다.

**A5 로그인 main 랜드마크 보강(2026-08-26):** 공개 `/platform/`의 로그인 전 접근성 트리에서 이름 있는 form·heading·입력·버튼은 확인됐지만 `main` 랜드마크가 없었다. 로그인·초기 로딩을 감싸는 `Centered` container를 의미 없는 `div`에서 `main`으로 바꿔 form 역할과 건너뛰기 대상은 그대로 보존하면서 스크린리더 랜드마크 탐색 경로를 추가했다. 360×800 CSS viewport에서 문서 폭 360px, 로그인 form 폭 312px, 입력·버튼 높이 52px와 가로 overflow 없음도 직접 확인했다. 이는 브라우저 accessibility tree와 반응형 구조 검증이며 실제 NVDA·VoiceOver·TalkBack 수동 평가는 아니므로 `platform-accessibility-manual-evaluation.json`의 `needs_review`와 `not_run` 상태는 유지한다.

**A5 로그인 장식 로고 접근성 트리 제외(2026-08-26):** 공개 `/platform/` 로그인 접근성 트리에서 시각 장식용 단일 문자 `P`가 의미 없는 generic text로 노출되는 것을 확인했다. 로고 container만 `aria-hidden="true"`로 제외하고 브랜드명·로그인 제목·form·레이블·상태 메시지는 그대로 보존했다. 변경 전 실패하는 component 회귀를 고정한 뒤 집중 49건, 루트 전체 65개 파일·1,081건, automation 전체 28개 파일·496건과 Astro 337개 파일 오류·경고 0건(기존 hint 49건)이 통과했다. 이는 장식 텍스트의 프로그램 방식 제외를 검증한 것이며 실제 NVDA·VoiceOver·TalkBack 수동 평가는 아니므로 수동 평가 상태를 변경하지 않았다.

**A5 로그인 실패 복구 접근성 계약(2026-08-26):** 로그인 제출 직전 form 내부 활성 요소를 기억하고 인증 실패로 controls가 다시 활성화된 뒤 그 요소로 포커스를 복귀시킨다. 오류와 안내 live region은 atomic announcement를 명시하고, 오류 발생 시 이메일·비밀번호 입력 모두 같은 alert를 설명으로 참조하며 `aria-invalid=true`를 유지한다. 격리된 `.invalid` Auth fixture의 실제 Chromium 검증은 비밀번호 입력에서 제출한 뒤 같은 입력으로 포커스 복귀, 요청 중 두 입력 잠금, 로그인 요청 1회, 중복 요청 차단, 실패 뒤 재시도 활성화, 두 필드 오류 연결, atomic alert, 비밀번호 마스킹 유지, browser page error·예상 밖 요청 0건을 확인했다. 루트 전체 65개 파일·1,081건과 automation 전체 28개 파일·496건은 통과했고, 기본 5초 automation 제한에서 환경 지연으로 CLI fixture 4건이 timeout된 별도 실행은 30초 제한의 동일 suite로 기능 통과를 재확인했다. 실제 credential·외부 Auth·DB는 사용하지 않았고 NVDA·VoiceOver·TalkBack 수동 평가 상태도 변경하지 않았다.

**A4 reconciliation source evidence 결속(2026-08-26):** 휴면 operation ledger에 원 청사진 SHA-256과 byte 길이를 비식별 metadata로 추가하고, mutation exact replay와 read-only reconciliation이 executed plan checksum·operation type뿐 아니라 두 source evidence도 exact 대조하도록 강화했다. 원본 bytes는 저장하지 않는다. 변조된 source SHA와 100→99 byte 길이가 기존 SQL에서는 completed reconciliation으로 통과하는 실패 회귀를 먼저 확인했고, 수정 뒤 둘 다 `design_reconciliation_conflict`로 차단했다. Post-apply verifier는 17개 필수 column과 source SHA/길이 제약의 canonical definition을 확인하며, 완화된 길이 제약을 실제로 거부했다. PostgreSQL 16 격리 semantic/post-apply, A4 집중 96건, 루트 전체 65개 파일·1,081건, automation 전체 28개 파일·496건과 Astro 337개 파일 오류·경고 0건(기존 hint 49건)이 통과했다. Bundle checksum은 `94705effd7e0910943fcf1123b57695148cc8013dac3a460991271ec21143bd6`으로 갱신했다. production migration·DB·GRANT·credential·traffic은 변경하지 않았다.

**A4 ledger execution identity 결속(2026-08-26):** 휴면 operation ledger가 approval ID·execution ID·승인된 plan checksum·authorization revision을 all-or-none으로 저장하고, fenced mutation만 실행 결과를 이 identity에 결속하도록 강화했다. 다른 execution 또는 승인 checksum replay, identity가 없는 저수준 ledger reconciliation은 모두 안정 conflict로 차단하며 status query의 승인 checksum과 live authorization revision도 exact 일치를 요구한다. 기존 SQL이 authorization fence의 승인 checksum을 받지 못해 실패하는 회귀를 먼저 고정한 뒤 PostgreSQL 16에서 plan 4개 operation이 단일 approval·execution·checksum·revision에 완전 결속된 `4|1|1|1|1|t`를 확인했다. 완화된 execution binding 제약은 post-apply verifier가 거부했고 rollback 뒤 object 부재도 확인했다. A4 집중 96건, 루트 전체 65개 파일·1,081건, automation 전체 28개 파일·496건과 Astro 337개 파일 오류·경고 0건(기존 hint 49건)이 통과했다. Bundle checksum은 `810b7d2ae988f254811a3e8dc2b29096213a43726ccfd601e0c6e1d3089be654`이며 production migration·DB·GRANT·credential·traffic은 변경하지 않았다.

**A5 로그인 실패 자동 접근성 감사 편입(2026-08-26):** 정적 로그인 화면만 검사하던 감사기에 읽기 전용 인증 거부 fixture와 post-navigation interaction hook을 추가해 실제 실패 상태를 axe 검사 대상으로 편입했다. 합성 `.invalid` 계정으로 제출한 뒤 alert 표시, 비밀번호 입력 포커스 복귀, 두 필드의 alert 설명 연결과 `aria-invalid`, atomic alert, 비밀번호 마스킹, controls 재활성화를 확인한다. Supabase origin 요청은 전부 브라우저 내부 합성 응답으로 차단해 외부 Auth·credential·DB mutation을 사용하지 않는다. 로컬 current-revision 프리뷰의 Chromium 감사는 7개 경로×2개 viewport, 총 14개 케이스가 모두 통과했고 violation·incomplete는 각각 0건이었다. 자동화는 실제 NVDA·VoiceOver·TalkBack 수동 평가를 대체하지 않으므로 전체 상태는 `needs_review`를 유지한다.

**A5 로그인 키보드 초점 순서 자동 감사(2026-08-26):** 공개 `/platform/` 로그인에서 이메일→비밀번호→제출→접근성 안내 링크의 실제 `Tab` 순서와 역방향 `Shift+Tab`, 양 끝 경계 이탈을 desktop·mobile Chromium으로 검증한다. 순서 불일치와 마지막↔처음 순환 함정을 실패시키고, 활성 요소 증거에는 입력값·URL query를 남기지 않는다. 공개 revision `86a86fbcdf906ca3455e73c08dd3ca2403d1b574`의 7개 경로×2개 profile 총 14개 케이스는 모두 통과했고 violation·incomplete는 0건이었다. KWCAG 6.1.1·6.1.2에 자동 증거를 추가했지만 실제 NVDA·VoiceOver·TalkBack 및 모바일 보조기기 평가는 남아 있어 전체 상태와 두 기준은 `needs_review`를 유지한다. Auth·credential·production DB·migration·GRANT·데이터 mutation은 사용하지 않았다.

**A5 로그인 키보드 초점 모양 자동 감사(2026-08-26):** 실제 Chromium `Tab` 순회 중 이메일·비밀번호·제출·접근성 안내 링크 각각의 computed 외곽선 스타일·너비·offset·색과 가장 가까운 불투명 배경을 기록하고, 2px 이상 외곽선과 3:1 이상 대비를 모두 요구한다. 숨긴 외곽선과 흰 배경 대비 1.82:1의 저대비 외곽선이 순서 검증과 별개로 실패하는 회귀를 고정했다. 공개 revision `f30f33fc4ea15e5f7fbbd97b4822361b7658e71d`의 desktop·mobile 네 컨트롤은 모두 2px solid, 3px offset, `rgb(10, 107, 78)` 대 흰 배경 6.51:1로 통과했고 전체 14개 감사 케이스의 violation·incomplete는 0건이었다. 집중 34건, 루트 전체 65개 파일·1,081건, Linux automation 28개 파일·505건, Astro check 오류·경고 0건과 Node 20 정적 빌드 7,914페이지가 통과했다. KWCAG 6.1.2에 자동 근거를 추가했지만 실제 NVDA·VoiceOver·TalkBack 및 모바일 보조기기 평가는 남아 있어 `needs_review`를 유지한다. 실제 credential·Auth 제출·production DB·migration·GRANT·데이터 mutation은 사용하지 않았다.

**A4 내부 helper 휴면 권한 검증(2026-08-26):** migration 초안은 canonical JSON·SHA-256·join-code helper의 EXECUTE를 회수하지만 post-apply verifier가 이 권한 재노출을 검사하지 않던 공백을 보강했다. public·anon·authenticated·service_role 네 역할과 세 helper의 12개 effective privilege 조합을 전수 대조하며 하나라도 열리면 `dormant privilege contract is unsafe`로 거부한다. PostgreSQL 16 격리 리허설에서 각 helper를 독립적으로 `authenticated`에 부여했을 때 모두 거부되고 권한 회수 뒤 다시 통과했다. A4 집중 96건, 루트 전체 65개 파일·1,081건, automation 전체 28개 파일·505건과 Astro check 337개 파일 오류·경고 0건이 통과했다. Bundle artifact 20개 checksum은 `0be367703ff7496485f77a8d7a6ae7f22be178c81f17be4a3970f035e1cc18e2`이며 production migration·DB·GRANT·credential·traffic은 변경하지 않았다.

**A4 ledger table 휴면 권한 전수 검증(2026-08-26):** migration이 ledger table에서 모든 권한을 회수하지만 post-apply verifier는 `SELECT`만 검사하던 공백을 보강했다. public·anon·authenticated·service_role 네 역할과 `SELECT|INSERT|UPDATE|DELETE|TRUNCATE|REFERENCES|TRIGGER` 일곱 권한의 28개 effective 조합을 전수 대조하며 하나라도 열리면 `dormant privilege contract is unsafe`로 거부한다. PostgreSQL 16 격리 리허설에서 28개 조합을 하나씩 부여했을 때 모두 거부되고 매번 권한 회수 뒤 다시 통과했다. Bundle artifact 20개 checksum은 `069c743b49990def2bd6332f1c8e109e609a0977d390e7a91a4953b1c502ca01`이며 production migration·DB·GRANT·credential·traffic은 변경하지 않았다.

**A4 ledger column 휴면 권한 전수 검증(2026-08-26):** PostgreSQL column-level ACL은 table-level 권한과 별도로 존재해 기존 table matrix만으로는 단일 column의 `SELECT|INSERT|UPDATE|REFERENCES` 노출을 잡지 못했다. Post-apply verifier가 public·anon·authenticated·service_role 네 역할과 네 column 권한의 16개 effective 조합을 `has_any_column_privilege`로 별도 대조한다. Table-level 권한은 false인 상태에서 `operation_id` column에 16개 조합을 하나씩 부여한 PostgreSQL 16 리허설은 모두 거부됐고 매번 권한 회수 뒤 다시 통과했다. Bundle artifact 20개 checksum은 `28bd43eaa87c81d87fd6af7e3142cefc24408844234d91321061a678412afc94`이며 production migration·DB·GRANT·credential·traffic은 변경하지 않았다.

**A4 checksum helper 구현 정본 검증(2026-08-26):** 체크섬 신뢰 경계인 canonical JSON·SHA-256 helper는 기존 post-apply verifier에서 존재와 권한만 확인해, SHA helper를 상수 반환으로 바꿔도 통과하는 공백이 있었다. 두 helper의 정확한 언어·`IMMUTABLE`·`STRICT`·invoker security·단일 고정 `search_path`, canonical object key/array 순서와 SHA-256 known-answer를 함께 검증하도록 보강했다. PostgreSQL 16에서 원래 metadata를 유지하고 canonical helper와 SHA helper의 body만 각각 상수 반환으로 바꾼 두 transaction은 모두 `checksum helper contract is unsafe`로 거부됐고 rollback 뒤 정상 verifier가 다시 통과했다. Bundle artifact 20개 checksum은 `15ea7ea0625a75fc9033bcd7c6d4b659cbb3e8c8180c2a0c1f6b1cba4b952142`이며 production migration·DB·GRANT·credential·traffic은 변경하지 않았다.

**A4 객체 owner 신뢰 결속(2026-08-26):** 휴면 함수의 EXECUTE ACL을 네 runtime role에서 회수해도 별도 LOGIN role이 SECURITY DEFINER RPC owner가 되면 implicit EXECUTE와 함수 교체 권한을 갖는데, 기존 post-apply verifier는 이 drift를 허용했다. Ledger와 canonical/SHA/join-code/authorization helper, mutation·status 각 두 overload까지 8개 함수가 동일 owner인지 확인하고, 그 owner가 `anon|authenticated|authenticator|service_role`이 아니면서 superuser 또는 `BYPASSRLS`인지 요구한다. PostgreSQL 16에서 단일 fenced mutation owner만 비특권 LOGIN role로 바꾼 경우와 ledger·8개 함수를 모두 그 role로 바꿔 owner 일치만 맞춘 경우를 각각 `owner contract is unsafe`로 거부하고 rollback 뒤 정상 검증을 확인했다. Bundle artifact 20개 checksum은 `f0f557bcf2d942afdf779f086a3918677423c19356cb3d5f555bb5cd7fda37cc`이며 production migration·DB·GRANT·credential·traffic은 변경하지 않았다.

**A4 authenticator 휴면 ACL 전수 검증(2026-08-26):** owner verifier가 runtime role로 분류하는 `authenticator`가 함수·ledger table·ledger column ACL matrix에는 빠져 있어, fenced mutation EXECUTE와 ledger/column SELECT를 명시 부여해도 기존 verifier가 통과하는 공백을 재현했다. Migration과 rollback은 public·anon·authenticated·authenticator·service_role 모두에서 ledger와 A4 함수 권한을 회수하고, post-apply verifier는 5개 역할×8개 함수의 40개 EXECUTE, 5개 역할×7개 table privilege의 35개, 5개 역할×4개 column privilege의 20개 effective 조합을 전수 대조한다. PostgreSQL 16 격리 리허설은 migration이 사전 `authenticator` grant를 회수하는지와 세 독립 재노출을 모두 `dormant privilege contract is unsafe`로 거부한 뒤 정상 복구되는지 확인했다. Bundle artifact 20개 checksum은 `dbe992446ea141db79ec5619c3dcc9d56b0068dfd8e730c24c487e291243c11f`이며 production migration·DB·GRANT·credential·traffic은 변경하지 않았다.

**A4 runtime schema CREATE 차단(2026-08-26):** 함수·table·column ACL이 닫혀 있어도 runtime 역할에 `climate_vote` schema CREATE가 열리면 새 overload·shadow object를 만들 수 있는데, 기존 post-apply verifier는 이 DDL 공백을 허용했다. Migration과 rollback은 public·anon·authenticated·authenticator·service_role의 CREATE를 회수하고 verifier는 5개 effective privilege를 전수 대조한다. PostgreSQL 16에서 migration 전 `authenticator` CREATE가 적용 중 회수되는지와 적용 뒤 5개 역할의 독립 재부여가 모두 `dormant privilege contract is unsafe`로 거부된 뒤 정상 복구되는지 확인했다. API 호출에 필요한 schema USAGE는 변경하지 않았다. Bundle artifact 20개 checksum은 `928754e42720e888b8f954d187218b4d043f0e43aae8da006561be087f2f1de2`이며 production migration·DB·GRANT·credential·traffic은 변경하지 않았다.

**A4 함수 exact configuration 결속(2026-08-27):** mutation RPC의 정상 `search_path`·`row_security=off`를 유지한 채 `session_replication_role=replica`를 추가하면 기존 verifier의 포함 검사를 통과해 함수 실행 중 FK·trigger를 우회할 수 있는 공백을 재현했다. Post-apply verifier는 canonical/SHA helper를 포함한 A4 함수 8개 모두의 `proconfig`를 migration 선언값과 exact 대조한다. 기존 포함검사를 쓰던 join-code·authorization helper와 mutation·status 각 두 overload 6개에 위험 설정을 독립적으로 추가한 PostgreSQL 16 리허설은 모두 함수별 안정 계약 오류로 거부됐고, reset 뒤 정상 검증이 복구됐다. Bundle artifact 20개 checksum은 `352503218ffe09256264839c4fcd83547f4465d754bb21980eba098c01d0a369`이며 production migration·DB·GRANT·credential·traffic은 변경하지 않았다.

## 다음 액션 (권장 순서)
1. ✅ **완료 (2026-08-29)** — `PHASE_A_ACTIVATION_DECISION_PACKET.md`의 D1~D6 제품 방향을 권고안대로 승인했다(§0 승인 기록, `DECISIONS.md`). 진행자 전환 시점·공공 데이터 등급·CSAP 등급·provider 적격성·tenancy topology·named pilot·owner는 조건부로 기록했다. production mutation은 승인하지 않았다
2. (별도 승인 시) P1C 휴면 schema 적용·`expect_staff_grants=off` 검증
3. (별도 승인 시) A2 count-only preflight RPC 설치·진단 후 blocker별 Auth·membership 또는 mapping remediation 계획 승인
4. fresh readiness와 별도 승인 뒤에만 A2 GRANT, A4 RPC 권한, staff traffic을 각각 활성화
5. 스크린리더·모바일 보조기기 수동 접근성 평가
6. 지정 프로젝트·세션만 SELECT하고 저장소 밖에 최소 원문 좌표를 쓰는 read-only identity export와 service-role-only RPC adapter·승인 패킷까지 완료. 9/3 live probe에서 service role 직접 SELECT는 `42501`, 읽을 수 있는 legacy snapshot 22개 중 `submission_item` 포함 payload는 0개였다. 패킷의 별도 승인으로 최소 RPC를 설치한 뒤 8/29 provenance map·검수 전 import plan 생성
