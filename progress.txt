# Ralph Progress Log
Started: Thu, Aug 27, 2026 10:32:22 PM
---

## Codebase Patterns

- **빌드는 `npx astro build` 뿐** — `npm run build`는 prebuild 체인이 Node 20에서 죽는다.
  반드시 `export PATH="$HOME/tools/node-v20.18.0-win-x64:$PATH"` 를 앞에 붙인다(Node 24면 조용히 죽음).
  전체 빌드 약 110초 · 7,900여 페이지. 타입 검사는 `npx astro check`(같은 PATH 필요) — 기준선 0 errors / 0 warnings / 49 hints.
- **테스트는 `npx vitest run src/`** — 기준선 58파일 1,004건 통과. `vitest.config.ts` include는
  `src/**/*.test.ts` + `scripts/**/*.test.mjs` 라 **`.tsx` 테스트는 안 잡힌다.** 로직은 순수 `.ts`로
  분리하고 테스트는 `.ts`로 쓸 것(기존 mod 아일랜드가 전부 `*-logic.ts` + `*.test.ts` 짝인 이유).
- **아일랜드 구조** — `src/islands/mod/Xxx.tsx`(React 렌더) + `xxx-logic.ts`(순수 로직) + `xxx-logic.test.ts`.
  DOM·네트워크 없는 로직만 로직 파일에 둔다.
- **Supabase 는 지연 생성** — `src/lib/supabase.ts` 의 `getSupabase()` 가 처음 호출될 때 클라이언트를 만든다.
  모듈을 import 하는 것만으로는 네트워크가 안 열린다. 「네트워크 없이 열려야 한다」는 요건은
  **fetch/subscribe 를 부르지 않는 것**으로 충족된다.
- **미리보기 라우트 패턴** — 인증 게이트 뒤에 있는 화면은 같은 컴포넌트에 `fixtureRows` 같은
  선택 prop 을 주어 픽스처로 띄우는 라우트를 따로 만든다. 컴포넌트에 「미리보기 모드」 분기를
  한 군데(useEffect 초입)만 두고 나머지는 건드리지 않는다.
- **비공개 라우트 3종 세트** — `getStaticPaths` 가 `ko` 만 내고, `<meta name="robots" content="noindex,nofollow">`,
  그리고 `astro.config.mjs` 의 sitemap filter 화이트리스트(`/agenda` + locale home)에 안 걸리게 둔다.
  `data-pagefind-body` 를 안 붙이면 검색 색인에도 안 들어간다.
- **Tailwind 아일랜드를 쓰는 `.astro` 는 `styles/global.css` 를 import 해야 한다.** 안 하면 무스타일로 뜬다.
  본부 화면 계열은 `hq.astro` 의 Pretendard body 폰트 + `.tr-num` 스타일 블록도 같이 복사한다.
- **브라우저 검증 수단은 playwright** — `automation/node_modules/playwright` 에 이미 있다(`automation/verify-canvas-browser.mjs` 참고).
  claude-in-chrome 도구는 이 세션에서 못 쓴다. 스크립트를 `automation/` **안에** 두어야 `import ... from 'playwright'` 가
  풀린다(레포 밖 절대경로 import 는 Windows ESM 에서 `ERR_UNSUPPORTED_ESM_URL_SCHEME` 로 죽는다).
  절차: `npx astro build` → `python3 -m http.server 4477 --directory dist` → playwright → **`netstat -ano | grep :4477` 로 PID 찾아 `taskkill //F //PID`**
  (`pkill -f http.server` 는 이 환경에서 안 먹는다).
- **「supabase 요청 0건」 판정은 외부 호스트로 할 것** — URL 에 `supabase` 로 정규식을 걸면
  빌드 산출물 `/_astro/supabase.<hash>.js`(같은 오리진 정적 청크)가 걸려 1건으로 뜬다. `supabase.co` 로 판정한다.
- **브라우저 검증 스크립트는 `automation/.artifacts/` 에 둔다** — `automation/` 안이라 `playwright` import 가 풀리고,
  `.artifacts/` 는 untracked 라 story 커밋에 안 섞인다. 카드/노드 개수는 **개수만 세지 말고 다중집합 비교**까지
  할 것(「한 장 빼고 한 장 복제」가 개수 검사를 통과한다).
- **UI story 는 스크린샷을 눈으로 볼 것.** DOM 검사를 전부 통과해도 라벨 혼동·줄바꿈 같은 문제는 시각으로만 잡힌다.
- **네트워크 0건 판정 시 Pretendard CDN 을 빼야 한다** — `global.css` 가 jsdelivr 웹폰트를 받으므로
  「외부 요청 0건」이 아니라 「supabase 요청 0건」으로 판정할 것.

---

## 2026-08-27 22:50 - US-001 픽스처 기반 취합 미리보기 라우트
Commit: ralph/submission-aggregation-l1-l4
- `automation/fixtures/0829-submissions.json` 신규 — 15조 × 꼭지 3개 = 항목 65건
  (꼭지1 27 · 꼭지2 21 · 꼭지3 17). 행 모양은 `HqSubmissionRow` 와 동일.
  조 이름·코드는 `조코드표_0829.csv`, 꼭지 문구는 `20260827_s6_open_0829_topics.sql` 에서 그대로 가져왔다.
  조마다 항목 1~3개이고, **조 사이에 겹치는 문장**(예: 버스 배차 간격 = 1분과 1조·3조,
  무더위쉼터 = 2분과 1조·2조, 석탄화력 = 3분과 1조·2조)과 **그 조에만 있는 문장**
  (장애인·축산분뇨·하천정비 등)이 함께 있다 → L1·L2 유사도가 실제로 뭔가를 보여줄 수 있다.
- `src/islands/mod/HqSubmissionBoard.tsx` — `fixtureRows?: HqSubmissionRow[]` prop 추가.
  주면 useEffect 초입에서 `setRows` 하고 즉시 return → fetch·realtime 구독·5초 폴링 **셋 다** 안 건다.
  `token` 을 optional 로 바꾸고 `load()` 초입에 `if (!token) return` 가드. 하단 안내문도 미리보기 문구로 분기.
- `src/pages/[lang]/moderator/insights/submission-lab.astro` 신규 — ko 단일, noindex,nofollow,
  상단에 「실제 조별 산출물이 아닙니다」 배너. `<HqSubmissionBoard fixtureRows={...} client:only="react" />`.
- `src/islands/mod/submission-lab-fixture.test.ts` 신규 6건 — 꼭지 3개·조 15개, 조별 항목 1~3개,
  조 이름 `N분과 M조` 정규식, **겹치는 문장 ≥2 / 단독 문장 ≥3**, HqSubmissionRow 모양.
  겹침을 테스트로 못 박은 이유: 픽스처가 밋밋해지면 L1~L4 화면이 아무것도 못 보여주면서 테스트는 통과한다.
- 검사: `npx vitest run src/` 60파일 1,018건 통과 · `npx astro check` 0 errors · `npx astro build` 성공.
- 브라우저 검증(playwright, dist 정적 서버 :4477) — 꼭지 탭 3개, 각 꼭지 조 컬럼 15개,
  포스트잇 27+21+17=65, 조별↔모아보기 토글 동작, **supabase 요청 0건 · websocket 0건 · 콘솔 에러 0건**.
  증거: `evaluation/2026-08-27-submission-lab-us001-grouped.png`, `...-flat.png`.
- **다음 이터레이션을 위한 학습:**
  - 미리보기 라우트가 이제 있다. US-003/005/008/010 의 브라우저 검증은 전부
    `http://localhost:4477/ko/moderator/insights/submission-lab/` 에서 한다. /hq 는 열 필요 없다.
  - 픽스처를 고치면 `submission-lab-fixture.test.ts` 의 겹침·단독 하한(≥2 / ≥3)에 걸린다. 의도적이다.
  - `buildBoards` 는 **행이 있는 조만** 컬럼을 만든다. 픽스처에서 조를 빼면 그 꼭지의 조 수가 15에서 준다.
  - 카드 id 규격은 `${topic_id}:${team_id}:${item_ordinal}` (board-logic 이 만든다).
    L2 「같은 조 쌍 제외」·L3 「몇 개 조인가」는 `Note.teamId` 를 그대로 쓰면 되고 id 파싱이 필요 없다.
  - 포스트잇은 `section article` 로 잡힌다(조별 뷰). 모아보기는 `article`. playwright 셀렉터로 유용.
  - 겹치는 문장이 **완전히 같은 문자열**인 짝(자카드 1.0)과 **비슷하기만 한 짝**
    (`bus` vs `bus2` — 「버스 배차 간격」 공유)이 둘 다 들어 있다. US-004 의 임계값 0.34 를
    시험할 때 후자가 경계 사례다.
---

## 2026-08-27 23:35 - US-002 L1 유사도 정렬 순수 로직
Commit: ralph/submission-aggregation-l1-l4
- `src/islands/mod/note-similarity.ts` 신규 — 낱말 단위 자카드. 임베딩·네트워크 없음.
  - `SIMILARITY_STOPWORDS`(그·및·등·이·가·을·를·은·는) 상수로 노출 — US-004 가 재사용한다.
  - `tokenize(text)` → 낱말 Set. 소문자화 → 한글/영숫자만 남기고 공백 분리 → 끝 조사 제거
    → 한 글자·불용어 제거. **조사는 떼고도 두 글자가 남을 때만 뗀다**(「제도」→「제」 방지).
  - `similarity(a, b)` → `{ score, sharedTerms }`. **점수만 돌려주는 함수를 만들지 않았다** —
    화면이 「왜 비슷한가」를 항상 낱말로 보여줄 수 있어야 한다.
  - `sharedTerms(a, b)` → 공유 낱말 배열(가나다순 고정).
  - `orderNotesBySimilarity(notes)` → 탐욕적 사슬 재배열. 입력 첫 카드에서 출발, 동점이면 원래 순서.
    입력 배열을 건드리지 않고 새 배열을 돌려준다. 두 장 이하면 그대로 복사.
  - 두 문장 다 셀 낱말이 없으면(0/0) 0 을 돌려준다.
- `src/islands/mod/note-similarity.test.ts` 신규 14건 — 같은 문장 1, 겹침 없으면 0, 불용어만 겹치면 0,
  양쪽 다 낱말 없으면 0, 개수 보존, **순열 보존**(id 다중집합 일치 + 중복 없음), 입력 불변,
  비슷한 카드 인접, 빈/1장 입력, 조사 제거, 「제도」 보존.
  ※ 개수만 세면 「한 장 빼고 한 장 복제」한 버그가 통과한다 → 순열 검사를 따로 뒀다.
- 검사: `npx vitest run src/` **61파일 1,040건 통과** · `npx astro check` 0 errors / 0 warnings / 49 hints.
  UI 를 안 건드리는 순수 로직 story 라 브라우저 검증·`astro build` 는 생략(라우트 변화 없음).
- `src/islands/mod/AGENTS.md` 에 「유사도 판정」 절 추가.
- **다음 이터레이션을 위한 학습:**
  - ★ **US-004 는 불용어와 임계값을 반드시 같이 정해야 한다.** 한쪽만 바꾸면 점수 분포가 통째로 밀린다.
    - 꼭지1 은 「우리는 ○○을 확인하였다」 형식이 **프롬프트로 강제**된다
      (`supabase/migrations/20260827_s6_open_0829_topics.sql:41`). 픽스처만의 문제가 아니라 **실데이터도 그렇다.**
      그래서 `우리`·`것을`·`확인하였다` 가 거의 모든 꼭지1 카드에 겹친다.
    - 픽스처 실측(같은 조 쌍 제외, 임계값 0.34): **꼭지1 18쌍 · 꼭지2 7쌍 · 꼭지3 5쌍.**
      꼭지1 18쌍 중 10쌍은 완전 동일 문장(1.00), 나머지가 0.36~0.46.
      상한 30쌍은 여유가 있다(꼭지1 조합 337쌍 중 18쌍).
    - **함정**: 보일러플레이트를 불용어에 넣으면 진짜 짝도 같이 죽는다. 버스 배차 짝
      0.46 `[간격|것을|배차|버스|우리|확인하였다]` → 보일러플레이트 제거 시 약 0.30 으로 **임계값 아래**.
      반대로 가짜 짝 0.36 `[것을|않는다|우리|주민|확인하였다]`(태양광 vs 재생에너지 — 겹친 낱말 5개 중 4개가
      보일러플레이트)는 약 0.18 로 정상적으로 죽는다.
    - 선택지 둘 — ①템플릿 불용어를 넣고 임계값을 함께 내린다(버스 짝을 경계 테스트로 못 박을 것),
      또는 ②`similarPairs` 안에서 **문서빈도 높은 낱말을 걸러낸다**. `similarPairs` 는 카드 전체를 보므로
      빈도를 셀 수 있고, 짝 단위 `similarity()` 는 못 센다 — ②가 구조적으로 맞는 자리다.
  - `similarity()` 는 `Note` 두 장을 받는다. 같은 조 판정은 `note.teamId` 비교면 된다(id 파싱 불필요).
  - 콘솔 출력으로 실데이터를 훑어보려면 `npx vitest run <파일> --reporter=verbose --disable-console-intercept`.
    기본 리포터는 `console.log` 를 삼킨다.
---

## 2026-08-27 23:40 - 운영 주의: 같은 작업트리에서 Ralph 두 개가 동시에 돌았다
- 이 세션도 US-002 를 골랐는데, **다른 Ralph 인스턴스가 같은 폴더에서 동시에** 같은 story 를 구현해
  `5ad8923` 로 먼저 커밋했다. 이 세션의 커밋 `7e84eb8` 은 결국 progress.txt 중복 항목만 남겼고,
  그 중복 항목을 이 항목으로 걷어냈다. 코드(note-similarity.ts/.test.ts)는 `5ad8923` 것이 정본이다.
- **다음 이터레이션을 위한 학습:**
  - 시작할 때 `git log --oneline -3` 과 `git status` 를 **같이** 볼 것. untracked 로 보이던 파일이
    몇 분 뒤 남의 커밋에 들어가 있을 수 있다(작업트리를 공유하기 때문).
  - Ralph 루프는 한 폴더에서 **하나만** 돌린다. 둘이 돌면 같은 story 를 중복 구현하고
    progress.txt·prd.json 이 서로를 덮어쓴다.
  - 이 PRD와 무관한 dirty 파일이 계속 남아 있다(`SubmissionPanel.tsx` 미리보기 prop, `log.md`,
    `index.md`, agenda json 3종). US-001 이전부터 있던 것이고 **story 커밋에 섞지 않는다.**
---

## 2026-08-27 23:50 - US-003 L1 정렬 토글 UI
Commit: ralph/submission-aggregation-l1-l4
- `src/islands/mod/HqSubmissionBoard.tsx` **한 파일만** 고쳤다. 새 로직 파일은 만들지 않았다 —
  `orderNotesBySimilarity` 가 이미 순수·검증 완료(US-002 테스트 14건)라 토글은 삼항 하나면 끝난다.
  래퍼(`sortNotesForView`)를 만들면 board-logic ↔ note-similarity 순환 import 만 생기고 새로 검증할 것은 없다.
  - `sortMode: 'team' | 'similar'` 상태 추가. **기본은 'team'** — 기존 동작이 그대로 남는다.
  - 「모아보기」에서만 토글이 뜬다. 조별 뷰는 조 순서 자체가 의미라 손대지 않았다(AC 범위도 모아보기다).
  - **거르기 → 정렬 순서**로 뒀다: `filterNotes(...)` 결과를 `orderNotesBySimilarity` 에 넣는다.
    반대로 하면 검색에 안 걸린 카드가 사슬 중간에 끼어 「이웃」이 화면상 엉뚱해진다.
  - 카드 수를 **정렬 버튼 바로 옆**에 뒀다(`data-testid="note-count"`, 「카드 N장」).
    헤더의 「총 N건」이 이미 있지만, 「배치만 바꿨다」는 전환 전후를 같은 자리에서 비교해야 보인다.
  - 안내 문구를 모아보기 그리드 위에 박스로 뒀다 — 「정렬은 배치만 바꿉니다 — 카드가 합쳐지거나 사라지지 않습니다」.
  - 「조별」(보기 방식)과 「조별 순서」(정렬)가 나란히 붙어 헷갈려서 그 사이에 `Eyebrow` 로 「정렬」 라벨을 넣었다.
    **스크린샷을 보기 전에는 몰랐던 문제다** — DOM 카운트만으로는 안 잡힌다.
- 검사: `npx vitest run src/` 61파일 **1,040건 통과**(기준선 유지) · `npx astro check` 0 errors / 0 warnings / 49 hints ·
  `npx astro build` 성공(7,915쪽). 새 테스트는 안 붙였다 — 순수 로직이 안 늘었고 개수 보존은 US-002 가 이미 못 박았다.
- 브라우저 검증(playwright, dist 정적 서버 :4477, `/ko/moderator/insights/submission-lab/`):
  토글 2개 표시 · `aria-pressed` 전환 · **카드 27장 → 27장 불변** · 화면 표시 「카드 27장」 불변 ·
  DOM 순서 실제 변경 · **정렬 전후가 같은 다중집합(순열)** · 「조별 순서」로 되돌리면 원래 순서 복귀 ·
  안내 문구 표시 · 콘솔 에러 0건.
  **같은 문장을 낸 다른 조 짝 10쌍이 전부 gap=1(이웃)** — 버스 배차(1분과1조·1분과3조), 석탄화력(3분과1조·3분과2조),
  무더위쉼터(2분과1조·2분과2조), 야외노동자(2분과3조·2분과5조), 노후주택(1분과2조·1분과4조), 소상공인(3분과2조·3분과4조).
  증거: `evaluation/2026-08-27-submission-lab-us003-similar.png`, `...-team.png`.
- **다음 이터레이션을 위한 학습:**
  - ★ **「supabase 요청 0건」 판정에 함정이 있다.** URL 에 `/supabase/` 로 정규식을 걸면
    빌드 산출물 `/_astro/supabase.<hash>.js`(같은 오리진 정적 청크)가 잡혀 1건으로 뜬다.
    실제 호출만 세려면 **`supabase.co` 같은 외부 호스트로 판정**할 것. US-001 기록의 「0건」과 모순이 아니다.
  - 검증 스크립트는 `automation/.artifacts/verify-submission-lab-l1.mjs` 에 뒀다(untracked).
    `automation/` **안**이라 `import { chromium } from 'playwright'` 가 풀리고, `.artifacts/` 라 커밋에 안 섞인다.
    US-005/008/010/012/013 브라우저 검증은 이 파일을 복사해 고치면 된다.
  - playwright 셀렉터 — 모아보기 카드는 `article`, 본문은 `article > p` 첫 번째, 조 이름은 `article span` 첫 번째,
    카드 수는 `[data-testid="note-count"]`. 정렬 버튼은 `getByRole('button', { name: '비슷한 것끼리' })`.
    **개수만 세지 말고 다중집합(sort 후 join) 비교까지 할 것** — 「한 장 빼고 한 장 복제」가 개수 검사를 통과한다.
  - ★ **UI story 는 스크린샷을 실제로 볼 것.** 이번의 「조별 vs 조별 순서」 혼동은 DOM 검사 전부 통과 상태에서
    눈으로만 잡혔다. 좌표·카운트 샘플로 UI 를 판정하지 말라는 지시가 이 뜻이다.
  - `Postit` 은 `showTeam` 이 true 일 때만 조 이름을 낸다(모아보기). US-005 의 「짝에 조 이름 표시」는
    이 컴포넌트를 그대로 쓰면 된다.
  - 도구 줄(진척 + 도구 박스)이 1600px 에서 이미 꽉 찼다. US-005/012 가 버튼을 더 넣으면
    줄바꿈(`flex-wrap` 은 이미 걸려 있다)되거나 별도 패널로 빼야 한다.
---

## 2026-08-28 00:00 - US-004 L2 닮은 짝 후보 순수 로직
Commit: 1121f4e
- `src/islands/mod/note-similarity.ts` 에 **추가만** 했다(새 파일 없음). US-002 의 `tokenize`·`jaccardOfSets` 를 그대로 쓴다.
  - `SIMILAR_PAIRS_THRESHOLD = 0.34` · `SIMILAR_PAIRS_MAX = 30` 상수로 노출.
  - `similarPairs(notes, threshold?, limit?)` → `SimilarPair[]` = `{ aId, bId, score, sharedTerms }`.
    **점수만 내는 함수를 또 만들지 않았다** — 화면이 「왜 비슷한가」를 낱말로 보여줄 수 있어야 한다.
  - 같은 조 쌍 제외는 `notes[i].teamId === notes[j].teamId` 한 줄. id 파싱 불필요.
  - 임계값 **미만**만 제외(같으면 낸다). 경계를 테스트로 못 박았다.
  - 정렬은 점수 내림차순 **안정정렬**에 기댄다 — `i<j` 로 넣은 순서가 동점의 순서로 남아 결과가 항상 같다.
  - 카드당 한 번만 토크나이즈한 뒤 O(n²) 루프를 돈다(`similarity()` 를 짝마다 부르면 n²번 쪼갠다).
- `src/islands/mod/note-similarity.test.ts` **13건 추가(총 27건)** — 임계값 미만 제외, 임계값 정확히 일치 시 포함,
  같은 조 제외 + 조가 다르면 포함, 내림차순, 동점 순서, 상한(66쌍→5/30), 짝 모양, 입력 불변, 빈/1장 입력.
  픽스처 실측 블록 4건도 붙였다(상한 안, 1.00 이 맨 앞, ★버스짝 경계, 같은 조 쌍 0건).
- 검사: `npx vitest run src/` **61파일 1,053건 통과**(기준선 1,040 + 13) · `npx astro check` 0 errors / 0 warnings / 49 hints.
  순수 로직 story 라 브라우저 검증·`astro build` 생략(US-002 와 같은 선례, 라우트 변화 없음).
- **★ 결정 — 문서빈도(DF) 필터를 넣지 않았다. 다시 논의하지 말 것.**
  US-002 가 남긴 선택지 ②(`similarPairs` 안에서 빈도 높은 낱말 걸러내기)를 **버렸다.** 이유는 US-002 자신의 실측이다 —
  보일러플레이트(`우리`·`것을`·`확인하였다`)를 빼면 **진짜 짝인 버스 배차가 0.462 → 약 0.30 으로 AC 가 못 박은 0.34 아래로 죽는다.**
  DF 필터 + 0.34 조합은 참을 죽인다. 대신 가짜 짝(태양광 vs 재생에너지 0.36)은 **살려 두고 사람이 거른다** —
  L2 는 설계상 「AI 제안 — 확정은 사람이 합니다」이고, 짝마다 `sharedTerms` 가 붙으므로
  `[것을|우리|확인하였다|주민|않는다]` 를 본 사람이 건너뛸 수 있다. **거짓 제안보다 참 누락이 더 나쁘다**는 판단이다.
  DF 를 도입하려면 임계값을 **같이** 내려야 하고, 그때 `★ 경계 — 「버스 배차」 짝` 테스트가 소리 내며 깨진다. 그러라고 둔 테스트다.
- **다음 이터레이션을 위한 학습:**
  - 픽스처 실측(임계값 0.34, 같은 조 제외) — **꼭지1 18쌍(그중 10쌍이 완전 동일 문장 1.00, 나머지 0.36~0.46) ·
    꼭지2 7쌍 · 꼭지3 5쌍.** 상한 30 은 셋 다 여유가 있다(잘리는 꼭지 없음).
  - ★ **꼭지2·꼭지3 은 1.00 짜리(완전 동일 문장)밖에 없다.** US-005 의 「닮은 짝」 패널을 꼭지2/3 에서 열면
    점수가 전부 1.00 이라 **패널이 잘 도는지 눈으로 판정이 안 된다.** 브라우저 검증은 **꼭지1 에서** 할 것
    (0.36~0.46 짜리 8쌍이 거기에만 있다).
  - US-005 가 쓸 것 — `SimilarPair` 는 **id 만** 들고 있다. 원문·조 이름은 화면에서 `Note` 를 id 로 찾아 쓴다.
    보드의 노트는 `board.teams.flatMap(t => t.notes)` 로 평평하게 얻고 `new Map(notes.map(n => [n.id, n]))` 로 찾으면 된다.
    조 이름 표시는 `Postit` 의 `showTeam` 을 그대로 쓴다(US-003 기록 참조).
  - US-005 의 「체크」는 **카드를 지우거나 합치지 않는다** — 두 카드에 같은 표시만 붙이는 상태값이다.
    `similarPairs` 는 순수하고 상태를 안 갖는다. 체크 상태는 UI 쪽 `useState`(또는 US-007 이후 영속화)로 따로 둔다.
  - `vitest.config.ts` include 가 `src/**/*.test.ts` 라 **`.artifacts/` 에 둔 임시 측정용 테스트는 안 잡힌다.**
    실데이터를 콘솔로 훑어보려면 파일을 잠시 `src/islands/mod/zz-*.test.ts` 로 두고
    `npx vitest run <파일> --reporter=verbose --disable-console-intercept` 후 **지울 것**(커밋에 섞이면 안 된다).
---
