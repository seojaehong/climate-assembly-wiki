# Ralph Progress Log
Started: 2026-08-31 (제출 내구성 + 마감 카운트다운 + 한글 1단계)
브랜치: ralph/submission-resilience-0912 (base = origin/main 4b4538f)
이전 run(parser-internalization, 8/8 완료)은 archive/2026-08-31-parser-internalization/ 에 있다.

## Codebase Patterns
(모든 이터레이션이 먼저 읽는다. 재사용 지식만 — story 세부는 아래 로그로)

### 이 저장소 규약
- 빌드는 **Node 20 포터블 필수**: `export PATH="$HOME/tools/node-v20.18.0-win-x64:$PATH"`.
  Node 24 는 astro build 가 무출력 사망
- `npm run build` = prebuild + astro + postbuild. `npx astro build` 만 쓰면 postbuild 훅이 안 걸려
  `dist/deployment-revision.json` 이 안 생긴다
- ★ **prebuild 가 `index.md`·`log.md`·`public/workshop-graph/data/*.json` 을 재생성해 워킹트리를 더럽힌다.**
  커밋 전에 `git checkout` 으로 되돌릴 것
- `package.json` 에 `test` 스크립트가 **없다**. `npx vitest run` 을 쓴다
- **테스트 기준선 = 90 파일 / 1,516 테스트 전부 통과** (2026-08-31, 이 브랜치 시작 시점)
- ★ **`.tsx` 테스트는 vitest include 에 안 잡혀 조용히 통과한 것처럼 보인다.**
  검증할 판단 로직은 반드시 `.ts` 로 뺀다 (`src/islands/mod/AGENTS.md` 파일 짝 규칙)
- 검증 규약: `scripts/verify-*.mjs` — **N/N 개수로 출력**, DB 에 쓰지 않는다
- ★ `verify-name-reparse.mjs` 는 규칙을 베끼지 않고 **esbuild 로 `.ts` 를 그 자리에서 변환해 import** 한다.
  단 `data:` URL 은 bare specifier 가 해석되지 않으므로 **변환본을 저장소 안 실제 `.mjs` 로 쓴 뒤
  `pathToFileURL()` 로 import** 할 것
- **의존성 설치**: `npm install --omit=optional` **금지**. rollup·esbuild·lightningcss 네이티브 바이너리가
  빠져 `astro build` 가 `Cannot find module '@rollup/rollup-win32-x64-msvc'` 로 죽는다
- **OneDrive EPERM**: node_modules 를 지우는 코드는 재시도를 감쌀 것. 편집이 검사와 커밋 사이에
  롤백된 사례가 있으니 긴 빌드 전후 `git diff --stat` 확인

### 이 화면(조 콘솔)의 구조 — 2026-08-31 코드 직독 확인
- **`/mod` 가 곧 조 화면.** 조는 `/mod?code=082901` 로 들어온다 (`ModConsole.tsx:1392`)
- 조의 **기본 탭은 `submission`** (`mod-tabs.ts:24-28`) → 조에게 항상 보여야 할 것은 탭 **바깥**에 둔다
- 꼭지 하나 = `TopicSection` 하나, 상태·저장·최종제출을 각자 가짐 (`SubmissionPanel.tsx:189`)
- 초안 키 = `climate_vote_draft:${code}:${topic.id}` (`SubmissionPanel.tsx:207`)
- `submission_save` 는 **items 전체 교체 = last-write-wins** (`20260808_s1_*.sql:190`)
- ★ 저장 성공 시 **`dropDraft()` → `loadSubmission()` 순서 고정.** 뒤집으면 서버가 나눈 결과(s15)가
  화면에 안 오고 「저장 안 함」으로 남는다 (2026-08-30 실화면 버그, `SubmissionPanel.tsx:318-334`)
- 본부 RPC 권한 = `attendance_token_row(p_token)` → `scope='hq'` (`20260827_s7_*.sql:25`)
- ★ `topic_list` 는 `returns table` 이라 컬럼 추가 시 **DROP 후 CREATE**. **grant 재부여 누락 시 조 화면 전면 장애**
- 네트워크·DB 없이 화면 검증하는 픽스처 라우트:
  `src/pages/[lang]/moderator/insights/submission-lab.astro` (+`-extreme`),
  데이터 `automation/fixtures/0829-submissions.json`

### 마이그레이션 폴더 — 트랙이 둘이고 사전순이 갈린다 (2026-08-31 실증)
- 파일명 사전순에서 숫자(`2`)가 알파벳(`p`)보다 앞이라 **`platform_*.sql` 이 모든 `2026*` 파일보다
  뒤에 적용된다.** 즉 날짜 파일로는 platform 트랙이 정의한 함수를 이길 수 없다
- ★ **새 마이그레이션을 쓰기 전에 `grep -n "<함수명>" supabase/migrations/platform_p*.sql` 을 돌릴 것.**
  두 트랙이 같은 함수를 정의하면 전체 재생 시 platform 쪽이 이긴다
- 2026-08-31 전수 확인: 겹치는 함수는 **`submission_save_v2` 하나뿐**(platform_p2:204-205 가 상한 30).
  `topic_list` 는 platform 쪽에 없다 → **US-007 은 이 함정에 안 걸린다**
- 행 수 상한(200)은 **표 check 제약이 아니라 RPC 안의 `jsonb_array_length(p_items) > N` 가드**다.
  "제약을 고쳤나"가 아니라 "어느 파일이 그 함수를 마지막으로 정의하나"가 판정 기준

### 한글(HWP/HWPX) — 2026-08-31 실증
- `vendor/kordoc` 은 **읽기 전용이 아니다.** `markdownToHwpx` · `fillHwpx` · `patchHwp` · `patchHwpx` ·
  `extractClickHereFields` · `validateHwpx` 가 있고 전부 실제로 돌아간다
- 다만 `dist/index.js` 가 `fs`·`zlib`·`crypto`·`stream` 을 정적 import 해 **브라우저 번들은 안 된다**
  (`zlib.deflateSync`·`crypto.createHash` 를 실제로 쓴다). Node 에서만 쓸 것
- ★ **한글 COM 자동화(`pyhwpx`)는 루프에서 실행 금지** — 앱 창이 떠서 멈춘다. 사람이 수동으로

---

## 2026-08-31 23:40 - US-001 (R0 — 꼭지당 행 수 상한 200 저장소·운영 드리프트 확인)
Commit: ralph/submission-resilience-0912
- **판정: 파일로 고정됨.** 상한 200 은 저장소 파일에 재기준화돼 있다. 그래서
  AC 조건("이미 고정돼 있으면 만들지 않는다")에 따라 `s17_item_cap_200.sql` 을 **만들지 않았다**
- 근거 줄번호
  · `20260830_s15_*.sql:132-133` submission_save 를 create or replace 로 200 재기준화
  · `20260830_s15_*.sql:193-194` submission_save_v2 도 같은 파일에서 200
  · `20260830_s15_*.sql:140·200` 분해 결과 200 초과 시 나누기 포기(글자 유실 없음)
  · `20260808_s1_*.sql:201-202` = **30** (옛 값). s15 가 사전순 뒤라 덮인다 = 드리프트 아님, supersede
  · `20260808_s1_*.sql:76-78` submission_item 의 check 는 kind·content(1~2000)·rationale(≤2000) 뿐 —
    **행 수 check 제약은 애초에 없다.** 상한은 RPC 가드로만 존재
  · 화면 쪽 상수도 200: `submission-panel-logic.ts:29` · `submission-guide.ts:20`
- **새로 찾은 잔여 위험 (이번 story 의 실제 수확)**
  `platform_p2_analysis_review.sql:204-205` 가 `submission_save_v2` 를 **상한 30 으로 다시 정의**하는데,
  `platform_*` 이 사전순에서 `2026*` 보다 **뒤**다 → 폴더 통째 재생·platform 트랙 적용 시 v2 만 30 회귀.
  · 지금 안 터지는 이유: 화면은 v2 를 안 부른다(`src/lib/deliberation.ts:107` 이 submission_save 만 호출),
    그리고 `platform_p2_*.sql:3` 이 스스로 「병합 전까지 프로덕션 미적용」이라고 적고 있다
  · ★ **날짜 파일(`2026…_s17_item_cap_200.sql`)을 만들어도 이 구멍은 안 막힌다** — 그 파일도 platform 보다 앞선다.
    고칠 주체는 platform 트랙. 이 저장소에서 p2 를 고치지 않는다(다른 트랙 파일)
- 변경 파일: `supabase/migrations/AGENTS.md` (「행 수 상한 드리프트」 절 신설) · `prd.json` · `progress.txt`
- 운영 DB 에 **SQL 을 한 줄도 보내지 않았다.** 판정은 전부 파일 직독
- 검사: `npx tsc --noEmit` 통과 · `npx vitest run` **90 파일 / 1,516 테스트 전부 통과**(기준선 유지, 신규 실패 0)
- **다음 이터레이션을 위한 학습:**
  - platform 트랙 사전순 함정은 위 `## Codebase Patterns` 에 올렸다. US-007 전에 그 grep 을 돌릴 것
  - `topic_list` 는 platform 쪽에 없으므로 US-007 의 DROP·CREATE 는 트랙 충돌 걱정 없이 진행 가능
  - 상한을 바꿔야 할 일이 생기면 고쳐야 할 곳은 4군데다: s15 의 submission_save·submission_save_v2,
    `submission-panel-logic.ts:29`, `submission-guide.ts:20`
---
