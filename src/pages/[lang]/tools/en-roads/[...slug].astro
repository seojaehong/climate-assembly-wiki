---
/**
 * /[lang]/tools/en-roads/[...slug] — DISABLED (2026-06-02 privacy)
 *
 * Public surface restricted to homepage + agenda pages only.
 * tools/en-roads/{ko,en}/ source markdown preserved on disk; route
 * emission suppressed. To republish: restore the previous body from git.
 */
export async function getStaticPaths() {
  return [];
}
---
