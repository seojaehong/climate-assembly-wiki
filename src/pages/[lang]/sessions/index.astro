---
/**
 * /[lang]/sessions/ — DISABLED (2026-06-02 privacy)
 *
 * Public surface restricted to homepage + agenda pages only.
 * Source file preserved; route emission suppressed by returning [].
 * To republish: restore the previous getStaticPaths body from git history
 * (commit prior to "chore(privacy): restrict public surface").
 */
export async function getStaticPaths() {
  return [];
}
---
