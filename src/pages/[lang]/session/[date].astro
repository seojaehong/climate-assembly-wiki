---
/**
 * DEPRECATED — superseded by sessions/[n].astro (D3-p, plural+ordinal).
 * URL pattern was /[lang]/session/{date}; new pattern is /[lang]/sessions/{n}.
 * Return empty to suppress route generation.
 * TODO: Delete this file when PowerShell -LiteralPath deletion is confirmed.
 */
export async function getStaticPaths() {
  return [];
}
---
